# Sofra — Progress

> Thorough restart reference. If resuming a new session, read this first.

## Current State (facts)
- **App:** Sofra — family meal-planning app. React Native / Expo + Firebase (Firestore/Auth/Storage) + TypeScript. Zustand state, React Navigation, React Native Paper. Rebranded ThaliPlan → Sofra.
- **Design:** Terracotta & Sage theme (light/dark/auto), Fraunces (display) + Inter (body), RN Animated motion. All screens dark-aware.
- **MVP1 features:** multi-dish meals + per-dish ratings, kids-tiffin planning, weekly auto-planner, local notifications, Insights charts, restaurants + dish library, in-app account deletion.
- **Health:** `tsc=0`, **46/46 Jest tests pass**. Latest RELEASE APK on Pixel 10 Pro (device-verified, no crashes).
- **Website:** `sofra.savvylabs.dev` (GitHub Pages) — fully branded with 7 SVG feature icons, dark-mode aware, 9-screen gallery. Live.
- **Firebase:** project `thaliplan`. Firestore + Storage rules deployed. Android Firebase config verified (SHA-1 ready).
- **Deployment:** Android path documented + ready (Play verification → `eas build` → internal testing). **iOS path FULLY documented** (4 guides: checklist, Firebase setup, App Store submission, quick-ref). Code is iOS-ready (no changes needed).

## Last Session (2026-08-05 — GOOGLE SIGN-IN FIXED (wrong SHA-1) ✅ + closed test SUBMITTED for review ✅)
- **✅✅ GOOGLE SIGN-IN ROOT CAUSE = WRONG Play-signing SHA-1 in Firebase.** Pulled `adb logcat` from Sameer's device during a live failed sign-in: `GoogleSignatureVerifier: package info is not set correctly` + `Auth [GetTokenResponseHandler] Server returned error: This android application is not registered to use OAuth2.0 … SHA-1 mismatch`. Pulled the installed `base.apk` (`installer=com.android.vending` = genuine Play build) and ran `apksigner verify --print-certs`: **real Play App Signing cert = SHA-1 `41:C8:B0:C8:40:C6:71:9F:5D:C3:14:06:4D:A2:1E:76:B7:2F:C1:F9`, SHA-256 `3A:82…1A:AB`.** Firebase had the SHA-256 RIGHT (`3A:82…`) but the SHA-1 WRONG (`17:F7…` belongs to a different key — mis-copied from Play's new "quantum-ready" App-signing page which lists multiple keys). Google Sign-in legacy API matches on SHA-1 → rejected every install. **FIX: added `41:C8…C1:F9` to Firebase → Android app.** Firebase auto-created the matching OAuth client (`349329204088-mvhn…`, Android) within ~1 min. **Sameer confirmed sign-in WORKS on device.** Global fix (project-level, all Play-signed installs — internal/closed/open/prod all re-signed with `41:C8`).
- **✅ `google-services.json` REFRESHED in repo** (now all 5 Android OAuth clients incl. `41:C8`) — **committed `59acdbb` + MERGED to `main` (fast-forward) + PUSHED to origin.** `android-play-launch` now fully contained in `main`; PR #8 auto-closes.
- **✅ VIDEO PROMPTS delivered → `docs/video-prompts-studio.md`**: (A) app-walkthrough storyboard (VO script + scene map using generic `docs/assets/screens/*.png`); (B) 3 brand-commercial prompts, one arc localized 3 ways — American/English, N-Indian desi/Hindi, Maharashtrian/Marathi (in-language VO lines + tagline supers + negatives). KEY RULE captured: never let AI draw the app UI/logo/text — composite real screens in post; re-record Hindi/Marathi VO with native speakers.
- **✅ 2 MISSING walkthrough screens RENDERED** (Rivera-Family generic, 1280×2706, matching existing mockups): `restaurant.html`/`profile.html` in `assets/brand/marketing/screens/` → PNGs `docs/assets/screens/restaurant.png` (restaurant detail: dishes + star ratings) + `profile.png` (profile/settings: name/photo edit, household+invite code, appearance/notifications, sign-out/delete). Chrome-headless render pipeline: `chrome --headless=new --window-size=1280,2706 --virtual-time-budget=4000 --screenshot=<abs .png> file:///<abs .html>`.
- **✅ CLOSED TESTING (Alpha) SUBMITTED FOR REVIEW.** The Alpha track had a draft release w/ NO bundle. Via Chrome: added AAB v3 (versionCode 3, 1.0.1) from library → release notes present → assigned tester list "sofra Test" (2 users) → track now ACTIVE → Publishing overview → **Submitted 14 changes for review** (closed-testing rollout + store listing + all app-content declarations + category). Now "Changes in review" (quick-checks ~6 min → Google review ≤7 days). This triggers the identity review that FIXES the temp name "com.thaliplan.app (unreviewed)" → "Sofra".
- **⛔ NEXT FOR SAMEER (production gate):** closed test now has only **2 testers**; Google requires **≥12 opted-in testers for 14 continuous days** before PRODUCTION. Add ≥10 more emails to the "sofra Test" list (Play Console → Closed testing → Testers) and have them opt in via the closed-test link. Review of the current submission proceeds regardless.

## Prior Session (2026-08-05 — Android AAB LIVE on internal testing ✅ + SHA-1 in Firebase ✅)
- **✅✅ AAB UPLOADED to Play internal testing** via `eas submit` (build `b221a8bb`, v1.0.1, versionCode 3). **Root cause of the 4 earlier failures = the service account had account-level "Release to testing tracks" but NOT app-level "Manage store presence"; EAS's submit touches the store-presence edit, so it 403'd.** FIX: Users&perms → SA → App permissions → Add app → Sofra → +Manage store presence → Save. Next submit succeeded instantly ("Submitted your app to Google Play Store! All done!"). NOT propagation after all.
- **✅ SHA-1 + SHA-256 (Play App Signing key) ADDED to Firebase** (thaliplan → Android com.thaliplan.app). New: SHA-1 `17:F7:87:3F:A2:40:47:E6:EF:49:FD:DF:E6:E5:23:E9:50:D5:F0:DB`, SHA-256 `3A:82:2E:74:70:5E:9C:9C:5B:82:1D:AA:87:2B:1B:AC:2E:84:37:05:40:B4:51:56:16:5E:86:D1:65:EC:1A:AB`. Now 4 SHA-1s + 1 SHA-256 total (debug 5E:8F, EAS upload EE:40, 2D:0B, Play-signing 17:F7). Google Sign-in will work on the Play build. SHA-256 future-proofs App Check/Play Integrity.
- **⛔ REMAINING FOR SAMEER:** (1) **Upload 8 store screenshots + icon + feature** (drag from `assets/brand/marketing/android/`, Chrome file_upload can't). (2) **Roll out the DRAFT internal release** (eas.json `releaseStatus:draft` → Play Console → Internal testing → review & roll out) + **add internal testers** + share opt-in link. (3) Then smoke-test the store build (Google Sign-in especially).
- **NOTE:** personal Play acct ⇒ 12-tester/14-day CLOSED testing required before PRODUCTION; internal testing is instant (≤100 testers) — start there.
- **RELEASE NOTES drafted for Sameer (paste into Internal testing → Release notes `<en-US>`, 500-char cap):** debut v1.0.1 copy — full store version (welcome + 6 feature bullets), concise version, tester-checklist version. Given in chat; not stored in a doc.
- **✅ STORE ASSETS ALL UPLOADED (by Sameer):** verified in store listing — app icon SET, feature graphic SET, phone screenshots "8 of 8" assigned (`sofra_shot_01..08`, all in asset library). Store listing COMPLETE. So the "Chrome can't upload" blocker is moot — Sameer did it.
- **⛔ APP NAME still shows "com.thaliplan.app (unreviewed)" b/c app = DRAFT, not yet REVIEWED.** Name+icon ARE "Sofra" in console. Clears only after Google review. **"Send app for review" is DISABLED** (Publishing overview): "complete required steps in dashboard." Pending/blocking items: **Content Rating = "Submit new questionnaire"** (needs submitting — a declaration), **Target audience = "18 and older"** (defensible: avoids Families policy; confirm w/ Sameer), Closed-testing Alpha country settings. Asked Sameer: shall Claude fill content-rating (standard meal-app answers: no violence/sexual/gambling) + send for review, or does he answer it. Internal testing WORKS now regardless — temp name is cosmetic.
- **GOOGLE SIGN-IN fix = wait ~1h (propagation), key is CORRECT** — see below.
- **✅ ADVERTISING ID declaration DONE (Claude, "No")** — was the actual last "needs attention" App-content item (no ad/analytics SDKs; consistent w/ no-ads data safety). App content now "caught up with everything."
- **⛔ NAME "com.thaliplan.app"→"Sofra" REQUIRES app REVIEW, which for a PERSONAL acct requires COMPLETING A CLOSED TEST — internal testing never triggers the identity review.** Blocker = "Closed testing - Alpha" track is a DRAFT (177 countries set, but NO app bundle attached, not rolled out) → "Send app for review" stays disabled. PATH: attach AAB v3 to Alpha → add ≥12 testers → roll out (triggers review → name fixes ~1-2 days) → run 14 days → apply for production. The 12-testers/14-days is a hard Google gate. **ASKED Sameer: shall Claude roll out the closed Alpha test now (closed to tester list, 177 countries) to trigger review + start the clock?** Awaiting his yes. Internal testing works today regardless; temp name is cosmetic.
- **✅ INTERNAL RELEASE IS LIVE + tester bug fixed:** release 1.0.1 = "Available to internal testers" (rolled out 5 Aug 18:23). Sameer's "no email / app not available" = TWO things: (1) internal testing NEVER emails — opt-in link is the only way in; (2) his email was mistyped in the tester list as `sameerrajwde@gmail.com` (missing the 'a') so his real account wasn't authorized. FIXED via Chrome: list "sofra Test" now = `sameerrajwade@gmail.com` + `asmitakhude@gmail.com`. **Opt-in link: https://play.google.com/apps/internaltest/4701720489862037731** — open on phone signed into the EXACT listed account → Become a tester → install (first release can take up to ~2h to propagate). App shows temp name "com.thaliplan.app (unreviewed)" until Google review — normal.

## Prior Session (2026-08-05 earlier — Android Play Console launch, autonomous)
- **GOAL:** publish Sofra to Play Store internal testing autonomously (Sameer away). Driven via Chrome automation on the Play Console + Google Cloud Console.
- **✅ DONE:** Play Console app "Sofra" (com.thaliplan.app) created; **all 9 compliance forms complete** (privacy policy, sign-in details, ads, content rating, target audience, data safety, government apps, financial features, health); category=Food & Drink + contact email/website set; **store listing TEXT saved as draft** (title "Sofra", short desc, full desc 1340 chars).
- **✅ SERVICE ACCOUNT for `eas submit` created** (the API path, since personal Play accounts lack the old API-access UI): enabled **Google Play Android Developer API** on GCP project `thaliplan`; created SA **`play-console-publish@thaliplan.iam.gserviceaccount.com`**; downloaded JSON key → **`play-service-account.json` at repo root (gitignored ✅, never committed)**. `eas.json` already points at this path.
- **✅ SERVICE ACCOUNT PERMISSION GRANTED (Sameer, in Play Console):** `play-console-publish@…` is now an Active user with **Release to testing tracks + Release to production** (verified saved). `eas submit` pipeline now authenticates + sees `com.thaliplan.app` end-to-end.
- **⏳ AAB UPLOAD STILL FAILING — permission not yet API-visible:** 4 `eas submit` runs failed with errors that *flip-flop* between "caller does not have permission" ↔ "missing necessary permissions" (incl. a retry 20min after grant, bash `b6j16t8h9`). Flip-flop = Play API eventual-consistency propagation across Google replicas (Google: up to 24h). Current permission is **ACCOUNT-level** only. **NEXT FIX (higher probability, asked Sameer): add APP-level grant** — Users&perms → the SA → App permissions tab → Add app → Sofra → check "Release to testing tracks" → Save (this is what Expo's pre-check wants; page left open in Chrome). **Fallback auto-retry running: bash `br6q0g7ml` sleeps 30min then re-submits.** Build ready: `b221a8bb`, v1.0.1, versionCode 3. Internal track still shows No releases/bundles.
- **✅ ANDROID STORE SCREENSHOTS — full 8-set generated (commit `9bb9507`):** iOS shots (1290×2796 = 2.17:1) are REJECTED by Play (max 2:1) — that was Sameer's "sizing" error. Built dedicated Android set at **1080×1920 (9:16), 24-bit, no-alpha, ~370KB** via 5 new branded `shot_*.html` (addmeal/calendar/celebration/share/household) + existing home/plan/insights → Chrome headless @2× → sharp downscale/flatten. Numbered `sofra_shot_01..08_*.png` for upload order; captions matched to actual screens (household src=signup, celebration src=plan+confetti). Old un-numbered PNGs removed. Icon 512² + feature 1024×500 already valid.
- **⛔ STILL NEEDS SAMEER (independent of the above):**
  1. **Upload store-listing VISUAL ASSETS** (Chrome `file_upload` only accepts chat-attached files): drag from `assets/brand/marketing/android/` → `sofra_icon_512.png` (App icon), `sofra_feature.png` (Feature graphic), `sofra_shot_01..08_*.png` (8 phone screenshots).
  2. **SHA-1 → Firebase** (after AAB uploads) — Claude will pull the Play App Signing SHA-1 (Play Console → App integrity) and hand it ready-to-paste; adding a fingerprint to Firebase is the same account-config change the safety gate blocks Claude from finalizing.
- **THEN:** verify Google Sign-in on the Play build → send Sameer the phone notification.
- **PR:** https://github.com/sameerrajwade/Sofra/pull/8 (branch `android-play-launch`) — PROGRESS + marketing assets. `play-service-account.json` gitignored, never committed.
- Play Console IDs: developer acct `8107930340888208101`, app `4972790754924510458`. AAB URL: https://expo.dev/artifacts/eas/wqkWv--_wyKbiDhAYmWmrpSTW9PtpHUxpgFZZKqDXEs.aab

## Prior Session (2026-08-04 — iOS 1.0.1 SUBMITTED FOR REVIEW ✅ + off sdk57 branch)
- **BRANCH:** left `sdk57-upgrade`; now on **`main`** (fast-forwarded to merge `3a20e1a` = all SDK57/iOS work). PROGRESS.md edit uncommitted (as before). Backup branches intact.
- **iOS GO-PUBLIC DONE via ASC (Chrome):** app was "1.0 Waiting for Review" w/ Build 5 (=1.0.0, has the New-Arch photo bug) — never released, so still the DEBUT. Removed 1.0 from review → edited **Version field 1.0→1.0.1** → removed Build 5, **selected Build 6 (1.0.1)** → Saved → **Add for Review → Submit for Review → "1 Item Submitted"**. Now **"1.0.1 Waiting for Review"**, review ≤48h, email on completion. No export-compliance prompt (ITSAppUsesNonExemptEncryption=false already set).
- **Release = "Automatically release this version"** (already selected) → goes LIVE to everyone on Apple approval, no manual step. Territories = all except EU (non-trader).
- **Decisions (Sameer, confirmed):** demo login `testuser-apple@testuser.com` is real + email-verified (works for reviewers); KEPT existing 5 branded iPhone 6.5" screenshots (Apple-accepted, generic data) — did NOT swap to newer 3-shot brand set. What's New NOT needed (debut release, app never public).
- **NEXT (Apple side): just wait for review.** If APPROVED → auto-releases publicly (done). If REJECTED → read reason in ASC App Review, fix, resubmit. Watch email.
- **HERO SCREENSHOT (this window):** built a clean Sofra title card (icon tile + Fraunces wordmark + sage rule + "Plan meals, remember it all" tagline, warm-paper bg). Generator = `scratchpad/gen-hero.js` (Chrome headless @DSF, fonts base64-embedded; explicit px width fixes a viewport off-center bug). Output in `assets/brand/marketing/ios/`: `sofra_ios67_brand_hero.png` (1290×2796), `sofra_ios65_brand_hero.png` (1242×2688), `sofra_ios_ipad_brand_hero.png` (2048×2732). Sameer approved design + wants it as FIRST screenshot + iPad too.
- **LAUNCH TEASER KIT (this window) → `docs/launch-teaser.md`:** for a surprise WhatsApp announce. Built 4K 9:16 brand cards (`assets/brand/marketing/ios/sofra_video_endcard_9x16_4k.png` w/ website pill + `sofra_video_titlecard_9x16_4k.png` clean); generator `scratchpad/gen-endcard.js`. Doc has: ready-to-paste **Veo 3** prompts (2× 8s text-to-video human/kitchen clips + optional image-to-video brand reveal using the title card) + negative prompt; edit plan (stitch 2 clips + overlay end-card last ~2.5s + app-screen insert; upscale to 4K); WhatsApp message copy (short + warm); TestFlight beta-onboarding note (external/public-link, needs beta review). **Claude can't render video — Sameer runs Veo 3 (Flow/Gemini), outputs ≤1080p → upscale.** Key rule: never let Veo draw the real UI/logo/text (use screen-rec + the PNG cards). Website = https://sofra.savvylabs.dev.
- **✅ HERO ADDED + RESUBMITTED — "1 Item Submitted" again.** Chrome `file_upload` only accepts chat-attached files (repo AND scratchpad paths both rejected), so **Sameer dragged the heroes into ASC himself + reordered**: iPhone 6.5" now 6 shots (Sofra hero #1), iPad 4 shots (hero #1). Verified Build 6 / 1.0.1 / demo login / "Automatically release" all intact → Add for Review → **Submit for Review DONE**. Back in queue, ≤48h, auto-release to everyone on approval. **iOS launch = fully staged; just awaiting Apple.** (Next update version → wire the hero into that release's screenshots too.)

## Prior Session (2026-08-04 — iOS live + Android APK install + photo-upload fix)
- **iOS SHIPPED:** app is LIVE on the App Store; internal testers added ✅ — iOS launch path done.
- **#BUG photo/avatar upload broke on BOTH platforms after SDK57 (New Arch)** — root cause: `fetch(uri).blob()` in `storage.ts` is broken under RN New Architecture (SDK52→57), so `uploadProfilePicture` threw → "photo couldn't upload" alert. FIX (no new deps): AvatarPicker now uses image-picker `includeBase64:true` → passes `{uri,base64,type}` (new exported `PickedImage`); `storage.ts` `uploadProfilePicture(userId, base64, contentType)` → `uploadString(ref, base64, 'base64', {contentType})` (also satisfies Storage rule's `contentType.matches('image/.*')`); ProfileScreen consumes new shape. **tsc=0, 50/50 tests.** NOT committed yet. Files: `src/components/AvatarPicker.tsx`, `src/services/storage.ts`, `src/screens/ProfileScreen.tsx`.
- **LOCAL Android build UNBLOCKED via short path:** copied repo (robocopy, minus .git/build caches) to **`C:\S`** → building `assembleRelease` there dodges New-Arch MAX_PATH (Ninja mirrors source path inside build dir, so short root shrinks it twice: ~298→~252 <260). Release signs w/ debug keystore (SHA-1 5E:8F… already in Firebase → Google Sign-in works, no EAS SHA-1 needed). **BUILD SUCCESSFUL in 34m11s (exit 0) — MAX_PATH SOLVED by short path.** APK: `C:\S\android\app\build\outputs\apk\release\app-release.apk` (94 MB), signed w/ debug keystore (5E:8F… already in Firebase → Google Sign-in works). Install needs uninstall of current EAS-signed build first (diff key EE:40 vs 5E:8F; data safe in Firebase). adb device still connected `192.168.86.121:35499`.
- **COMMITTED `4662106` (photo fix + share link + card CTA + name edit + version→1.0.1). EAS iOS PRODUCTION build 6 RUNNING** = build `b8883869` (auto-inc 5→6, creds valid, Team 3HQ9T7PKN4). Latest local APK copied into DiningPlanner `android/app/build/outputs/apk/release/` (gitignored). NEXT after build: `eas submit -p ios` → then ASC (Chrome) attach build 6 + What's New + submit for review.
- **✅ VERSION = 1.0.1 (Sameer confirmed).** iOS build 6 (`b8883869`, 1.0.1) built + **`eas submit -p ios` DONE** (submission `7fa31931`, ASC App ID 6797710095, non-interactive via stored API key) → **STUCK IN EAS FREE-TIER QUEUE (30+ min, status=Queued, "waiting for submission process to start")** — NOT a failure, just free-tier congestion (yesterday's submit was fast bc queue was empty). Build 6 has NOT reached Apple yet (ASC TestFlight still shows only 1.0.0 build 5 Complete from Aug 3; builds 4 & 2 Failed). CLI process alive+polling. A DUPLICATE submit (c845ee17, 11:28) got started (Sameer ran cmd manually / thought Ctrl+C cancelled it — it did NOT; Ctrl+C only stops local CLI, EAS submission keeps running server-side). Claude CANCELLED the duplicate in the EAS dashboard (now =Canceled). **Clean state: ONE original submission `7fa31931` (build 6, 1.0.1) still QUEUED free-tier ~45min, not started.** Options: wait (free queue clears eventually, non-urgent — app not public yet) OR pay EAS priority. Awaiting Sameer's call.
- **WEBSITE + ASSET REORG + MERGE-TO-MAIN (this session):** (a) Split marketing assets by platform: NEW `assets/brand/marketing/android/` (Play icon/feature/branded shots + HTML srcs) mirroring `ios/`; shared social/brand assets stay at root; `screens/` stays shared. **GOTCHA: `.gitignore` had bare `android/` → ignored the new folder; anchored to `/android/` (root RN build only) so marketing android/ is tracked.** (b) Website: DELETED `docs/architecture.html` (exposed Firestore collection/doc schema) + stripped its nav/footer links across all pages; `#get` now shows App Store + Google Play (coming-soon, no dead links, branded SVGs); FAQ expanded (platforms=iOS+Android, Apple Sign-In, photo+name edit); security.html emoji→branded line-SVG icons. privacy.html Firebase mention KEPT (legit sub-processor disclosure). No new app screens → gallery unchanged. **✅ MERGED to main: PR #7 (merge commit `3a20e1a`, 10 commits — whole SDK57/iOS line). GitHub Pages (main/docs) rebuilding → sofra.savvylabs.dev updates in ~1-2 min.** Branch sdk57-upgrade kept.
- **✅✅ RESOLVED — Sameer upgraded to EAS Starter ($19/mo, high-priority queue, cancellable). The QUEUED submission `7fa31931` got retroactively bumped + COMPLETED in seconds → Status=Success, binary UPLOADED to App Store Connect 12:50 PM. Build 6 (1.0.1) now PROCESSING at Apple (~5-10 min → appears in TestFlight).** No re-run needed. EAS pricing ref: Free=low-priority queue (caused the 2h wait); Starter $19 = high priority + $45 build credit (also helps upcoming Android prod build); Production $199 overkill. Cancel anytime.
- **2-HOUR QUEUE DIAGNOSED (not a bug):** original `7fa31931` STILL Queued after 2h. Verified status.expo.dev = ALL OPERATIONAL, no incident. Root cause = EAS free-tier queue has NO SLA + deprioritized behind paid; some days minutes, some days hours. Submission is valid, just not picked up. **Re-running does NOT help (same queue) — DON'T.** Paths: (1) EAS PRIORITY submit (paid) = only reliable queue-skip — recommend if shipping today; will hit same wall on Android prod build; (2) leave overnight = clears on its own, no urgency (app still in Apple review, not public); (3) Apple Transporter (free) = bypasses EAS but needs a Mac. Awaiting Sameer's pick.
- **✅ BRANDED iOS SCREENSHOTS generated (6.7" 1290×2796 + 6.5" 1242×2688) for home/plan/insights** → `assets/brand/marketing/ios/sofra_ios{67,65}_brand_{home,plan,insights}.png`. Include Sofra lockup + Fraunces headline + sage rule + subtitle + phone w/ GENERIC data (Rivera Family). Generator = `scratchpad/gen-ios-shots.js` (system Chrome headless @2x + sharp downscale; embeds Fraunces/Inter TTFs + lockup; run with `NODE_PATH=<proj>/node_modules`). Sameer approved design.
- **⚠️ OPEN — iPad branded shots (2048×2732):** wider canvas needs different layout (not centered phone); plain iPad shots exist from before. Awaiting: brand iPad too, or use plain?
- **⚠️ OPEN — GO PUBLIC:** to release 1.0.1 to everyone still need (Chrome/ASC): pull in-review 1.0.0 record→set 1.0.1, upload new screenshots, add What's New (photo fix/name edit/share links), attach build 6, submit for review.
- **~~OPEN DECISION 1 — launch version #~~ RESOLVED (1.0.1):** app is NOT public yet (1.0.0 still UNDER REVIEW, never released). Build 6 is tagged 1.0.1. Options: ship debut as **1.0.1** (no rebuild, valid — recommended) OR revert app.json→1.0.0 + rebuild (~20m) for a "1.0.0=launch" story. Awaiting Sameer. What's New draft: photo-upload fix / editable name / share links.
- **⚠️ OPEN DECISION 2 — iOS branded screenshots:** Android has RICH branded store shots (`sofra_shot_{home,plan,insights}.png`, HTML src `shot_*.html`, Sofra frame+copy+link); iOS `sofra_ios_67_*` are plainer device frames. Plan = re-render `shot_*.html` at iOS canvas (NOT resize — aspect 9:16→9:19.5 differs) → export 6.7" 1290×2796 + 6.5" 1242×2688 + iPad 2048×2732. Awaiting Sameer: which screens (3 vs all 5) + full copy/link overlays y/n.
- **✅ ON-DEVICE VERIFIED (Sameer, Pixel 10 Pro): photo upload WORKS + name edit WORKS.** Photo fix = XHR→blob→uploadBytes (below). FONT: Sameer+wife prefer CURRENT (Fraunces/A) — **font change DEFERRED to later MVP**, no code change; mockup archived at artifact 36dc36a7. NEXT = commit `sdk57-upgrade` + EAS iOS build (ship photo/share/name fixes to App Store) + EAS Android production AAB.
- **PHOTO FIX v1 (base64 uploadString) FAILED on-device** — Firebase's uploadString('base64') also reconstructs a Blob from ArrayBuffer, which RN doesn't support → still threw. **FIX v2 (canonical RN method): XHR `responseType:'blob'` → `uploadBytes(ref, blob, {contentType})`** (`storage.ts` `uriToBlob`); AvatarPicker passes `{uri,type}` (dropped base64); ProfileScreen now SURFACES the real error (code+message in alert + console.error) so a 3rd failure is diagnosable via `adb logcat`. **NOT yet on-device verified.**
- **4 UX ITEMS DONE in code (batched w/ photo fix):** (2) FamilyScreen invite share appends `https://sofra.savvylabs.dev`; (3) ShareStatModal card adds "Get Sofra free · sofra.savvylabs.dev" CTA under tagline; (4) ProfileScreen name now tappable (pencil) → Paper Dialog edits name → `updateUserProfile(id,{name})`+auth store (fixes Apple email-prefix name; Portal host = PaperProvider App.tsx:92). tsc=0, 50/50. (1) FONT = still TODO (mock variants). Synced src→C:\S, incremental rebuild = **3m26s (JS-only, native cached) → APK rebuilt + installed in-place (`adb install -r`, same debug key = no uninstall, data kept).** ON-DEVICE TEST of photo fix + 4 items PENDING.
- **FONT compare (item 1) DELIVERED as Artifact:** https://claude.ai/code/artifact/36dc36a7-0113-460c-a5a6-9427610f02a1 — real Home screen in 3 type treatments (A current all-Fraunces / B toned-down: serif only on brand+stat number / C formal: all-Inter, no serif). Built with actual Fraunces+Inter TTFs base64-embedded (from `node_modules/@expo-google-fonts`, only 2 families installed). Recommendation = **B**. Awaiting Sameer+wife's pick → then wire font choice into `src/config/theme.ts` `Fonts`. (Browser-pane verify blocked — new-origin approval needed while user away; validated HTML structurally instead.)
- **(original 4 UX items context):** (1) FONT: wife (PO) says Fraunces looks fancy/informal — plan mock 2-3 variants as screenshots (current / lighter Fraunces / neutral serif), decide from visuals not abstract. (2) FAMILY-CODE SHARE has NO link (`FamilyScreen.tsx:40` msg = code only) → append smart link `sofra.savvylabs.dev` (one link, both stores). (3) SHARE CARD has brand+tagline but NO download CTA → add footer `sofra.savvylabs.dev`/"Get Sofra free". (4) APPLE name shows email-prefix "sameerrajwade" not "Sameer Rajwade" — ROOT CAUSE: Apple returns name ONLY on 1st-ever auth; Sameer re-authed → null → `useAuthStore.ts:75/158` falls back to `email.split('@')[0]`. FIX = editable name on Profile (`updateUserProfile(id,{name})` exists) — NOT overkill, it's the only reliable recovery. #2/#3/#4 are JS-only → fast incremental rebuild (~2-3m, no C++). Then commit → EAS iOS/Android.
- **Android EAS APK (`4da33c3f`, ~94 MB) INSTALLED on Pixel 10 Pro via wireless adb.** adb at `C:\Users\samee\AppData\Local\Android\Sdk\platform-tools\adb.exe` (NOT on PATH). Wireless flow: `adb pair 192.168.86.121:40225 <code>` (needed kill-server first — protocol-fault on 1st try) → `adb connect 192.168.86.121:35499`. Install: signature mismatch vs old debug-signed app → **uninstalled `com.thaliplan.app` then installed fresh** (data safe in Firebase, user re-signs in).
- **EAS SHA-1 ADDED TO FIREBASE ✅** so Google Sign-in works. Extracted from APK via `apksigner verify --print-certs` (keytool -jarfile failed = APK v2/v3 sig only). EAS SHA-1 = `EE:40:8F:10:C5:D2:FE:26:69:E1:98:21:8B:7C:21:DF:62:81:FF:A3` (SHA-256 `0B:37:...:8C:85`). Added via Chrome to Firebase console → thaliplan → Android app fingerprints (now 3 SHA-1s). **AWAITING Sameer's device test of Google Sign-in.**

## Prior Session (2026-08-03 latest — Expo SDK 52→57 upgrade, branch `sdk57-upgrade`)
- **WHY:** Apple rejected the iOS build — must use iOS 26 SDK / Xcode 26. Expo SDK 52 (RN 0.76) can't compile on Xcode 26 (`fmt` consteval error). Sameer chose to UPGRADE (not patch). Branch `sdk57-upgrade` off `ios-setup`; clean checkpoint commit `68c817b`; Android backup intact at `C:\Users\samee\DiningPlanner-Android`.
- **DONE:** `npm i expo@^57` + `expo install --fix` → expo 57.0.9, react 19.2.3, react-native 0.86.2, typescript 6, @types/react 19, all expo modules 57.x. Fixes: app.json splash→`expo-splash-screen` plugin (+font/sharing/status-bar plugins auto-added); tsconfig `types:[jest,node]` (TS6); code: RN0.86 `ColorSchemeName 'unspecified'` handling in useThemeStore + App.tsx Appearance listener; Celebration `absoluteFillObject`→explicit edges. **tsc=0 (was 167), 50/50 tests, expo-doctor 20/20.** Committed `7eaec79`.
- **iOS COMPILE FIX CONFIRMED:** prod build `ff68c56f` (SDK57, build 4) FINISHED on Xcode 26 → fmt wall cleared. Submitted (2-min upload; ASC API key stored so non-interactive submit works). **Apple PASSED the SDK check but rejected build 4 with ITMS-90683: missing Info.plist purpose strings** (AvatarPicker uses launchImageLibrary+launchCamera). FIX: app.json ios.infoPlist += `NSPhotoLibraryUsageDescription` + `NSCameraUsageDescription` (commit `3a67fdc`, iOS-only). Rebuilding as build 5 = `a519e2a1` (in progress). **⚠️ Still NOT runtime-tested — New Arch; watch chart-kit, google-signin@13, vector-icons.**
- **ANDROID LOCAL BUILD (SDK57) UNVIABLE on Windows — ROOT CAUSE = upgrade's New Architecture:** New-Arch C++ codegen creates ~298-char paths (e.g. `...react_codegen_rngesturehandler_codegen.dir\C_\Users\samee\DiningPlanner\node_modules\...RNGestureHandlerDetectorShadowNode.cpp.o`) > Windows MAX_PATH 260. SDK52 (old arch) didn't → why local worked before. Tried 3 ways: (1) from C: → `ninja: longer than 260`; (2) enabled `LongPathsEnabled=1` (Sameer, admin) → NDK ninja still ignores it, same error; (3) `subst X:` → fixed 260 BUT broke Expo autolinking (`cmd` + Node can't find package.json on a virtual drive). subst removed. Only reliable LOCAL fix = physically move repo to short real path (e.g. C:\S). Cloud/EAS unaffected (Linux). Keystore prep done + reverted: prebuild kept debug.keystore 5E:8F…F6:25; `android/local.properties`→SDK path; JDK17 OK.
- **DECISION (Sameer): build Android test APK on EAS** → `4da33c3f` (preview/apk, New-Arch, SDK57) building. **SHA-1 clarification for Sameer:** adding EAS keystore SHA-1 to Firebase is ADDITIVE (old debug SHA-1 5E:8F… stays) → existing debug-signed testers KEEP working, NO reinstall from the SHA-1. BUT the EAS APK is a DIFFERENT signing key → can't install over the old debug-signed app (signatures mismatch) → a tester switching to it must uninstall+reinstall (data safe in Firebase). **OPEN Q to Sameer:** proceed with EAS keystore + add its SHA-1, OR configure EAS to sign with the existing debug keystore (seamless update for current testers, no new SHA-1). Awaiting his choice; APK build polling.
- **⚠️ ANDROID LOCK ENDED:** shared codebase moved to SDK 57 → Android must be re-tested + rebuilt (new Play AAB) before its launch. The already-built Android AAB `b81c9f41` is on OLD SDK 52.
- **iOS build 4 rejected ITMS-90683** (missing Info.plist purpose strings — AvatarPicker uses image-picker); FIXED app.json ios.infoPlist += NSPhotoLibraryUsageDescription + NSCameraUsageDescription (commit `3a67fdc`). Build 5 `a519e2a1` (SDK57) UPLOADED + PASSED → TestFlight "Ready to Submit" ✅. Submit works non-interactive now (ASC API key stored from Sameer's first submit).
- **iOS LISTING PROGRESS (Claude via Chrome):** ✅ app record + name "Sofra: Family Meal Planner" + subtitle "Plan meals, remember it all" + categories F&D/Lifestyle + desc/keywords/promo/URLs/copyright + Privacy Policy URL. ✅ **App Privacy label PUBLISHED** (5 data types: Name/Email/Photos/OtherUserContent/UserID — all App Functionality, Linked, NO tracking). **STILL TODO: screenshots** (Claude CAN'T upload — Chrome file_upload only takes user-attached files; resized to 1242×2688 + iPad 2048×2732 and SENT to Sameer to drag in), **Age Rating** (→4+ all None), **Content Rights** (no 3rd-party), **Pricing** (Free), **attach build 5 + export compliance**, then Add for Review. Demo account = Sameer.
- **ANDROID EAS APK BUILT** (local Windows blocked by MAX_PATH): build `4da33c3f` (preview/apk, SDK57) FINISHED → install URL `https://expo.dev/artifacts/eas/hhe00vI1i3uMa77xgwNDlDX0QVU20NyjnuuYvbDH-Lo.apk`. Signed w/ EAS keystore (diff SHA-1 from debug) → **Google Sign-in needs EAS SHA-1 added to Firebase (TODO, Claude via Chrome)**; email/pw + Apple work now. adb currently shows NO device (wireless dropped) → Sameer installs via phone browser download. App name decision: keep "Family Meal Planner" (SEO) over "Family Meal Memory".

## Prior Session (2026-08-03 — Sign in with Apple + iOS publish plan, branch `ios-setup`)
- **REALITY CHECK for Sameer:** the only iOS builds on EAS are SIMULATOR builds (`preview` profile, `ios.simulator:true`) → `.tar.gz`, Appetize-only, NOT App-Store-uploadable. Android production `.aab` (`b81c9f41`) IS store-ready. To ship iOS we must make a NEW signed device `.ipa` via `eas build -p ios --profile production` — EAS builds+signs in the cloud, NO Mac needed. Sameer confirmed Apple Developer acct is ACTIVE.
- **Sign in with Apple IMPLEMENTED** (App Store guideline 4.8 requires it when Google login is offered — top rejection cause). iOS-only, Android-inert. Added deps `expo-apple-authentication@~7.1.3` + `expo-crypto@~14.0.2` (flagged: new deps, but iOS-native/no Android behavior change; button is `Platform.OS==='ios'` + `isAvailableAsync` gated). `app.json`: `ios.usesAppleSignIn:true` + plugin (android block untouched). `auth.ts` `signInWithApple()` uses SHA-256 nonce (Crypto) → Firebase `OAuthProvider('apple.com')`; captures Apple's one-time name → displayName. Store action + official `AppleAuthenticationButton` in AuthScreen (dark-aware). tsc=0, 50/50 tests. NOT built yet (needs fresh EAS build for the entitlement).
- **DONE this session via Chrome automation:** (1) ✅ Firebase Apple provider ENABLED (Auth→Sign-in method; Services ID left blank — native iOS needs only the toggle). (2) ✅ Apple **Team ID = `3HQ9T7PKN4`** captured; membership ACTIVE (Individual, renews 2027-08-04, auto-renew on). (3) ✅ `eas.json` → added `submit.production.ios.appleTeamId`. (4) ✅ Apple assets generated by subagent in `assets/brand/marketing/ios/` — `sofra_icon_1024.png` (1024², no-alpha, App-Store-safe), 5× iPhone 6.7" `sofra_ios_67_*.png` (1290×2796), 3× iPad 12.9" `sofra_ios_ipad_*.png` (2048×2732). Visually verified — branded frames, generic Rivera-Family data.
- **DSA trader status RESOLVED ✅ = NON-TRADER** (Sameer is on H1B — declaring "trader" = asserting a business/professional capacity, which we deliberately avoided; also a free app w/ no revenue is genuinely non-commercial = non-trader is accurate). Done via Chrome; ASC Compliance now shows DSA Active. TRADEOFF (accepted): app NOT distributed in EU. **H1B WATCH-ITEM: do NOT enable the deferred paywall / any monetization (IAP, ads, paid) without an immigration attorney — free app w/ no income is low-risk; any self-employment income is an H1B unauthorized-work question.** Free Apps Agreement Active; Paid Apps Agreement unsigned (not needed).
- **DECISIONS (2026-08-03):** keep iPad support (supportsTablet stays true, iPad screenshots kept); territories = all EXCEPT EU (per non-trader). App name = "Sofra: Family Meal Planner" (fallback if "Sofra" taken). Listing pack written → `docs/ios-appstore-listing.md` (paste-ready copy + App Privacy label + age-rating + reviewer notes). Committed `6c2fd6d` (Apple sign-in) + `4e30328` (listing pack).
- **SUBMISSION IN PROGRESS (2026-08-03 later):** Prod build `20b48e5f` (Xcode16/iOS18.2 SDK) built + UPLOADED by Sameer via `eas submit` (interactive; created+stored an ASC API key on EAS → future submits can be non-interactive). **Apple REJECTED it: must build with iOS 26 SDK / Xcode 26.** FIX (Android-safe, iOS-only): `eas.json` build.production.ios.image=`macos-tahoe-26.5-xcode-26.6`; triggered rebuild `4e6a7226` non-interactively (reused stored dist cert/profile, no Apple login). **RISK: Expo SDK 52 compiling on Xcode 26 may fail → would force an Expo SDK upgrade (big, Android-lock implications).** Polling the rebuild.
- **ASC app record CREATED:** "Sofra: Family Meal Planner", Apple ID (ascAppId) `6797710095`, bundle com.thaliplan.app, SKU sofra-ios-001, English US. `eas.json` submit.production.ios has appleTeamId+ascAppId. **Metadata DONE via Chrome** (from `docs/ios-appstore-listing.md`): promo text, description, keywords, support/marketing URL (sofra.savvylabs.dev), copyright, subtitle "Plan meals, remember it all", category Food & Drink/Lifestyle. **Metadata STILL TODO:** screenshots (must resize 1290×2796→**1242×2688** for the 6.5" slot; iPad 2048×2732 ok), App Privacy nutrition label, Age Rating questionnaire (all None→4+), Content Rights (no 3rd-party), Pricing (Free), then select build + export-compliance + Add for Review.
- **Appetize test build READY** (fresh, incl. Sign in with Apple): sim build `62dc2105` → `https://expo.dev/artifacts/eas/irjkXHk1owOVtNvrcrpfeJgLbnHaeaPn9fNBJihsMXE.tar.gz` (SDK rule doesn't affect simulator/Appetize).
- **REMAINING (Sameer):** create a demo test account in-app → for Apple reviewers (listing-pack §6); test the Appetize build. Everything else Claude drives.

## Prior Session (2026-08-02 — pre-launch auth fixes, branch `ios-setup`)
- **DECISION: block launch to fix auth first** (Path A = Firebase link-based, no backend/no billing). Code-only + Firebase-console; ZERO Android-config diff (no app.json/eas.json/package.json touched — lock holds). tsc=0, **50/50 tests pass**. ALL changes are shared RN/Firebase-JS code → apply to BOTH iOS + Android (no platform branch).
- **#SECURITY Firebase errors no longer leaked:** new `src/utils/authErrors.ts` maps codes → generic copy; wrong-password / unknown-account / invalid-credential ALL say "Incorrect email or password." (no DB/SDK leak, no account-enumeration). Wired into every `useAuthStore` catch. Locked by `authErrors.test.ts` (4 tests).
- **Sender name:** Sameer owns savvylabs.dev NOT sofra.com; only wants inbox to read "Sofra" (not app.thaliplan). Path A sender-name="Sofra" console field fully covers it — no domain/backend. From-address stays firebase domain (he doesn't care).
- **#4 reset email OPEN:** Sameer confirms NOTHING arrives (inbox+spam). Most likely = tested a Google-only account (no password → Firebase silently sends nothing). NEXT: check Auth→Users Providers col for that email; if only-Google, expected. Else create fresh email/pw account + retest; if still nothing → project email-config dig.
- **BUILD STRATEGY (Sameer's call): Android = LOCAL, iOS = EAS.** Committed `e0bd110` on `ios-setup`. Cancelled an unneeded EAS Android build; free tier = 1 concurrent. Android release APK built LOCALLY via `npx expo run:android --variant release` (device connected over wireless adb; release signs w/ debug keystore SHA1 5E:8F:16:...:F6:25 = the stable one already in Firebase → Google Sign-in works). Build = 3m11s (NORMAL; the "slow"/hang was expo run:android idling on Metro:8081 after BUILD SUCCESSFUL — next time use `cd android && ./gradlew assembleRelease` to build+exit). APK `android/app/build/outputs/apk/release/app-release.apk` (67MB); INSTALLED on Pixel (com.thaliplan.app v1.0.0, 14:48). AWAITING Sameer device test of the 4 fixes. iOS sim build on EAS (`--profile preview`) for Appetize DONE = build `620270a7`, artifact https://expo.dev/artifacts/eas/Mhwupxa54G8EnSKzpGJuecYiyDruK3QJyg72ruuG4aw.tar.gz — REPLACES stale `e7a0f565` (predates these fixes). Local toolchain confirmed: SDK, adb, gradle, JDK17 all present.
- **Google Sign-in caveat on local/debug-signed APK:** debug-keystore SHA-1 must be in Firebase or Google sign-in silently fails; email/password + new features test fine regardless.
- **VERIFICATION EMAIL CONFIRMED WORKING** (Sameer received it). **Level A branding DONE by Claude via browser automation in Sameer's Chrome** (Firebase console has no CLI/API for these): Public-facing name → "Sofra" (Project Settings); Email-verification + Password-reset templates Sender name → "Sofra" (both saved OK). %APP_NAME% now renders "Sofra" in all subjects/bodies. NO rebuild (server-side, both platforms). Project is on BLAZE (Level B feasible w/o plan change). Level B (custom domain noreply@savvylabs.dev + full HTML via Cloud Fn+Resend) DEFERRED. NOT done: visible do-not-reply footer in body (reply-to already =noreply; offered, awaiting Sameer OK to edit body HTML).
- **Forgot-pw copy fixed (code):** removed the "just use Continue with Google" nudge — a user may register abc@gmail.com w/ email+password on purpose; don't steer to Google. Now neutral "if an account exists, a link is on its way." tsc=0, 50/50.
- **#1 Email verification (NEW):** `signUpWithEmail` now sends a verification link; auth store tracks `emailVerified`; new gate screen `VerifyEmailScreen` sits between auth and app ("I've verified"/"Resend"). Google accounts arrive verified → skip gate. Wired in `App.tsx` + `AppNavigator`.
- **#2 Forgot password:** code path was already correct (`sendPasswordResetEmail`) — real cause of "no email" = tested a Google-only account (no password) or spam. Hardened the success copy to say so.
- **#3 Branding:** Path A can only set sender NAME=Sofra + subject/body + no-reply footer (From-address stays Firebase domain; custom sender needs Path B). Exact paste-in copy → `docs/firebase-email-templates.md` (SAMEER console task).
- **#4 Meal-type bug FIXED:** tapping Dinner on Home now pre-selects Dinner in AddMeal (passed `mealType` nav param) instead of defaulting to Lunch and falsely reporting a conflict.
- **NEEDS DEVICE TEST before Android build:** verify-email gate + resend + reset copy + meal-type slot. Not yet run on device (RN — not browser-previewable).

## Prior Session (2026-08-02 — Appetize walkthrough)
- Reviewed `ios-appetize-runbook.md`; iOS sim build #2 (`e7a0f565`) is the artifact to test. FLAGGED: EAS artifact URLs expire ~30d — if the .tar.gz 404s, `npx eas-cli build:view e7a0f565` or fresh `eas build -p ios --profile preview`.

## Prior Session (2026-08-02 — iOS wiring on branch `ios-setup`)
- **Android LOCK established:** backup `C:\Users\samee\DiningPlanner-Android` (repo minus node_modules); work on branch `ios-setup`; hard constraint added (see Constraints). Verified Android config byte-for-byte untouched in every edit.
- **Registered iOS app in Firebase** (`thaliplan` project, bundle `com.thaliplan.app`, nickname "ThaliPlan iOS") → additive, Android app untouched. Got iOS OAuth client.
- **Wired iOS Google Sign-in (JS-SDK-correct, no plist in repo):** `app.json` plugins → google-signin `iosUrlScheme` (proven iOS-Info.plist-only, no Android branch); `auth.ts` → added `iosClientId` (Android ignores); `eas.json` preview → `ios.simulator:true` (Appetize path, no Apple acct needed). iOS OAuth values: client `349329204088-nmihufdrn14vsqikc5tpotqf37otvaui`.
- **EAS linked:** project `sofrasavvylabsdev` (owner `savvylabs`, id f4ddb333-3bef-4b83-a470-a98b046a86a8); app.json slug → `sofrasavvylabsdev` (approved; Expo-internal only, Android package id unchanged). Added `ios.infoPlist.ITSAppUsesNonExemptEncryption:false`.
- **DISCOVERED + FIXED pre-existing EAS-build blocker (affects Android too):** `@expo/vector-icons@15.1.1` peer-needs `expo-font>=14` but SDK52 pins `expo-font@13.0.4` → `npm install` ERESOLVE fails on EAS (and locally). Fix = `.npmrc` `legacy-peer-deps=true` (ZERO version change; dry-run tree identical). **Android production EAS build will need this same .npmrc.**
- **Health:** tsc=0, 46/46 tests pass. Committed on `ios-setup` as `f514818`.
- **iOS builds:** #1 `0c7036a9` ERRORED (npm conflict, pre-fix). #2 `e7a0f565` **FINISHED** ✅ — iOS SIMULATOR build (isForIosSimulator=true, SDK52, v1.0.0 build1). Artifact (.tar.gz): https://expo.dev/artifacts/eas/ug9sdxLZ8LsngkQ4dbO9nf0hyXXPfx2Tisu9COOnlks.tar.gz — ready for Appetize.io.
- **Prior session (2026-08-01):**
- **Website icon refresh:** feature-card emoji (🍲✨📊…) → 7 branded SVG icons. Live on `sofra.savvylabs.dev`.
- **iOS deployment research & guides:** Created 4 comprehensive guides (deploy checklist, Firebase setup, App Store submission, quick-ref). Key findings: iOS differs from Android (URL schemes instead of SHA-1, stricter privacy label, silent failures documented).
- **iOS testing strategy:** Appetize.io cloud simulator (free tier: 30 min/month, 3 min/session) for pre-testing before giving builds to friends. No Mac/iPhone/Apple account needed for initial testing.
- **Model decision:** Opus 4.8 Medium effort chosen for iOS work (better reasoning on multi-step config, catches silent failures, worth cost vs. wasted Appetize sessions).
- **Launch readiness:** Android path ready. iOS path fully documented + testing strategy confirmed.

## Next Up (Sameer's launch path)
**Android PRODUCTION AAB — BUILT ✅** (commit `280446f`; build `b81c9f41-f1db-465f-82ec-58f5b536f330`; versionCode auto→2; EAS-managed upload key). AAB: https://expo.dev/artifacts/eas/sdbfOC8hQUrQTv-HJfDE9FZDVrLVhggoTchkDbhJM7k.aab
1. Upload AAB → Play Console (personal acct ⇒ 12-tester/14-day closed test before prod; internal testing instant).
2. **CRITICAL SHA-1 (Google Sign-in breaks without it):** Play build is signed w/ EAS upload key + Play App Signing key — BOTH SHA-1s differ from the debug keystore (5E:8F...) that works locally. After 1st upload: Play Console→App integrity (App-signing + Upload SHA-1) + `npx eas-cli credentials` (EAS upload SHA-1) → add ALL to Firebase→Android app fingerprints. Claude can do this via Chrome automation like the email templates.
3. Data Safety form, content rating, target audience, privacy URL, store listing (assets in `assets/brand/marketing/`).

**iOS — NOW LIVE PATH (Apple Developer acct ACTIVE as of 2026-08-03):** Sign in with Apple done in code. Remaining to ship: enable Apple provider in Firebase → create app in App Store Connect → add `submit.production.ios` to eas.json (needs Apple Team ID + ASC App ID) → `eas build -p ios --profile production` (cloud signing, no Mac) → `eas submit -p ios` → 6.7" screenshots + App Privacy label + metadata → submit for review. NOTE: existing `620270a7` is a SIMULATOR build only — cannot go to the App Store.

**Still open:** device-test the 4 fixes on the installed APK; Providers-col check for reset email (#4); do-not-reply footer decision.

**iOS (simulator build READY — Appetize next):**
1. ✅ Build #2 FINISHED — artifact URL above. NEXT: create Appetize.io account, upload the .tar.gz (or paste the artifact URL). Guard the free tier (30min/mo, 3min/session) — run the checklist deliberately.
2. Run the 10-point smoke test (watch Google Sign-in especially — simulator OAuth is the main risk).
3. If a build fails: `eas build:view <id> --json` → logFiles[0] → `curl -sk --compressed <url>` (it's NDJSON; grep `npm error`/phase).
4. If all pass → enroll Apple Developer ($99/yr) → signed build → TestFlight → App Store (`ios-deploy-checklist.md`).
- NOTE: `@react-native-google-signin` needs a dev/custom build (not Expo Go). EAS login is done (sameerrajwade); eas-cli NOT on Sameer's real-terminal PATH (use `npx eas-cli` or `npm i -g eas-cli`).
- Guides ready: `ios-deploy-checklist.md`, `ios-firebase-setup.md`, `ios-appstore-submission.md`, `ios-quick-ref.md`.

**Post-launch:** Update `#get` button href (line 120) to live Play URL.

## Constraints
- **ANDROID LOCK (iOS work in progress):** Android is FROZEN. iOS changes are additive to iOS-only sections ONLY. HANDS OFF: `app.json` root keys + `"android"` block (L23-29); `eas.json` existing `android`/`submit` keys; `package.json` dependency versions (no bumps for iOS). No new/upgraded deps to support iOS without flagging Android impact first. Every iOS commit must show ZERO diff to Android config. iOS work lives on branch `ios-setup`. Backup restore point: `C:\Users\samee\DiningPlanner-Android` (full repo minus node_modules).
- **Firebase = JS SDK (not native RN Firebase):** `src/config/firebase.ts` uses a platform-agnostic `firebaseConfig` JS object → iOS reuses the SAME config for Firestore/Auth/Storage. NO `GoogleService-Info.plist` needed for those. The iOS guides' plist/native steps mostly DON'T apply. Only iOS-specific secret = Google Sign-in iOS OAuth client (iosClientId + reversed-client-id URL scheme) in `app.json`.
- Repo: github.com/sameerrajwade/Sofra. Package id `com.thaliplan.app` must NOT change (Firebase-linked).
- **Never handle signing/keystore passwords — Sameer signs.** Signing path = **EAS managed** (cloud build, EAS-stored upload key, Play App Signing).
- Motion = built-in RN Animated only (no Reanimated). Paywall/monetization DEFERRED to post-launch.
- Develop → test → show results → only then build/deliver. Verify on device via `adb exec-out screencap -p`.
- Firebase project = `thaliplan`. Prod rule deploys: `firebase deploy --only firestore:rules` / `--only storage`.

## Key file & asset locations
- **Video prompts:** `docs/video-prompts.md` — 10× 20-second brand-commercial briefs (real-people shoots): cast, setting, timecoded beats, which app screen to feature, on-screen line, + production notes. End-card = logo lockup + Google Play badge.
- **Branding / marketing assets:** `assets/brand/marketing/` (has its own `README.md`). Master logo source: `assets/brand/icon_full.svg` (terracotta tile) + `assets/brand/mark_cream.svg` (bowl only). Palette: terracotta `#C0532E`, sage `#5E8B6A`, warm paper `#FBF7F2`. Render pipeline = Chrome headless @2× → sharp downscale; icon via sharp-from-SVG. Editable `.html` source sits next to every `.png`. Exports:
  - `sofra_icon_512.png` 512×512 — **Play Store app icon** (full-bleed)
  - `sofra_feature.png` 1024×500 — **Play feature graphic** (subtle URL)
  - `sofra_shot_home/plan/insights.png` 1080×1920 — **Play phone screenshots** (branded frames; embed the GENERIC mockups — store-ready, no test data)
  - `sofra_reel.png` 1080×1920 — YouTube Shorts / Instagram Reels (+ Play pill)
  - `sofra_square.png` 1080×1080 — Instagram feed
  - `sofra_og.png` 1200×630 — website Open Graph / link-preview image
  - `sofra_lockup.png` 900×280 — transparent horizontal logo lockup (site header/press)
- **Generic app-screen mockups:** `assets/brand/marketing/screens/*.html` → rendered PNGs. All 9 screens (login, household, home, addmeal, plan, celebration, insights, share, calendar) rebuilt with GENERIC global data — "The Rivera Family"; dishes Veggie Stir-Fry / Grilled Salmon / Spaghetti Bolognese / Beef Tacos / Roast Chicken; restaurants Nonna's Kitchen / Green Bowl; cuisines Italian/American/Mexican/Asian; USD. **Copied into `docs/assets/screens/*.png` — the live site gallery (index.html) is now fully generic.**
- **Store listing copy:** `docs/store-listing.md` (title, short/full description, category, URLs).
- **Android deploy checklist:** `docs/android-deploy-checklist.md` (signing/build, Firebase, Play compliance, listing, rollout, launch sequence).
- **MVP2 backlog:** `docs/MVP2.md`.
- **NotebookLM ad brief PDF:** `C:\Users\samee\Downloads\Sofra_Ad_Brief_for_NotebookLM.pdf` (12pp). NOTE: embeds PRE-rename screenshots ("Dine Outs") — regenerate if visuals must match current UI.
- **Website:** GitHub Pages from `docs/`, custom domain `sofra.savvylabs.dev` (via `docs/CNAME`). Pages: index.html, guide.html, features.html, security.html, architecture.html, privacy.html, terms.html.

## LAUNCH STATUS

### DONE (code / infra / assets — all verified)
- **Launch-blocker bug fixed:** Dine Out/Takeout with an unrated dish wrote `items:[{name, rating:undefined}]` → Firestore "Unsupported field value: undefined". Fixed at source + deep recursive `stripUndefined` (`src/utils/sanitize.ts`) now wraps EVERY Firestore write. Device-verified: dine-out saves cleanly.
- **5-agent audit → 20+ fixes** (meal / planner / insights / restaurants / auth). Incl. **P0 account-deletion data-loss** (now deletes Auth user FIRST), planner kids-tiffin leak + data-loss, insights false "0% home" / range headline / empty-window / TZ bucketing, restaurant future-visit counts + TZ dates, avatar 1MB cap (512px), atomic arrayUnion/arrayRemove membership, signup household-name sync.
- **Unique Dishes = home-only** (was double-counting outside meals); Dish Library + Insights consistent. **Relative dates** Today/Yesterday/N-days-ago (`src/utils/relativeDate.ts`).
- **Plan↔Calendar synced:** planner suggests HOME dishes only; saved outside meals show restaurant name + dish; day header font + TODAY highlight box match Calendar.
- **Firestore + Storage rules deployed to prod** (leave-household branch added; both match repo).
- **eas.json** wired: `submit.production.android` (internal track, draft), production `autoIncrement` + `appVersionSource: remote`. Hermes on. AAB config in place.
- **Brand kit + store copy + privacy/terms** ready. In-app account deletion live.

### REMAINING FOR LAUNCH — Sameer console tasks (NOT code)
1. `npm i -g eas-cli` → `eas login` → `eas build -p android --profile production` → accept "generate keystore" → download the **.aab**.
2. Play Console: create app "Sofra" → **Internal testing** track → upload the .aab (or `eas submit -p android` after placing `play-service-account.json` at repo root — it's gitignored).
3. **CRITICAL:** after first upload, copy the **Play App Signing key SHA-1** (Play Console → App integrity) AND the upload-key SHA-1 → add BOTH to Firebase console (project settings → Android app). Then **verify Google Sign-in on the internal-testing build** — it silently fails if SHA-1 is missing.
4. Play compliance: **Data Safety form** (collects: email, name, user content/meals, avatar photo; encrypted in transit; user-deletable), **content rating** (IARC → Food & Drink, likely Everyone), **target audience** (not child-directed), **privacy URL** https://sofra.savvylabs.dev/privacy.html, category Food & Drink.
5. Store listing: paste copy from `docs/store-listing.md`; upload `sofra_icon_512.png` (512×512), `sofra_feature.png` (1024×500), and ≥2 of `sofra_shot_*.png` (1080×1920).
6. Rollout gate: **personal Play account** ⇒ 12-tester/14-day closed testing before production; **internal testing is instant** (up to 100 testers) — start there.
7. Pre-submit smoke test on the STORE artifact: sign-in → household → log meal (incl. dine-out) → generate+accept plan → insights → avatar upload → delete account.

## WEBSITE — REVAMPED (done this session)
`sofra.savvylabs.dev` (GitHub Pages from `docs/`). Consumer launch pass DONE:
- **Screens fully generic + live** — all 9 `docs/assets/screens/*.png` (Rivera Family, global dishes) power BOTH the index gallery AND `guide.html`. The old numbered `docs/screens/*.png` are ORPHANED (referenced by nothing) — safe to delete later.
- **Nav de-dev'd** on index + guide: `Features · Guide · FAQ · Get the app` (Architecture/Security demoted to footer).
- **Open Graph / Twitter meta** added to index + guide (image = `docs/assets/og.png` = the 1200×630 brand OG). `docs/assets/lockup.png` also copied in for future header use.
- **FAQ section** added (recipe app? shared account? cost? privacy? platforms? planning?).
- **"Get Sofra"** section → launch/**"Coming soon on Google Play"** state (removed the dead `CHANGE-ME` GitHub link). ⚠️ ONE TODO left in `index.html` (HTML comment at #get): after Play launch, set the button href to the live listing URL + relabel "Get it on Google Play".
- theme-color + canonical added. Static, GitHub Pages, dark-aware.

## Verified on device (Pixel 10 Pro, this session)
Dine-out save works (no crash) · Home "Unique Dishes" = 1 (home-only) · Dish Library "Palak Paratha · Today · 1 dish" · Plan shows Saturday TODAY box + "Honest"→"Dosa" dine-out + home-only generated dishes + Calendar-matching fonts.

## Backlog / non-blocking / later
- **MVP2** (`docs/MVP2.md`): relative-date larger units (weeks/months), optional combined home+outside dish view, notification deep-linking.
- **Non-blocking watch-items:** orphaned Storage avatar bytes on account delete (rule-safe); spending-trend chart with exactly 2 equal data points (low-confidence react-native-chart-kit).
- **Post-launch:** crash reporting (Sentry vs Crashlytics — needs DSN), Firebase App Check (Play Integrity), reads Tier 3 (denormalized stats only before large histories), icon/splash PNG regen if desired, i18n.
- Regenerate the NotebookLM PDF screenshots if it'll be shared (currently pre-rename).

## History (condensed — earlier sessions)
- **PRs #2–#5 all MERGED** to `main`: #2 (14 UX/device fixes), #3 (GitHub Pages site), #4 (Firestore reads cache-first single-source — ~2-3k reads/day → ~1 load/launch), #5 (Insights/Dish window fixes, "Outside Meals" card, tappable Most Cooked, windowed dish counts, date-rollover recompute, pure dishStats/insightsRange utils).
- Cache-first store: all meals/dishes loaded once per session, screens filter in memory, writes update memory locally, re-read only on pull-to-refresh/household change (no onSnapshot by design). `useMealStore.test.ts` locks it.
- Prior verified-correct behaviors (not bugs): dish counts include thali sides (`items`); windowed vs all-time counts; kids excluded from family metrics.
