# Sofra — Progress

> Thorough restart reference. If resuming a new session, read this first.

## Current State (facts)
- **App:** Sofra — family meal-planning app. React Native / Expo + Firebase (Firestore/Auth/Storage) + TypeScript. Zustand state, React Navigation, React Native Paper. Rebranded ThaliPlan → Sofra.
- **Design:** Terracotta & Sage theme (light/dark/auto), Fraunces (display) + Inter (body), RN Animated motion. All screens dark-aware.
- **MVP1 features:** multi-dish meals + per-dish ratings, kids-tiffin planning, weekly auto-planner, local notifications, Insights charts, restaurants + dish library, in-app account deletion.
- **Health:** `tsc=0`, **113/113 Jest tests pass** (17 suites). Latest RELEASE APK on Pixel 10 Pro (device-verified, no crashes). Phase-1 cold-start + Phase-2 grocery + Phase-5 Batch-A delights built; awaiting device test.
- **v1.2.0 STRATEGY (locked 2026-08-31):** Sameer's call — ship ALL remaining additive features as ONE v1.2.0 to iOS + Android together; **Android HOLDS for it** (don't launch vc4/1.1.0 early — Android gets one first impression, debut at full feature parity, not thin-then-iterate). Batches: A=Phase-5 delights (cook-again/decide/leftovers) → APK#1; B=Phase-3 recipe + widget → APK#2; C=Phase-4 dish-pack share → APK#3; then fold tech-debt (edge-to-edge/orientation/branded links), bump 1.2.0, staged rollout both stores. NOT building (friction traps): nutrition/macros, aisle grocery, full recipe DB/import, community feed, delivery. ⚠️ verify Google targetSdk requirement before the 1.2.0 native build; widget is the one risky native piece (may slip to 1.2.1).
- **Retention focus (NEW):** first real-user feedback in → discipline/habit is the churn risk. All 5 items now shipped in code (`tsc=0`, 56/56 tests) — NOT yet device-tested or released.
- **Website:** `sofra.savvylabs.dev` (GitHub Pages) — fully branded, dark-mode aware, 9-screen gallery. Live. **Get-the-app section now has a LIVE App Store download button** (Play still "coming soon").
- **Firebase:** project `thaliplan`. Firestore + Storage rules deployed. Android Firebase config verified (SHA-1 ready).
- **Deployment:** **✅ iOS 1.1.0 LIVE on the App Store** (`apps.apple.com/app/id6797710095`, App ID 6797710095); 1.2.0 update in progress (see Last Session). Android: prod access GRANTED → **✅ v1.2.0 (versionCode 5) SUBMITTED FOR PRODUCTION REVIEW 2026-09-03** (full rollout, 176 countries + rest of world; managed publishing OFF = auto-publishes on approval, ≤7 days). Listing complete (name/desc/icon/feature/8 generic screenshots). App was NEVER live before — this is the true Play Store debut.

## Last Session (2026-09-03 PART 27 — Android SUBMITTED to prod; iOS prepped but HELD on card bug)
- **Discovered the app was NEVER public**: stuck in Closed testing; earlier sessions built+staged a v1.2.0 full-rollout prod release but NEVER clicked "Submit for review" — that unclicked button was the whole reason it wasn't on the Store.
- **✅ ANDROID v1.2.0 SUBMITTED FOR PRODUCTION REVIEW** (full rollout, 176 countries + rest of world; managed publishing OFF → auto-publishes on Google approval, ≤7 days). Verified via browser Play Console: binary vc5 already uploaded, listing complete & Live (name/short+full desc/icon/feature/8 generic "Rivera Family" device-framed screenshots), Android dev verification OK, no blocking compliance → clicked Submit → quick checks passed → "Changes in review." Sameer said LET ANDROID PROCEED.
- Made Play-legal 2:1 screenshots in `branding/assets/play-screens/` (NOT used — existing device-framed ones were better).
- **⏸️ iOS v1.2.0 PREPPED BUT HELD (NOT submitted):** In App Store Connect created v1.2.0, attached build 8, set What's New (grocery/recipes/global evolving dish library/budget/sharing — NO "1400+ Indian" per Sameer), sharpened keywords (added grocery+recipe: `dinner,recipe,grocery,menu,weekly,mealprep,lunch,tiffin,thali,takeout,dineout,kids,cuisine,kitchen`), promo text, auto-release selected, demo sign-in carried. **Sameer STOPPED before submit: metric cards don't look good on his iPhone.** Nothing sent to Apple — sits in "Prepare for Submission."
- **iOS UI BUGS FOUND + FIXED (from Sameer's iPhone photos, commit ce1c525, tsc=0, 154/154):** (1) **Card value wrap** — MetricCard value (Fraunces xxxl) wrapped in the 3-col Restaurants stat row (`$1265`→`$126`/`5`); fixed with numberOfLines=1 + adjustsFontSizeToFit + flexShrink on value. (2) **Keyboard trap** — DishDetailSheet recipe/notes editor was hidden behind keyboard w/ no exit; wrapped sheet in keyboard-aware ScrollView (automaticallyAdjustKeyboardInsets), flattened inner ScrollViews→Views, modal maxHeight 90%. (3) Restaurants "Unique Places"→"Places" (was truncating). (4) iOS back button read "HomeMain" → gave HomeMain title 'Home'. These are iOS-specific (Android device-verified fine) → Android build UNCHANGED (Sameer said let it proceed).
- **iOS build 9 tested by Sameer on iPhone → 2 more issues found + FIXED (commit after ce1c525):** (1) value wrap fixed but **card TITLES** still truncated ("UNIQUE PLA…") → MetricCard title now adjustsFontSizeToFit + flexShrink + letterSpacing 0.3; shortened Restaurants titles to Spend/Visits/Places. (2) **Filter/legend pills wrap to 2 lines on iOS** (Android 1 line) — iOS font wider. Made compact across ALL filter/legend rows: DishLibrary quick filters (compact+xs), Plan pref chips, Insights legends (source + veg), History filter row. Left multi-select GRIDS wrapping (cuisine/dish/country pickers, ingredient chips) — correct. tsc=0, 154/154.
- **Build 10 tested → pills STILL wrapped (Paper Chip compact insufficient) + budget card taller. ROOT FIX (commit 302d49a→next):** Paper `<Chip>` internal padding/min-width can't shrink enough → built **`src/components/FilterPill.tsx`** (tight TouchableOpacity/View + Text, paddingH 10) and replaced Chip in ALL filter/legend rows: DishLibrary quick filters, Plan pref chips, History source filter, Insights source+veg legends. Multi-select GRIDS (AddMeal ingredients, cuisine/dish pickers) keep Chip. **Budget card taller** = subtitle "of $X budget" wrapped in narrow 3-col card → MetricCard subtitle now numberOfLines=1 + adjustsFontSizeToFit (equal height). tsc=0, 154/154. Removed dead Chip imports (dead styles left, harmless).
- **🎉 ANDROID v1.2.0 LIVE ON GOOGLE PLAY (2026-09-03 13:31) — the milestone is DONE on Android.** Google approved my morning submission within hours → Production Releases: "1.2.0 · Available on Google Play · full rollout, all countries." Public URL play.google.com/store/apps/details?id=com.thaliplan.app WORKS (verified logged-out: Install button, 10+ downloads). **CORRECTION to earlier: I wrongly said Android was never public — it WAS in review when I said it, then approved same day.** Sameer's friend installed the real public app. **Website Android button FLIPPED LIVE** (index.html, pushed to main 8b3ab73).
- **iOS: v1.2.0 (build 12) "Waiting for Review"** (Apple ≤48h, auto-releases). 1.1.0 currently live → App Store link works now, 1.2.0 auto-replaces on approval. Sameer verified build 12 on iPhone = "perfect" (FilterPill single-line pills, equal cards, all fixes).
- **Next Up:** (1) ⏳ Await Apple approval → iOS 1.2.0 auto-releases (only pending item). (2) Website live on both store links + v1.2.0 screens (Pages redeploying from main). BOTH public store links now work. NOTE (answered): IARC=FYI; app FREE both stores; H1B=refer to attorney.

## Prior Session (2026-09-02 PART 26 — v1.2.0 release: builds queued, submission prep)
- Sameer authorized full launch. Deleted all personal device screenshots from scratchpad (incl. a private IG/WhatsApp capture I mistakenly SENT — owned it). Bumped app.json 1.1.0→**1.2.0**, committed all session work to branch **v1.2.0-release**. tsc=0, 154/154.
- **BOUNDARY (held then resolved):** I cannot enter Apple/Play credentials / 2FA (hard rule). BUT `eas build`/`eas submit` use STORED keys (ASC API key + Play service account in eas.json) — auth cached as sameerrajwade — so no interactive login needed. EAS builds+submit ARE doable by me.
- **✅ EAS PRODUCTION BUILDS QUEUED (both, non-interactive, managed signing — no keystore pw):** Android AAB `ad9fe683` (versionCode 4→5), iOS IPA `4e1f4992` (Team 3HQ9T7PKN4, cert+profile valid). Running on EAS servers ~20-40min.
- **⚠️ TWO honest flags to Sameer:** (1) **`eas submit` uploads the BINARY, NOT store-listing screenshots.** Adding the new value-add images to the LISTING needs the console — and eas metadata canNOT push Play screenshots (Android listing images MUST be done in Play Console). Also the new generic screens (grocery/dishlibrary/dishdetail) are 1280×2706, NOT the App Store size (1290×2796) — need re-render at store sizes for iOS. (2) **iOS card-wrap layout is UNVERIFIED** (Sameer reported the bug on V1.1.0 iPhone; I never saw V1.2.0 on iOS) — recommend iOS → TestFlight → verify → prod, NOT straight to production, else we may ship the exact bug.
- **Next Up:** poll builds (`eas build:list`); when done → `eas submit` Android to production track + iOS to TestFlight (verify card layout) then prod; help Sameer add listing screenshots in-console; eas.json submit track is currently internal/draft → change to production for Android.
- Sameer found 7 real UI/device bugs (unit tests can't catch these; I skipped the device QA pass I'd offered — owned it). `tsc=0`, 154/154. Rebuild `build-fixes7.log`.
- (1) **Search icon on Home brand bar REMOVED** (Sameer: top bar is the brand, never discussed). Global search feature fully removed (SearchScreen deleted, nav/type reverted) — parked pending a proper placement decision.
- (2) **Metric card alignment** — budget progress bar made Outside Spend taller; MetricCard now RESERVES the progress slot on every card (fixed 11px area) so all grid cards stay equal height.
- (3) **Dish Library pills half-cut-off** — reverted the horizontal ScrollView to a flexWrap row so all 4 pills (This month/Show all/Fav/Not made 30+) are always fully visible (wraps if needed, never scrolls off).
- (4) **AddMeal Notes typed in REVERSE on Android** (classic controlled-multiline bug) — Notes now UNCONTROLLED (`defaultValue`, not `value`; state mirrors via onChangeText for save). Recipe field same class → `key={recipe-<dishName>}`+`defaultValue` (remounts for prefill, uncontrolled while typing).
- (5) **Text box hidden by keyboard (Android edge-to-edge)** — `undefined` KAV behavior relied on window-resize which edge-to-edge breaks; switched to `behavior="padding"` (event-driven) + `keyboardDismissMode="interactive"`. NEEDS device verify.
- (6) **Plan header touched the status bar** — Plan has no nav header (headerShown:false) and didn't inset; added `useSafeAreaInsets` → `paddingTop: insets.top + Spacing.sm` on the scroll content.
- (7) **Product tour "Restaurant memory" card opened Home not Restaurants** — added `homeScreen` to the tour Step, that step now pushes Home→Restaurants; screenLabel updated. Website: added grocery/dishlibrary/dishdetail to the docs/index.html gallery; removed search from demo home.html + re-rendered home.png.
- **ACCOUNTABILITY:** these are exactly the UI/device-behavior gaps unit tests miss. Committed to actually WALKING the device (real QA pass) after this build, not just green unit tests.
- **DEVICE QA WALK (found MORE bugs unit tests missed):** ✓#1 search gone (Home top bar clean), ✓#2 cards aligned (budget bar slot reserved), ✓#3 pills all visible (flexWrap). Reversal fix: found the DishDetailSheet notes+recipe fields ALSO reversed (only fixed AddMeal first) → made those uncontrolled (defaultValue) too; also the DishLibrary Add-Dish recipe field (key+defaultValue). **#5 keyboard STILL hid the Notes field** after behavior=padding → added `scrollRef.scrollToEnd()` on Notes onFocus (edge-to-edge Android doesn't auto-scroll). Rebuild `build-kbd.log` to verify #4/#5.
- **iOS FEEDBACK (Sameer on V1.1.0 iPhone — OLD store build):** (1) "MainHome" header+back on Home = ALREADY FIXED in V1.2.0 (HomeMain headerShown:false, AppNavigator:99). (2) metric cards (unique dishes/restaurants/spend) wrap wrong on iOS but Android perfect — grid reworked in V1.2.0; will NOT blind-edit Android-perfect layout; VERIFY + fix iOS card layout during EAS/TestFlight (can't test iOS locally, no simulator here).
- **DEVICE-VERIFIED (build-kbd on Pixel):** ✓#1 search gone, ✓#2 cards aligned, ✓#3 pills all visible, ✓#4 Notes typed "abcdef" → shows "abcdef" (reversal GONE), ✓#5 Notes field scrolls above keyboard on focus. #6 Plan safe-area + #7 tour Restaurant-step = code-fixed, NOT device-verified this session (adb navigation kept exiting to Sameer's personal apps — stopped poking to avoid his private screens). Both fixes are straightforward; verify on next device pass or TestFlight.
- **Next Up:** device-verify #6/#7; then EAS store builds iOS+Android (add haptics+offline at clean prebuild; verify iOS card grid + fix if needed). Release gate: catalog implicitly signed off.
- Sameer pushed back: budget wasn't in, "make sure what you promised except photo+month-view is done." Owned it (had only done elevation). Delivered the rest. `tsc=0`, **154/154** (added 16 negative-scenario tests, 138→154).
- **BUDGET-vs-actual (P1, their specific gap) DONE everywhere:** Insights new "Dine-out budget" card (spend/budget bar + left/over, monthly windows only); Home "Outside Spend" card shows `of $X budget` + progress bar (sage under / red over); Restaurant "Total Spend" same (This-month window). Added reusable `progress`/`progressColor` to MetricCard.
- **Also DONE:** dining-out cuisine lens (Insights "Dining out by cuisine", outside meals only); DishDetailSheet notes + times-cooked/last-made stat row + **delete dish** (new `deleteDish` in firestore+useDishStore, optimistic; `Dish.notes?` added); grocery "Share list" (unchecked items as text); **global search** (new SearchScreen in Home stack — dishes [meal-derived+library] + restaurants; search icon added to Home header).
- **QA:** ran full suite **5× → 154/154 every run, deterministic, zero flakiness**. New `edgeCases.test.ts` (16 negative/boundary: empty data, leftovers-only, all-dupes, future-only, degenerate grocery/insights/pack).
- **build-ui2.log BUILD SUCCESSFUL** — device OFFLINE at install (wireless dropped), NOT yet on-device this round; APK ready.
- **STILL OPEN (honest):** haptics + offline banner = need NATIVE deps (expo-haptics, netinfo) → do during the clean EAS prebuild, NOT the incremental mirror build (risk to store build). Consistent skeletons + grocery group-by-dish = minor P2, not done. Imagery + month-view = deferred by Sameer.
- **✅ build-ui2 INSTALLED on Pixel (pid 2005, clean).**
- **✅ GENERIC DEMO SCREENS (new functions) DONE:** they're HTML rebuilds in `branding/assets/screens/*.html` (The Rivera Family generic data — Veggie Stir-Fry/Grilled Salmon/Spaghetti Bolognese/Beef Tacos, Nonna's Kitchen/Green Bowl, USD; NOT real Rajwade), rendered to PNG via headless Chrome @1280×2706. Created/updated: **home** (now w/ search icon, 6-day streak, budget bar on Outside Spend, Grocery card), **grocery** (shared list), **dishlibrary** (Family/Kids toggle, 4 pills, Explore/Share/Import, catalog rows, "1,492 dishes"), **dishdetail** (ingredients+recipe+notes+delete). Copied to `docs/assets/screens/`. Verified renders look store-ready.
- **Next Up:** (optional) insights-budget + dish-share generic screens; wire new PNGs into docs/index.html gallery; skeletons/grocery-group if wanted. Then EAS store builds iOS+Android (add haptics+offline at that clean prebuild). Catalog signed off implicitly (Sameer browsing). ASO strings + QA sheet in artifact.

## Prior Session (2026-09-02 PART 23 — UI polish pass from audit + Sameer decisions)
- Sameer reviewed the launch-readiness report. **Decisions:** dish photos = NEXT MVP (defer); Calendar month-view = **deferred** (showed a mockup widget; Sameer + I agreed — don't disturb a surface new users are still learning, week view stays the one calendar); QA = his team runs from the artifact directly; demo screens = Claude to capture; **UI enhancements = build now (the main point).**
- **Built (pure-style, zero behavior change, tsc=0):** rolled the `makeElevation` warm-depth system through every previously-FLAT surface for cohesion — Calendar day cards, Grocery rows (+ hairline border), Family/Settings/Profile cards, and the DishDetailSheet modal (e3). Now the whole app shares one depth system (was half-lifted). Also added **Calendar past-day muting** (muted surface + "Past" pill, mirrors Plan) for week-view consistency.
- **NOT done (flagged, next UI increment):** budget-vs-actual ring on Home/Insights (P1); haptics (needs `expo-haptics` dep + rebuild); consistent skeletons across list screens (P2); dining-out cuisine lens (P2). Imagery + month-view intentionally deferred.
- **GAP-CLOSURE PASS (Sameer: "no gaps"):** swept EVERY card/sheet surface. Closed the ones missed in the first pass — PreferencesSection (on Profile), RestaurantDetailScreen, DishShareModal + DishPackImport sheets (e3), TourOverlay coach card (e3). Elevation now covers the whole app; only intentionally-flat surfaces remain (ShareAppCard/ShareStatModal = branded capture cards, StarRating = row, AuthScreen = no card). tsc=0. `build-elev2.log` BUILD SUCCESSFUL — but device OFFLINE at install (wireless dropped) so NOT yet on-device this round; APK ready.
- **TESTING STATUS (answered Sameer):** 3 tiers — (1) automated unit tests 138/19-suites green every build (logic safety net: footprint/insights/dishStats/catalog-floor/planner/grocery/recipe/streak/diet); (2) manual QA scenario sheet in the artifact = for the human team, NOT yet executed; (3) Claude ad-hoc device smoke checks per change. GAP: no Detox/RNTL UI-integration tests, manual matrix not formally executed. Offered to run a device QA pass myself + produce pass/fail log.
- **✅ build-elev2 INSTALLED + VERIFIED on Pixel:** Profile (Household + Preferences cards now lifted, consistent w/ Home) + Calendar (past days Aug31/Sep1 muted + "PAST" pill, day cards lifted). Elevation rollout complete + on-device confirmed. (Shots show real "Sameer/Rajwade" data → reinforces demo-data need; also visible: Monthly dine-out budget $500 = the unused-budget P1.)
- **Next Up (2 open Qs to Sameer, phone currently connected):** (1) run the device QA pass now (Claude walks the scenario sheet → pass/fail log)? (2) DEMO screenshots — set up generic "Riveria Family" household vs accept real-data internal shots? Then EAS store builds iOS+Android (release gate on catalog sign-off). Remaining UI increment (not yet done): budget-vs-actual ring, haptics (needs expo-haptics), consistent skeletons, dining-out cuisine lens.

## Prior Session (2026-09-02 PART 22 — Launch-readiness review, NO code changes)
- Sameer asked for 4 launch tracks: (1) expert screen-by-screen UX audit, (2) store demo w/ new features, (3) perfect ASO metadata, (4) full QA scenarios. Read remaining screens (Calendar, DishDetailSheet, Profile) to ground it. Produced a single **Launch Readiness artifact**: https://claude.ai/code/artifact/84c65ec2-6400-4df6-9ad6-5eddf7ea98b5 — a report covering all 4 in Sofra's own brand.
- **AUDIT (13 findings, ranked):** 2× P0 — (a) elevation system only half-applied (Home/DishLibrary/Restaurant lifted; Calendar/Grocery/Family/Settings/Profile/DishDetailSheet still flat = half-lifted app) → ~30min roll-out fix; (b) NO food imagery anywhere (all icon thumbnails) = biggest "posh" gap, needs scoping (curated dish images vs user meal photos). P1: budget-vs-actual never shown (monthlyDineOutBudget exists, unused on Home/Insights); Calendar week-only + no plan edit/swap; DishDetailSheet lacks notes/photo/stats/delete; no meal photo in AddMeal; no global search. P2: haptics, offline banner/queue, consistent skeletons, mute past days in Calendar, dining-out cuisine lens.
- **DEMO:** confirmed Android link immutable (package-based, safe now). Generic "Riveria Family" data setup + 8-shot list (new features first) + 30-45s video arc. Website gallery + store screenshots must be recaptured generic (current show real "Sameer" data).
- **ASO:** copy-ready both stores — iOS name "Sofra: Family Meal Planner" (26/30), subtitle "Plan meals, tiffins & grocery" (29/30), keywords 99/100; Play short desc "Plan dinners & tiffins, build a grocery list, and remember what you cook" (72/80); What's New + full-desc structure; India-store keyword localization to regional cuisine names.
- **QA:** full scenario sheet by area, weighted to this session's changes (pills/kids-Show-all/grocery-today-onward/share-counts/leftovers-in-insights/unique-count) + cross-cutting (dark, safe-area, offline, large text).
- **Next Up:** Sameer to pick — (A) fix the 2 P0s (elevation consistency now [safe] + scope imagery decision) BEFORE recapturing demo screenshots, then (B) EAS store builds iOS+Android (tomorrow's work; release gate lifts once catalog signed off on-device). ASO strings ready to paste; QA sheet ready for the test pass.

## Prior Session (2026-09-01 PART 21 — Indian-first catalog deep expansion, tsc=0, 138/138)
- Authored **+499 authentic Indian regional dishes** (real ingredients/diet/category) in new `src/data/indiaDeepCatalog.ts` (`INDIA_DEEP`), wired into STARTER_CATALOG. Catalog **993 → 1492**. All 5 priority regions now clear the 80 floor: **Maharashtrian 82, Gujarati 81, Punjabi 86, South Indian 82, Bengali 81** (were 22/15/17/22/21). Also deepened Rajasthani/Awadhi/Hyderabadi/Kashmiri/Goan/Sindhi/Bihari/Odia/Assamese/Andhra/Tamil/Kerala + new Karnataka & Konkani regions.
- **Data hygiene:** removed 45 name-collisions (dishes already in catalog, mostly common N-Indian under 'Pan-Indian'); fixed 3 pre-existing dupes (renamed British 'Chicken Tikka Masala'→'British Chicken Tikka Masala', Greek 'Baklava'→'Greek Baklava', Pan-Indian 'Dum Aloo'→'Dum Aloo Banarasi'). Catalog now has ZERO duplicate names.
- **New test** `src/data/__tests__/catalogCoverage.test.ts` — enforces the 80-dish floor for the 5 priority regions + ≥600 Indian total + no-duplicate-names + valid diet/category. Prevents the "7 Maharashtrian dishes" regression from ever recurring.
- **NOTE (told Sameer):** catalog is a BUNDLED static file in the app (not Firestore) — new dishes reach users only via a new app build/release, not a live DB push.
- Building test APK `build-catalog.log`. **PLAN (Sameer): today = local test APK to phone; TOMORROW = EAS store builds for iOS + Android.** ⚠️ Release gate lifts for EAS only once Sameer confirms the catalog on-device — EAS AAB/IPA is tomorrow's work.

## Prior Session (2026-09-01 PART 20 — Sameer on-device feedback, 6 fixes, tsc=0, 130/130, device-verified)
- Sameer tested PART-19 APK on Pixel, gave 6 items. Fixed all. `tsc=0`, **130/130** (+2 insights leftover tests). Rebuild `build-fixes2.log`.
- (1) **Unique Dishes count** — Home `uniqueDishNames` now counts thali sides in items[] (dedup lowercase, excl leftovers) → matches Dish Library "This month" (was 1 vs 2). Insights `uniqueDishes` aligned the same way (home + items + excl leftovers).
- (2) **Dish Library layout** — 4 pills now a single horizontal ScrollView row (This month/Show all/Favorites/**Not made 30+** [shortened]); Explore+Share+Import collapsed into ONE 3-button row (were 2 stacked rows).
- (3) Leftovers correctly hidden — Sameer confirmed, no change.
- (4) **Grocery plan pull** — `addFromPlan` now shops TODAY-onward (`from = max(weekStart, today)`), never re-pulls ingredients for already-cooked past days (was pulling yesterday's dishes).
- (5) **Insights exclude Leftovers** — cuisine breakdown (was showing phantom "Other" from leftovers; now excluded from numerator AND denominator) + most-cooked (was listing "Leftovers") + unique dishes all skip the leftovers marker.
- (6) **Import/Export placement** — REMOVED from Family (Sameer disliked it there); primary stays in Dish Library; added a dedicated **"Dishes" section in Settings** (its own section, not nested) as the secondary path.

## Prior Session (2026-09-01 PART 19 — UX-fixes release BUILT, tsc=0, 128/128, device-verified)
- Built all 5 locked workstreams (see PART 18). `tsc=0`, **128/128** (was 124; +4 new footprint/leftovers tests). Share footprint locked to **strictly cooked** (meal-derived, not library∪meals). NOT device-tested yet — no APK built this session.
- **WS2 Footprint (dishPack.ts):** rewrote `buildDishPack` to be MEAL-DERIVED — family dishes = distinct dishes cooked at home (incl. thali items[], excl. leftovers); kids = distinct kids-meal dishes; restaurants = distinct restaurantName from outside meals (fixes 35→27; matches Restaurants tab). Library/restaurant docs are ENRICHMENT only. Updated dishPack.test (12 tests: cooked-only, unsaved-included, thali, leftovers-excl, restaurant meal-derived).
- **WS1 Unified filter model (DishLibraryScreen):** independent audience (family|kids) × view preset (month default / all / favorites / stale). Each pill self-sufficient (fixes #3 empty-stale + kids "Show all" bug A). Header "Dish Library"; Family|Kids segmented toggle shows only when kids meals exist. Insights drill-down now uses `focusNames`+`window` (separate from pills). New params in navigation/types; HomeScreen 3 deep-links + InsightsScreen openDish updated.
- **WS3 Leftovers guard:** `aggregateDishes` now excludes the leftovers marker (was showing a phantom "Leftovers" library row) + test; AddMealScreen hides the Ingredients & recipe section and never spawns a "Leftovers" dish when the dish is Leftovers.
- **WS4 Import/Export → Dish Library:** new `components/DishShareModal.tsx` — 3-bucket checklist (All dishes / Kids tiffins / Restaurants, pre-ticked, empty buckets disabled, confirm-before-share) → creates pack → BRANDED CARD IMAGE (Sofra + code + selected counts + "from <household>") shared via captureRef+RNShare with the code. "Share dishes / Import a code" row added under "Explore more dishes" in DishLibrary. FamilyScreen upgraded to the same modal (dropped old plain-text share + its imports).
- **WS5 5-tab IA (Option B):** Grocery removed from the bottom bar (now Home·Calendar·Plan·Insights·Profile) and moved INTO the Home stack; reachable via a **Grocery quick-entry MetricCard** in HomeScreen — fills the blank half-cell next to Kids Tiffins (full-width when no kids), plus a copy in the empty state so it's always reachable. Shows unchecked-item count. types.ts: Grocery moved MainTab→HomeStack.
- **WS6 UI UPLIFT (the long-deferred premium-polish pass, Sameer asked to include it):** added ONE cohesive **elevation system** `makeElevation(isDark)` in theme.ts (e1 cards / e2 raised / e3 sheets) with a WARM brown shadow in light mode (not pure black — matches terracotta ground) and border-led depth in dark. Replaced the ad-hoc per-card `shadowColor:c.black, opacity 0.05` in MetricCard, MealCard, QuickActions with `...elevation.e1`; lifted the previously-flat DishLibrary dish rows + Restaurant cards to e1 too. Now depth is uniform across Home/Calendar/Insights/DishLibrary/Restaurants. (Still deferred: translucent layered surfaces beyond the existing icon-badge tints — the elevation system is the core of what was promised.)
- **REVIEW before build:** removed dead `navigation`/`useNavigation` in DishLibrary; memoized `seed` (kids `[]` had unstable identity → aggregates recomputed each render); swept for dangling `initialFilter`/`monthDishes`/`quickFilter`/Grocery-tab refs (clean); FamilyScreen import cleanup verified. `tsc=0`, 128/128.
- **APK BUILT + INSTALLED + VERIFIED ON PIXEL:** `build-ux-fixes.log` BUILD SUCCESSFUL 4m12s → app-release.apk (94MB) → `adb install -r` Success (device `57150DLCH002E1` wireless), launched clean (pid, no FATAL). Screenshotted: Home dashboard (Grocery card fills the Kids-Tiffins blank cell, warm elevation visible, 5 tabs, **Leftovers dinner shows guarded** — Home pill only, no ingredients/recipe/cuisine) + Dish Library (header "Dish Library", Family|Kids toggle, 4 pills This-month-default/Show-all/Fav/Not-made-30+, Share dishes|Import a code row). All 6 workstreams visually confirmed on-device.
- **⚠️ FLAGGED to Sameer (open decision):** Home "Unique Dishes" (counts PRIMARY dishName only) = 1, but Dish Library "This month" (counts thali items[]) = 2 → tap-through mismatch. Pre-existing; exposed by the new nav. Options: align Home to count items[] (→2, recommended) or keep primary-only. NOT changed yet.
- **Next Up:** Sameer's on-device review of pills/kids-Show-all/share-counts(27)/branded-card/depth-feel; decide the Unique Dishes count. Then catalog Indian-first deep (#1). Release gate still active.
- **Next Up:** (1) Build a local test APK (short-path C:\S, JS-incremental) + install on Pixel → Sameer device-tests: pills incl. kids "Show all", share counts vs Restaurants tab (27), branded card, 5-tab bar + Grocery Home card, leftovers has no ingredients. (2) Then catalog Indian-first deep + coverage-floor test (#1, follow-up release). (3) Deferred: the real premium-polish design pass (elevation system, not ad-hoc shadows — only soft card shadows shipped so far). Release gate still active (no EAS AAB/IPA until sign-off).

## Prior Session (2026-09-01 PART 18 — Sameer feedback ALIGNMENT, planning only, NO code)
- Sameer tested the latest APK (no new-dishes pack) and raised 6 product-level points + 2 more bugs. Ran a grounded discussion (read DishLibrary/Home/AppNavigator/dishPack/quickActions/grocery/firestore). **NOTHING built — awaiting final scope lock.**
- **Diagnosed, agreed:** (1) Catalog is 993 dishes but the MIX is wrong — Maharashtrian only ~22 vs Japanese 31; decision **Indian-first deep** (core Indian regions 80–150 each + a coverage-floor test). (2) Leftovers already excluded from library/counts/planner (items:[], cuisine:'') — only gap = suppress ingredient/recipe/grocery UI on a Leftovers entry. (3) **Pill bug (real):** DishLibrary opens scoped (monthDishes/window) and "Not made 30+" filters WITHIN the scope → 0 results; want header "Dish Library" + 4 pills (This month DEFAULT / Show all / Fav / Not made 30+), each self-sufficient. (4) Move Import/Export INTO Dish Library (by "Explore more dishes"); export = 3 checkboxes (All/Kids tiffin/Restaurants, confirm first) + **branded share-card IMAGE** w/ code; import = code only. (5/6) 6 tabs congested → drop to 5, Grocery → Home card in the blank cell next to Kids Tiffins; import/export's real home is Dish Library not Settings/Family.
- **TWO new bugs (root-caused):** (A) Kids-tiffin "Show all" dumps the WHOLE family library (scope has 2 dims audience×window; "Show all" wipes both — same disease as #3). (B) Share-pack counts match nothing (showed 28 dishes/16 tiffins/35 restaurants vs real 50+/27): `buildDishPack` packs LIBRARY dishes only (misses meal-history/"virtual" dishes → undercount) AND `getRestaurants` returns all auto-created + unnormalized-dupe restaurant docs (overcount). **Root fix = unified filter model (audience×scope×status) + single "household footprint" source (library ∪ meal dishes; normalized-deduped restaurants) that BOTH the library and the share pack read.**
- **ALL DECISIONS LOCKED:** catalog = **Indian-first deep** (follow-up pass); sequencing = **UX fixes first, catalog after**; IA = **Option B** (5 tabs Home·Calendar·Plan·Insights·Profile — Sameer KEEPS Calendar as a tab: active users still familiarizing, deep-link ≠ findable; Grocery → Home card in the blank cell next to Kids Tiffins). **Share footprint = meal-derived truth:** dishes = library ∪ home-meal dishes (incl. thali items[]); restaurants = distinct restaurantName from outside meals (all-time) — **NOT `getRestaurants()` docs**. ROOT CAUSE of the 35-vs-27: Restaurants tab derives from MEALS (RestaurantScreen.tsx:98) = honest 27; share pack read raw `restaurants/{id}` docs (inflated by rate-a-dish + pack-import 0-visit placeholders + name variants) = 35. Same disease as the dish undercount — pack must read the same meal-derived source the screens do.
- **LOCKED PLAN — UX-fixes release (build in this order, tsc+jest green each step, NO store package till Sameer signs off):** (1) **Unified filter model** on DishLibrary — audience×scope×status independent; header "Dish Library" + pills This-month(default)/Show-all/Fav/Not-made-30+ each self-sufficient (fixes #3 + kids "Show all" A). Files: DishLibraryScreen, HomeScreen params. (2) **Honest footprint** — new tested `utils/footprint.ts`; buildDishPack reads it (fixes share counts B). Files: footprint.ts+test, dishPack.ts, FamilyScreen. (3) **Leftovers UI guard** — suppress ingredient/recipe/grocery affordances on a Leftovers entry (#2). Files: DishDetailSheet, AddMealScreen. (4) **Import/Export into DishLibrary** by "Explore more dishes" — export = 3 checkboxes (All/Kids/Restaurants, confirm first) + branded share-card IMAGE w/ code; import = code only; keep secondary pointer in Family (#4/6). (5) **5-tab IA Option B** — Grocery off the bar → Home quick-entry card; AppNavigator + HomeScreen (#5). One open nuance flagged to Sameer: union includes onboarding-seeded-but-never-cooked library dishes (default = share them; alt = meals-only "strictly made").
- **Next Up:** Sameer says "go" → build Workstream 1 first. Catalog Indian-first deep + coverage-floor test #1 is the follow-up release. Release gate still active (no EAS AAB/IPA until sign-off).

## Prior Session (2026-08-31 PART 8 — 5 Sameer fixes + catalog 472 ✅ CODE DONE, awaiting device)
- **All 5 fixes done (tsc=0, 92/92):** (1) **Ingredients in AddMeal** — optional collapsed section at bottom of home-meal form (`ingSection`), prefills from existing dish OR `matchCatalogDish`, saves onto the dish on meal-save (upsert, non-blocking, home only); recipe slots here later. (2) **Selectable grocery** — `DishDetailSheet` ingredients now rows w/ **checkbox (default all ticked)** + Title Case + remove-x; "Add N to grocery" adds only ticked (answer to "what if already have it at home" = untick staples). (3) **Catalog 430→472** (2nd depth pass: German/Turkish/Greek/Spanish/French/British/Japanese/Thai/Korean/Vietnamese + more India everyday/Kerala/Tamil/Awadhi). (4) **Title Case** display for grocery items + ingredients everywhere (`toTitleCase`; dedup still on lowercase key). (5) **Thin DishLibrary top 4→2 rows** — search row w/ single `tune-variant` filter icon (combined Sort+Cuisine Menu w/ check marks), one horiz-scroll row = All/Favorites/stale + **Explore chip** (removed the big banner + separate filter row; state `filterMenuVisible` replaced cuisine/sort menus).
- **✅ PART-8 build ON DEVICE (PART 9):** phone reconnected (adb `57150DLCH002E1` over TLS); `install -r` PART-8 APK → Success, launched clean. All 5 fixes + 472 catalog live on Pixel.
- **✅ BATCH A BUILT (Phase-5 delights, tsc=0, 104/104):** new `src/utils/quickActions.ts` (pure) — `cookAgainDishes` (recent home dishes, dedup, excl. kids/outside/leftovers/future), `decideForMe` (single suggestion; planner-style staleness+favorite score, avoidRepeatDays skip, injectable rng/now), `isLeftovers`/`LEFTOVERS_NAME` marker + `quickActions.test.ts` (12 tests). New `src/components/QuickActions.tsx` (presentational: "Decide for me" + "Leftovers" pills, suggestion card w/ Cook-this/shuffle/dismiss, horiz "Cook again" chips). Wired into `HomeScreen` ("Quick add" section, shows once meals>0): `quickLog` fills first-empty family slot today (else dinner), replace-confirm on conflict (mirrors AddMeal dupe guard). Leftovers logged as home meal w/ items:[] + cuisine:'' and EXCLUDED from unique-dish count + "haven't made in a while" (honest streak, no fake dish). **APK #1 building** (`C:\S\android\build-batchA.log`, JS-only incremental) → then install for Sameer.
- **✅ BATCH A ON DEVICE + BATCH B RECIPE BUILT (PART 10, tsc=0, 113/113):** APK#1 (Batch A) installed on Pixel, launched clean — awaiting Sameer's cook-again/decide/leftovers test. **Recipe-on-dish (Phase 3) DONE:** `Dish.recipe?:{type:'youtube'|'url'|'text',value}` (schemaless); new `src/utils/recipe.ts` (`parseRecipeInput` classifies single-token URL→youtube/url [auto-adds https], else text; `youtubeId`, `recipeActionLabel`, `recipeIcon`) + `recipe.test.ts` (9 tests). `DishDetailSheet` gains a Recipe section below grocery: add/paste link or type steps → Save; link shows Watch/Open (Linking.openURL), text shows inline; ✏️ edit, clear-to-remove (writes recipe:null). `stripUndefined` recurses plain objects so the map writes clean. **APK#2 (recipe) BUILT + INSTALLED on Pixel** (build-batchB.log, 1m14s, launched clean pid 5868) — awaiting Sameer's recipe test.
- **✅ WIDGET DECISION (Sameer):** BOTH Android + iOS widgets ship IN 1.2.0 (parity over speed). = the big native workstream: Android `react-native-android-widget` (config plugin, JSX layout) + iOS WidgetKit Swift extension w/ App Group data share. Needs a FULL clean native rebuild (not JS-incremental) + EAS build on Sameer's iPhone to test iOS (can't test iOS widget on Pixel). Proposed content: "Tonight: <planned dinner>" + tap-to-log fallback when nothing planned (awaiting Sameer's confirm on content).
- **✅ SAMEER FEEDBACK FIXES (PART 11, tsc=0, 115/115):** (1) QuickActions pills wrapped wrong → numberOfLines+adjustsFontSizeToFit+flexShrink+h-padding. (2) "Quick add" placement now CONDITIONAL — top (under dashboard) when today has no meals, bottom once today is planned (`todayPlanned` in HomeScreen). (3) DishLibrary "Explore" chip was clipped in the horizontal filter scroll → pulled OUT into its own full-width dashed **"Explore more dishes ›"** row. (4) NEW **"Add this week's ingredients to grocery"** button on PlanScreen — gathers home-dish ingredients from the current plan, dedupes (`ingredientsForDishes`), adds via `addGroceryItems`; alerts if dishes have no ingredients yet (answers Sameer's "how do ingredients get to grocery"). (8) **PLANNER VARIETY FIX (big):** `pickDish` was top-3-by-fixed-sort → seeded/tied dishes gave the SAME picks every regenerate. Rewrote to WEIGHTED-RANDOM over the whole eligible pool (weight=score−cuisinePenalty, floor 1); threaded optional `rng` through `generateMealPlan`/`pickDish` (default Math.random; rng=0 still picks top so old tests hold) + 2 new variety tests. **✅ APK BUILT + INSTALLED on Pixel** (build-fixes.log, 1m19s, launched clean pid 23242) — awaiting Sameer's re-test of 1/2/3/4/8 (esp. planner regenerate variety + Plan→grocery).
- **✅ FEEDBACK ROUND 2 (PART 12, tsc=0, 115/115):** (1) recipe in AddMeal — main-dish ingredient section now also has a **Recipe** field; (6) **BIG**: that section was placed BELOW "Add another dish" so it read as belonging to sides → **relocated directly under Main dish** ("Ingredients & recipe", collapsible), saves recipe onto the dish on meal-save (parseRecipeInput, recipeTouched guard). (7) removed duplicate "Add meal" — dropped the in-content hero heading, set the modal header title dynamically (Add/Edit). (8) tour finish now resets Profile tab → ProfileMain (was stranding on Settings = "still in demo"). (3) DishLibrary rows show **Ingredients · N** + **Recipe** discoverability pills next to cuisine (active style when present; both open the detail sheet). (4) **Add Dish** dialog now has Ingredients (comma) + Recipe fields (scrollable content). (2) **MealCard** (Home+Calendar) shows a **Video/Recipe** button when the home dish has a link recipe → opens it (Linking), no card-size change. (5) **Grocery tab** now has a time-aware **"Add {this|next} week's plan ingredients"** pull (Mon–Wed=this week, Thu–Sun=next) — reads home meals in that week, dedupes ingredients, adds. (Kept the Plan-tab button too.) **APK building** (build-fb2.log).
- **✅ FEEDBACK ROUND 3 (PART 13, tsc=0, 115/115):** **ROOT-CAUSE BUG FOUND** — `handleSave` in AddMealScreen had a **stale-closure deps bug**: `ingredients/ingTouched/recipeDraft/recipeTouched/dishes/addDish/updateDish` were NOT in the useCallback deps, so ingredients/recipe typed after mount were **never persisted** (Sameer's "Bharit didn't save" + recipe-not-showing). Added them → saves now work. (1) MealCard recipe = **ONE simple pill** "Recipe" for ANY type (was hidden for text + labeled Video/Recipe); opens YouTube/browser via Linking, text opens a Portal Modal text box. (2) moved Ingredients & recipe section to **AFTER all dishes, before Cuisine** (dishes are the priority; Sameer reversed the last round's placement). (4) DishLibrary quick-filter pills were a **horizontal ScrollView with flexWrap** → stretched vertically & overlapped the Explore row; changed to a plain wrapping `View`. **APK building** (build-fb3.log).
- **🎨 DESIGN CALLS (Sameer asked):** (9) Home brand header ~30% is too big — AGREED, redesign to compact: greeting top-left + Sofra/tagline right (own visible change, next). (10) Glassmorphism — RECOMMENDED AGAINST real blur (native @react-native-community/blur = Android-inconsistent, perf + New-Arch risk on a live app); instead a tasteful premium-polish pass (depth/shadows/translucent surfaces/gradients). Both deferred to a design batch.
- **✅ FEEDBACK ROUND 4 (PART 14, tsc=0, 115/115):** (a) **Grocery catalog fallback** — week-pull only read dish-doc ingredients; dishes like Avocado Toast keep ingredients in the CATALOG → now falls back to `matchCatalogDish(name).ingredients` when the dish doc has none (both Grocery-tab pull + Plan button). (9) **Compact Home header** — was ~30% full-bleed brand block; now a slim row (greeting left, Sofra+tagline right in a pill), height insets+76 (was +156). (b) **Quick add redesign** (Sameer unhappy, deferred to me) — consolidated into ONE cohesive card: "Cook again" chips + divider + Decide-for-me / Leftovers split row; suggestion takes over the card (Cook this / Shuffle / dismiss). (10) light polish (soft card shadow). **APK building** (build-fb4.log).
- **📋 STATUS BOARD** artifact for Sameer: https://claude.ai/code/artifact/af676350-353a-488c-96f2-d0f8105f7a78 (done vs building vs queued).
- **✅ AUTONOMOUS BATCH (PART 15, Sameer asleep, tsc=0, 124/124):** **Dish-pack sharing (Phase 4) DONE + on device.** `DishPack` types; `utils/dishPack.ts` (`generatePackCode` unambiguous alphabet, `buildDishPack` = definitions ONLY [family dishes + distinct kids-tiffin dishes + restaurant NAMES; never meals/ratings/spend/history], `newPackDishes`, `packDishesToDishes`) + `dishPack.test.ts` (9 tests incl. privacy asserts). `firestore.createDishPack`/`getDishPack` + `createRestaurantIfMissing` (0 visits, no fake). **`dishPacks/{code}` rule added + DEPLOYED** (read=signed-in, write=creator). `DishPackImport.tsx` (code → checklist grouped family/kids/restaurants, pre-ticked new, import via addDishesBatch). FamilyScreen "Share & import dishes" section: Share (builds pack → `Share.share` code) + Import modal. **APK built + installed** (build-share.log, pid 30892). **Notification deep-linking DONE (building):** daily → Calendar focused on **tomorrow** (highlight+scroll, resolves tomorrow at tap time); weekly → Plan **autoGenerate** (fresh unsaved plan); monthly → Insights lastMonth. Updated `notifications.ts` data payloads, App.tsx response listener, MainTab param types (Calendar.focusDate, Plan.autoGenerate), PlanScreen (auto-gen effect), CalendarScreen (focus highlight+scroll). **build-notif.log built + INSTALLED** (pid 32354). **Polish pass (#10):** soft depth/shadow on MetricCard + MealCard (matches QuickActions card). **build-polish.log built + INSTALLED** (pid 1592). Status artifact updated (sharing=Done, notif+polish=in build). **AUTONOMOUS RUN COMPLETE — 3 APKs installed tonight (sharing, notifications, polish).**
- **✅ WEBSITE + GIT MERGE (PART 16, 2026-09-01):** Added new-feature cards to `docs/index.html` (Grocery / Recipes / Dish sharing / Ten-second shortcuts) + a "Groceries, recipes & sharing" section in `docs/features.html`; refreshed auto-plan + reminders copy. **Committed the whole v1.2.0 feature set on branch `v1.2.0-features` → merged to `main` (fast-forward) → pushed** (`d762a8d..5cd80da`) → Pages redeploying sofra.savvylabs.dev. `tsc=0`, 124/124.
- **⚠️ DEVICE OFFLINE (wireless adb dropped 2026-09-01 AM):** could NOT capture new-feature SCREENSHOTS for the gallery, nor install more APKs. Gallery still shows the 9 OLD screens — new screenshots (Grocery, dish recipe, Quick add, Sharing) PENDING a phone reconnect → capture → add to `docs/assets/screens/` + gallery + follow-up push.
- **⚠️ FLAGGED:** `docs/` is the Pages source, so internal .md in it (PROGRESS, FEATURE_ROADMAP, *-deploy-checklist, ios-firebase-setup, release-playbook, store-listing) are PUBLICLY served. Pre-existing; recommend a Jekyll `_config.yml` exclude or moving internal docs out of `docs/`. Awaiting decision.
- **✅ CATALOG EXPANSION + CRASH FIX (PART 17, 2026-09-01, tsc=0, 124/124):** Sameer gated ALL iOS/Android store packages behind expanding the meal catalog. Used WebFetch on ~30 Wikipedia cuisine pages → authored **+314 researched dishes with real ingredients** (catalog 468 → **~782**), staged in scratchpad `catalog-research{,-2}.txt`, transformed via `gen-catalog.js` (dedup vs existing, category/diet validation, non-grocery token strip) → `RESEARCH_EXPANSION` + `RESEARCH_EXPANSION_2` arrays appended to `starterCatalog.ts` (wired into STARTER_CATALOG). Cuisines: Japanese/Korean/Thai/Vietnamese/Filipino/Chinese/Indonesian/Malaysian/Turkish/Lebanese/Greek/French/Spanish/German/British/Polish/Russian/Portuguese/Ethiopian/Moroccan/Nigerian/Mexican/Brazilian/Peruvian/Argentine/Jamaican/Pakistani/Sri Lankan. **Also FIXED a FamilyScreen crash** ("Maximum update depth exceeded" — store selector `s.dishes.map()` returned new array each render → useSyncExternalStore infinite loop; fixed = select stable `s.dishes` + `useMemo`). Found it via device screenshot testing. **Committed + merged to main + pushed** (`5cd80da..694f322`).
- **⚠️ RELEASE GATE (Sameer, explicit):** do NOT build any iOS/Android STORE package (EAS AAB/IPA) until Sameer signs off on the expanded catalog. Local test APKs for verification are fine (not "packages"). EAS is authenticated (`sameerrajwade`/`savvylabs`, managed signing). Sameer wants: Play production release of ONLY v1.2.0 (no What's New), iOS 1.2.0 submitted so it auto-releases (Apple review is mandatory — can't skip), website store links updated, generic "Riveria Family"-style branding on new screenshots. Prereqs still open: version bump 1.1.0→1.2.0, targetSdk/edge-to-edge compliance, widgets decision (default: ship without → 1.2.1), device-verify the FamilyScreen fix + browse new catalog. **Captured screenshots so far** (Home/Grocery/DishLibrary/DishDetail) show real "Sameer" data → MUST retake generic before store use.
- **Next Up (needs Sameer / devices / decisions — did NOT do autonomously):** (1) **Widgets** Android+iOS — native dep + config plugin + EAS build on Sameer's iPhone for iOS. (2) **Tech debt** (edge-to-edge/orientation/branded links) — fold into the 1.2.0 native build; verify Google targetSdk first. (3) **Catalog data pass** — real web-sourced dishes (research-heavy, quality-critical — save for a focused session, not autonomous). (4) **Marketing refresh** — after UI is final. (5) **1.2.0 store release** — bump version, EAS builds, staged rollout both stores (release action = Sameer). Android prod-access granted; still HOLDING for 1.2.0.
- **⏳ STILL OPEN from feedback:** (5) Catalog quality — Sameer wants real web-crawled dish/cuisine data for ~195 countries, not hand-authored ~275; needs a sourcing plan. (6) Marketing refresh — demo video + 9-screen website gallery + store listings must show the NEW features (grocery, recipes, quick-add, catalog) before 1.2.0 launch. (7) Notification deep-linking — daily→tomorrow's-menu card, weekly→Calendar w/ auto-gen (unsaved) plan, monthly→Insights wins; needs notification `data` payload + nav response handler (was NOTIF-DEEPLINK in backlog).
- **Next Up:** install APK (fixes) → Sameer re-tests 1/2/3/4/8. Then widgets (both platforms, in 1.2.0). Then tackle 5/6/7. Then Batch C = Phase-4 share. Then tech-debt + bump 1.2.0 + verify targetSdk + staged rollout both stores. Android prod-access granted; HOLDING for 1.2.0.

## Prior Session (2026-08-31 PART 7 — Phase 2 bug fixes + catalog depth pass ✅, on device)
- **FIXED grocery + ingredient write errors (Sameer hit "error" on both):** (1) `firestore.rules` had NO rule for `households/{id}/grocery/**` → deny-all blocked every grocery write. Added member-scoped read/create/update/delete rule + **DEPLOYED** (`firebase deploy --only firestore:rules --project thaliplan`, compiled+released OK). (2) `DishDetailSheet` called `updateDish` on VIRTUAL dishes (history-only, id=name, no doc) → update failed = the "error" on first ingredient add. Now `ensureRealDish()` creates the doc via `addDish` first (mirrors favorite-on-virtual), then writes; added revert + visible Alert on failure; addToGrocery ensures real dish + confirms count.
- **Catalog DEPTH pass 285 → 430 dishes (Sameer: too thin/no research).** Per-region now: Pan-Indian 33, American 25, South Indian 22, **Maharashtrian 22 (was 7)**, Punjabi 17, Italian 16, Gujarati 15, Chinese 14, Bengali 13, Middle-Eastern 12, Mexican 11 + Japanese/Thai/Korean/British/French/Greek/Spanish deepened; new India regions Hyderabadi/Bihari/Sindhi/Assamese/Odia. Added as `EXPANSION` array. Still 63 countries. tsc=0, 92/92.
- **✅ REBUILT + REINSTALLED on Pixel** (`192.168.86.121:33507`): synced src→C:\S, `assembleRelease` (1m22s), `install -r` (no uninstall — same debug key as PART 6 dev build), launched clean (pid, no FATAL). Grocery rule is live cloud-side so it applies even to the PART 6 build.
- **⚠️ Sameer to re-test:** grocery add (manual + dish→ingredients→grocery), ingredient add on a dish, ⋯ menu now appears once list has items. NOTE menu only shows when grocery list non-empty (by design). Onboarding seed still only on NEW-household create; Explore banner = the always-visible path.

## Prior Session (2026-08-31 PART 6 — Phase 1 UI BUILT + catalog → 63 countries ✅, autonomous)
- **Catalog 90 → 285 dishes / 63 countries** (Sameer: "expand to ≥50 countries"). Added all continents — Europe (Germany, Poland, Russia, Ukraine, Portugal, Netherlands, Sweden, Ireland, Hungary, Austria, Switzerland, Belgium, Turkey), Asia (Bangladesh, Nepal, Malaysia, Singapore, Myanmar, Cambodia, Taiwan, Iran, Iraq, Israel, Saudi, Afghan, Uzbek), Americas (Brazil, Argentina, Peru, Colombia, Chile, Cuba, Jamaica, Canada, Venezuela), Africa (Morocco, Egypt, S.Africa, Kenya, Ghana, Senegal), Oceania (Australia, NZ) + India regional depth + China/Italy/Mexico/USA regions. Fixed Middle-Eastern `cuisineTag:'Indian'` bug. **Not a cap — keeps growing by array append.**
- **Phase 1 UI + logic BUILT (tsc=0, 85/85 tests, 14 suites — was 82):** `src/components/StarterDishPicker.tsx` (reusable 3-step: country search+multi-select → CONDITIONAL region step (only if `regionsForCountry>1`) → veg toggle + category-grouped pre-ticked pills w/ show-more/add-all/counter + "add your own" autocomplete + "Add these N"). Onboarding: `HouseholdSetupScreen` name→seed step; `createHousehold(name,userId,seedCatalog?)` seeds BEFORE flipping householdId (so library/planner populated on first load). `firestore.addDishesBatch` (chunked writeBatch ≤450) + `useDishStore.addDishesBatch` (dedupes by name). Planner: never-cooked score capped (NEW_DISH_NOVELTY=21, not 999) so real rotation wins once history exists (+test). `DishLibraryScreen` visible "Explore dishes" banner → same picker in a Modal (NO hidden screen). AddMeal: `handleSelectDish` falls back to `matchCatalogDish` to pre-fill cuisine. `Dish.ingredients?` + `catalogEntriesToDishes` helper added.
- **Phase 2 BUILT END-TO-END (logic + UI, tsc=0, 92/92, 15 suites):** `src/utils/grocery.ts` (`normalizeIngredient` w/ light plural-fold, `dedupeNewItems`, `ingredientsForDishes`) +7 tests; `GroceryItem` type; `firestore` grocery CRUD (getGroceryItems, addGroceryItems batch, setGroceryChecked, deleteGroceryItem, deleteGroceryItems); `useShoppingStore` (cache-first, optimistic toggle, clearChecked/clearAll, dedupe on add) + switchHousehold cache-clear + barrel. **UI:** new **Grocery TAB** (6th tab, cart icon — `src/screens/GroceryScreen.tsx`: one shared checklist, checkboxes, manual add e.g. "dishwashing pods", checked sink to bottom, Clear checked / Clear all via ⋯ menu). **`DishDetailSheet.tsx`**: tap a dish in Library → view/edit `Dish.ingredients` chips + "Add to grocery list". DishLibrary row: tap = open sheet, STAR is now its own tap target for favorite (was whole-row). AddMeal 10s path untouched.
- **✅ BUILT + INSTALLED ON PIXEL (dev release APK, debug-signed).** Mirrored src→C:\S, `assembleRelease` (4m18s, JS-only), **had to UNINSTALL the Play vc4 build first** (Play-signed can't be replaced by debug-signed) → installed dev APK, launched clean (pid, no FATAL). Wireless adb dropped mid-build; reconnected via `adb connect 192.168.86.121:33507` (mdns). **Sameer re-signs in w/ Google (data is in Firestore, nothing lost).** Device: `192.168.86.121:33507` / `57150DLCH002E1`.
- **⚠️ ON-DEVICE TEST NEEDED (Sameer):** onboarding seed picker (needs a NEW household to see it) + Explore modal (Dish Library banner) + Grocery tab + dish→ingredients→grocery. Watch: Paper Modal full-screen feel, chip perf w/ 285 dishes, 6-tab bar width.
- **Next Up:** Sameer device-tests Phase 1+2 → tweaks → then Phase 3 (recipe-on-dish: `Dish.recipe?` view/edit in DishDetailSheet). Optional Phase 2 polish: Plan "add this week's ingredients" one-tap. Android prod-access wait still open in parallel. NOTE: device now has DEV build, not Play build — reinstall store build from Play to get back to prod.

## Prior Session (2026-08-30 PART 4 — roadmap written + Phase-1 FOUNDATION built ✅)
- **Full phased plan → [docs/FEATURE_ROADMAP.md](FEATURE_ROADMAP.md)** (scored table: Value/Effort, 13 features across 5 phases + "NOT building" traps). Read that first for the build plan.
- **Share model FINALIZED (Sameer):** dish-pack shares ONLY → unique **family dishes** (defs), unique **kids-tiffin dishes** (distinct audience='kids' names), unique **restaurant NAMES** (+cuisineType, NOT their dishes/ratings/spend/visits). Never meals/spend/ratings. Phase 4.
- **BUILT Phase-1 foundation (tsc=0, full suite 82/82, +13 new):**
  - `src/data/starterCatalog.ts` — global catalog (~90 dishes): India regions (Punjabi/Gujarati/Maharashtrian/South Indian/Bengali/Pan-Indian) + world (Chinese/Italian/Mexican/American/Thai/Middle-Eastern), each w/ `{continent,country,region,name,cuisineTag BROAD,diet,category,ingredients,weight}`. Extend = append array, no code change. + `CATALOG_COUNTRIES`, `regionsForCountry()`.
  - `src/utils/starterDishes.ts` (pure) — `normalizeName`, `matchCatalogDish` (type-to-match enrichment; unknown→null, never gates logging), `suggestCatalogDishes` (autocomplete), `pickStarterDishes({countries,regions,diet,limit})` (weighted, category-balanced, veg-filter, deduped, deterministic), `toSeedDish` (region→categoryTags[], timesCooked:0, NO migration).
  - `src/utils/__tests__/starterDishes.test.ts` — 13 tests (match/suggest/pick balance+veg+region+determinism/seed).
- **Next Up (Phase 1 remaining):** (1) reusable curated **pick-list component** (pre-ticked pills, ~20+show-more, collapsible groups+add-all, counter, DishSuggestInput, "just add these N"); (2) onboarding step in HouseholdSetupScreen (country→region multi→veg→pick-list→seed); (3) `addDishesBatch` (writeBatch) in firestore; (4) planner tweak — cap never-cooked `daysSinceLast=999` so real history wins; (5) wire `matchCatalogDish`/`suggestCatalogDishes` into AddMeal dish input. Then Phase 2 (ingredients+grocery). Native app → device test by Sameer. Android prod-access wait still open in parallel.

## Prior Session (2026-08-30 PART 3 — grocery model locked + new feature ideas, no code yet)
- Sameer refined grocery + added ideas. Overarching rule reaffirmed: **10-second meal logging WINS; grocery/recipe are additive, never mandatory.**
- **Grocery = ONE household checklist** (not per-dish): ingredients from chosen dishes MERGE + **dedupe** (case-insensitive + light singular/plural); pure checkboxes to tick while shopping; **Clear checked + Clear all/Reset**; **manual add** too (e.g. "dishwashing pods") so it works as a normal list. Populated by user choice (from Plan "add this week's ingredients" or dish detail "add to grocery"), never auto.
- **Ingredients live on the DISH, not the meal** (this is what keeps logging 10s). Model (all schemaless, NO migration): `Dish += ingredients?: string[]`, `Dish += recipe?: {type:'youtube'|'url'|'text', value}`; `households/{id}/grocery/{itemId}={text,checked,source:'dish'|'manual',dishId?,createdAt}`. Dish-detail sheet = tap dish → ingredients (edit/add) + "Add to grocery"; recipe added later.
- **Curated pick-list = not overwhelming:** tap-toggle CHIPS/pills (pre-ticked), show ~20 + "show more" expander, light collapsible groups (mains/sides/breakfast/special) w/ add-all, running counter, type-your-own via DishSuggestInput, + a "just add these N for me" button (the auto-50 escape hatch) INSIDE the flow.
- **Catalog + matching:** author a large structured global catalog (deep in India regions + ~15 world cuisines w/ ingredients; broad elsewhere); typing a dish autocompletes/matches → auto-fills cuisine/region/diet/ingredients; unknown name still logs instantly (never gated). Honest: not literally "every dish on Earth" — share feature grows the long tail.
- **NEW — dish-pack SHARE (approved idea):** Household A "Share my dishes" → 6-char `dishPacks/{code}` (dish DEFINITIONS only — names/cuisine/region/diet/ingredients/recipe; NEVER meals/ratings/spend) → Household B imports via same pick-list UI. Privacy-safe, growth loop. **KEY REUSE:** one pick-list component powers onboarding seed + "add cuisine later" + pack import.
- **NEW ideas floated (on-brand, sub-10s):** "Cook again" 1-tap re-log; "Decide for me" instant single suggestion; home-screen widget ("Tonight: X"); leftovers 1-tap; recipe-on-dish. HOLD: nutrition/macros, aisles/quantities, community feed (friction traps).
- **Next Up:** Sameer OK'd curated list. Build order: **Starter Catalog + reusable pick-list + type-to-match + Phase-1 content** FIRST (show working before grocery) → dish ingredients + grocery list → recipe-on-dish → share pack → cook-again/widget. Awaiting his go to start the catalog. Android prod-access wait still open in parallel.

## Prior Session (2026-08-30 PART 2 — SCOPED top gap features, no code yet)
- Sameer wants a global, region-deep starter-dish catalog + new-user cold-start seeding ("blank state → 50 dishes"). Scoped 3 features against real code.
- **Grounded in code:** Dish docs are standalone (`cuisineTag`,`timesCooked`,`lastCookedDate`,`categoryTags[]`); `addDish` exists; planner scores never-cooked dishes `daysSinceLast=999` → they get picked FIRST (key gotcha).
- **My pushback (agreed direction):** (1) do NOT silently inject 50 — use a curated, editable PICK-LIST (protects "learns what YOU cook" moat + dodges the 999 planner flood); (2) multi-select regions not one; (3) veg preference = hard filter (uses existing `diet`/`utils/diet.ts`); (4) balanced 50 by category (mains/sides/breads/rice/breakfast/snack), not 50 mains.
- **Scope 1 — Starter Catalog + cold-start:** bundled static `src/data/starterDishes.ts` (Continent→Country→Region→dish{name,cuisineTag BROAD,region,diet,category,weight}); region rides in `categoryTags[]` = NO migration; `cuisineTag` stays broad so Insights/variety don't fragment. New onboarding step in HouseholdSetupScreen (country→region multi→veg pref→pick-list). New `utils/starterPick.ts` (pure, tested weighted blend). `addDishesBatch` (writeBatch). Planner tweak: cap never-cooked score so real history wins later. Content phased: P1=India 6–8 regions ~120–150 + ~12 global cuisines; more = JSON-only later. Code ≈1–1.5d; content = incremental.
- **Scope 2 — Grocery list:** `households/{id}/shopping/{itemId}`={text,checked,addedBy,createdAt}, `useShoppingStore` (cache-first), tab/card + swipe-check. ~0.5–1d.
- **Scope 3 — Per-dish notes+photo:** extend Dish `notes?`,`photoUrl?` (schemaless), dish detail sheet, reuse profile-photo upload. ~1d.
- **Next Up:** awaiting 2 decisions — (a) curated pick-list [recommended] vs auto-seed-50 (can add a "just add 50" button inside the list); (b) build order [recommended: Catalog → grocery → notes/photo]. Then build Scope 1 incl. authoring Phase-1 India+global catalog. Optionally write a `docs/` spec first. Android prod-access wait (below) still open in parallel.

## Prior Session (2026-08-30 — COMPETITIVE / FEATURE-GAP ANALYSIS, no code)
- Sameer asked for a thorough market/feature analysis: what competitors have that Sofra lacks, and what Sofra has that they don't. Grounded Sofra's side in actual code (types/screens/services/README/MVP2); competitor grid from model knowledge (verify live before any pitch use).
- **Framing:** Sofra is NOT a recipe app — it's a home-cook rotation engine + food diary + dine-out tracker. Category is separate from Mealime/Paprika/Plan-to-Eat/Samsung Food/AnyList/eMeals/Cozi.
- **Must-close gaps (positioning-safe):** (1) lightweight shared grocery/shopping list, (2) per-dish notes/"how I make it" + photo, (3) meal/dish photos, (4) manual plan edit (drag/swap/lock — verify PlanScreen), (5) cold-start starter dishes, (6) home-screen widget ("tonight's dinner").
- **Optionals (audience-dependent):** nutrition/diet filters, grocery delivery integ, web app. **Traps to AVOID:** full recipe DB/import, community feed, cooking mode — they dissolve the moat.
- **Moat (keep leaning in):** rotation-from-your-history, dine-out+spend+budget, thali multi-dish, kids tiffin, veg/nonveg multilingual detect, food-life analytics, zero-activation (names not recipes).
- **Market read:** primary = South-Asian/diaspora households + established home cooks w/ decision fatigue + families tracking eating-out spend. NOT recipe-discovery or macro-tracking crowds.
- **Next Up (if pursued):** live web-verify competitor grid; optionally write `docs/COMPETITIVE_ANALYSIS.md` or an artifact one-pager; then scope the grocery-list + per-dish-notes/photo features. No code changed this session.

## Prior Session (2026-08-27 — ANDROID: APPLIED FOR PRODUCTION ACCESS ✅)
- **✅ 12-tester/14-day gate CLEARED.** Dashboard → Production showed all 3 criteria checked and "Apply for production" enabled. Via Chrome (Sameer logged in, dev acct `8107930340888208101`, app `4972790754924510458`).
- **✅ SUBMITTED the production-access application** (3-step form, all 8 fields, full best-in-class answers grounded in real closed-test history). Confirmation: "We have your application for production access… We'll email the account owner… usually ≤7 days. Applied today, 15:43." Answers given: recruited friends/family (no paid provider); recruit difficulty = "Neither difficult nor easy"; engagement = real target-user usage of all features; feedback = via messages/calls, no crashes, fixes = Samsung nav-bar overlap + cost-field keyboard + veg/nonveg + share card + reminders; audience = adults who cook for households (18+); value = "what's for dinner" planner; first-year installs = **0–10K**; changes = the v1.1.0 fix bundle; ready = minor issues fixed, no crashes, device-verified, tests pass, iOS already live.
- **✅ VERSION VERIFIED (no 1.0.0 risk).** Latest app bundles = ONLY vc4=1.1.0 (Active, newest) + vc3=1.0.1 (Inactive). No 1.0.0 Android build exists (Android debuted at 1.0.1). Play requires ascending versionCode → prod release WILL be vc4/1.1.0. Store name now live as "Sofra: Family Meal Planner".
- **✅ IN-APP ANDROID LINK confirmed permanent:** `play.google.com/store/apps/details?id=com.thaliplan.app` (package-based, `com.thaliplan.app` never changes). In [src/config/links.ts](../src/config/links.ts). Won't change; activates when prod goes live.
- **✅ WEBSITE UPDATED + PUSHED to main** (`d762a8d` → Pages redeploy): added **veg/non-veg** feature card to `docs/index.html` (veg-mark icon) + `docs/features.html` (insight card); refreshed features.html reminders card → "Daily reminders & streak" (log-today + streak, matches v1.1.0). iOS store button stays LIVE; **Android button kept "Coming soon"** — verified the public Play link 404s until production rolls out. index.html TODO comment now carries the exact permanent Play URL for a one-line flip at launch.
- **⛔ NEXT (Android):** wait for Google's production-access email (≤7 days). When granted → Production → Create release → **pick versionCode 4 (1.1.0)** from library → set countries (all 177) + release notes → review & roll out (consider staged %). Then flip the website Play button live (href + drop aria-disabled + relabel, per the TODO comment in index.html) — `PLAY_STORE_LIVE` in the app is already true. Production track currently: "You don't have access to production yet" (gated on the application).
- **📋 TECH DEBT for NEXT BUILD (vc5 / ~v1.1.1) — NOT now (don't disturb the clean 1.1.0 prod push).** Three non-blocking items to ship together in one build: (1) **Edge-to-edge deprecated APIs (Android 15):** [App.tsx:95](../App.tsx) `<StatusBar backgroundColor translucent={false}>` calls deprecated `setStatusBarColor`; on Expo SDK 57 edge-to-edge is always-on so these props are already no-ops → fix = drop `backgroundColor`+`translucent`, keep `barStyle`; app already uses safe-area insets. (2) **Orientation/resizability (Android 16):** [app.json:6](../app.json) `"orientation":"portrait"` → large screens ignore it from A16; fix = set `"orientation":"default"` + verify landscape/tablet layout (iOS already supportsTablet). (3) **Branded store links (hide `com.thaliplan.app`):** package id is immutable (can't change on existing app) → instead add `sofra.savvylabs.dev/android` (+`/ios`,`/get`) redirect pages on the website AND point `PLAY_STORE_URL`/`APP_STORE_URL` in [src/config/links.ts](../src/config/links.ts) at those branded URLs so in-app share uses them. Android branded link only after prod is public. All 3 = ~20min code + 1 device test. Requires new EAS build.
- **⏰ SCHEDULED CLOUD ROUTINE created 2026-08-27, fires once 2026-09-07 14:00 UTC (9am Chicago):** `trig_01W83sbEgcxn5rZNnCqNyFFS` (https://claude.ai/code/routines/trig_01W83sbEgcxn5rZNnCqNyFFS). A cloud agent will PREP v1.1.1 on branch `v1.1.1-android-compliance` — apply the 3 fixes above (edge-to-edge, orientation, branded links + `docs/{ios,android,get}.html` redirects), bump to 1.1.1, run tsc+jest, and OPEN A PR. It is PREP-ONLY: it does NOT build, EAS, release, or touch Play Console (no creds in cloud + native app needs Sameer's device test). Store push stays manual: review PR → device-test → EAS build → Play release. Edit/disable at the routines URL.

## Prior Session (2026-08-26 PART 2 — v1.1.0 SHIPPED: iOS submitted to App Store + ASO metadata refresh both stores)
- **Round-4 fixes committed + merged to `main`** (`177f090`, via `release-1.1.0`) + pushed. Version bumped 1.0.1 → **1.1.0** in app.json (appVersionSource=remote, autoIncrement handles buildNumber/versionCode). Marketing video (~90MB) gitignored (`branding/Video/`, `branding/assets/video/`, `*.mp4`) — do NOT commit video to this repo.
- **EAS production builds** (both, non-interactive, credentials already on EAS — no 2FA): iOS build **#7** (id 9056d011) + Android **versionCode 4** AAB (id 2085f8a9). Both finished.
- **✅ iOS 1.1.0 SUBMITTED FOR APP STORE REVIEW** (up to 48h). Via Chrome (Sameer logged in, 2FA). Created v1.1.0 in ASC, attached build 7, submitted. `eas submit -p ios` uploaded the binary (ASC API key on EAS servers). Apple Developer License Agreement banner was present but did NOT block submission.
- **ASO metadata refresh (broad family-meal positioning, search-optimized) — Sameer's picks: title "Sofra: Family Meal Planner" both stores, broad positioning, TEXT metadata only (keep screenshots):**
  - **iOS (saved, in the 1.1.0 submission):** Name already `Sofra: Family Meal Planner` (26/30). Subtitle `Plan meals, remember it all` → **`Plan weekly dinners & tiffins`** (29/30). Keywords `dinner,recipe,cooking,menu,weekly,tiffin,grocery,food diary,household,kids,eat,mealprep` → **`dinner,cook,menu,weekly,mealprep,lunch,tiffin,thali,takeout,dineout,household,kids,leftovers,cuisine`** (100/100 — dropped recipe/grocery[2.3.7 risk]/food-diary/eat, added takeout,dineout,thali,leftovers,cuisine,lunch). Promo Text + What's New refreshed. Description/screenshots kept.
  - **Android (STAGED "ready to send for review", NOT yet sent):** App name `Sofra` → **`Sofra: Family Meal Planner`** (26/30). Short desc → **`Weekly meal planner for families: dinners, tiffins, takeout & what to cook`** (74/80). Full description + screenshots + icon kept.
- **Android AAB vc4 pushed to BOTH testing tracks** (eas submit default `internal/draft` was NOT enough — draft + wrong track). In Play Console: **Internal testing** release published (live to internal testers ~1h, no review). **Closed testing (Alpha, the 12-tester group)** new release created from vc4 (added from library) + release notes → sent for review; **the staged store-listing metadata change bundled INTO that closed-release review** (store listing page now shows plain "Live", no pending badge). **Production STILL GATED:** "12 testers opted in for **13 days**" — needs 14 → "Apply for production" unlocks **~2026-08-27**. Then Apply → Google review (≤7 days) → production release w/ vc4. NOTE: eas.json android submit is `track:internal, releaseStatus:draft` — to push testers directly next time, roll out in Play Console or change eas.json.
- **What's New (both, incremental tone so review sees a routine update):** veg/non-veg marks, logging streak + daily reminder, cleaner share card + polish, nav-bar/Plan fixes.
- **⛔ NEXT:** (1) iOS: watch email/Resolution Center for review result (≤48h); if approved, releases (consider enabling Phased Release in Version Release before it goes live). (2) Android ~tomorrow: Apply for production, then release vc4 + send listing for review. (3) Optional: push vc4 to the CLOSED track so testers get 1.1.0 (eas.json submit track is `internal`; change if desired).

## Prior Session (2026-08-26 — CLOSED-TEST FEEDBACK ROUND 4: all 4 items CODED; tsc=0, 69/69; device-tested)
- Closed testing came back clean (no major bugs). 4 small callouts, each root-caused + fixed:
  1. **Tabs under system buttons (Samsung) + "dark mode stops at a bar" = SAME bug, FIXED.** `AppNavigator.tsx` tabBar was hardcoded `height:60, paddingBottom:4`, no `insets.bottom` → bar underlapped 3-button nav; surface bg only painted 60px so the strip below showed through (read as dark-mode ending). Now `useSafeAreaInsets()` → `height:60+insets.bottom`, `paddingBottom:4+insets.bottom`. Pixel uses gesture nav so Sameer never saw it.
  2. **Cost accepts letters on some Android keyboards, FIXED.** `decimal-pad` is only a hint on Android (Samsung kbd/paste inject letters). `AddMealScreen` cost `onChangeText` now strips `[^0-9.]` + collapses extra dots (belt-and-suspenders with existing NaN parse guard).
  3+4. **"Fully planned" but blank mid-week days, FIXED (`PlanScreen`).** Root cause: `hasGaps` counts only today-onward empties but the grid shows the whole week incl. past days. Now: (a) subtitle "This week is fully planned" → "Upcoming days are all planned" (honest w/ past blanks); (b) past days (`day.date < todayStr`) render MUTED (opacity 0.5, surfaceVariant bg, elevation 0) with a grey "Past" pill — decided NOT to populate the past (fake data); logged past meals still show. (c) Soft nudge banner when `0 < allDishes.length < 3` ("add a few more home dishes for variety") — generation stays UNGATED (keeps first-run aha). Note: `allDishes` is home-meals-only, so a tester who logged only takeout hits the existing "No dishes yet" alert.
- **✅ APK BUILT + INSTALLED on Pixel** (`57150DLCH002E1`, `assembleRelease` 3m28s via C:\S, 94 MB, debug-signed, launched clean no FATAL). Synced the 3 changed files into C:\S first (it's a plain source copy, no .git). Round-4 build log: `C:\S\android\build-round4.log`.
- **⛔ NEXT:** Sameer device-tests all 4. Tab-bar inset is subtle on his Pixel (gesture nav = thin inset) — the dramatic case is the tester's Samsung 3-button nav. If good → commit on a branch + merge. Store builds via EAS. Android production still gated by Google's 12-tester/14-day unlock.

## Prior Session (2026-08-25 — FEEDBACK ROUND 3: all 5 items CODED + tested; APK build+install in progress)
- Sameer tried the released build, raised 5 items; scoped via 4 product-fork questions, then implemented. `tsc=0`, **69/69 tests** (was 56 — +13 new diet tests).
  1. **Tour (`TourOverlay.tsx`):** (a) pulsing **+** spotlight (ring + mock FAB + "Tap + to add a meal", card lifted) on the log step; (b) screen-name chip on the coach card (Home/Calendar/Plan/Insights/Settings); (c) Skip/Done navigate back to Home (was stranding on Settings).
  2. **Share (`ShareAppCard.tsx`):** dropped expo-sharing double-sheet → ONE `Share.share({url,message})` (PNG + tappable store links + code). Removed the leftover-text-share-after-cancel bug.
  3. **Streak:** new `StreakPill.tsx` (compact flame + "N-day streak" pill, taps to log) REPLACES `StreakCard.tsx` (deleted) in HomeScreen. Dropped the day-strip + gradient card per Sameer.
  4. **Family (`FamilyScreen.tsx`):** reworded to warm, non-blaming — "Other homes" / "Switch to another home" / "if your family uses more than one home code…nothing is lost." Behavior unchanged.
  5. **Veg/Non-veg (meal-level, default VEG — Sameer agreed):** `diet?:'veg'|'nonveg'` on `Meal` (schemaless, no migration). New `utils/diet.ts` (keyword detect, English + Hindi/Urdu: murgh/gosht/keema/macchi/jhinga/anda…, word-boundary safe: flags "Egg Curry" not "eggplant"). New `VegMark.tsx` (green/maroon standard mark) shown on MealCard (Home+Calendar) + History. AddMeal: Veg/Non-veg toggle auto-set from dish names (dietTouched stops auto-follow once flipped). Insights: family "Veg vs Non-veg" split card + veg/nonveg line on Kids-tiffin card. `mealDiet()` infers for old meals. Deferred: egg category + planner integration.
- **APK #1 built + installed + launched clean on Pixel** (`57150DLCH002E1`). Sameer tested → 4 tweaks:
  1. **Streak pill placement** — was marooned (20% width, rest blank). Moved onto the "This month" heading row, right-aligned (`HomeScreen` monthHeaderRow; removed pill's marginTop).
  2. **Veg/Non-veg toggle too tall** — now matches MealTypeToggle (paddingVertical sm, radius sm, surfaceVariant, selected=primary+white text).
  3. **Share card = image ONLY text on Android** (RN core Share drops images on Android). Sameer chose **react-native-share** (installed 12.3.1, native dep → rebuild). Refactored BOTH `ShareAppCard` + `ShareStatModal` to `RNShare.open({url:file://…, type:image/png, message, failOnCancel:false})` → card PNG + tappable store/site links in ONE sheet on **iOS AND Android**.
  4. **Veg share-stat showed only veg %** → now value `79% / 21%` (both, auto-shrink to fit) + sub `Veg N · Non-veg M · ▲/▼ pts vs last period` (new prevVegPercent trend in `dietSplit`).
- Also fixed a 100%-nonveg ratio-bar corner. `tsc=0`, **69/69**.
- **APK #2 (with react-native-share) built + installed + launched clean.** Sameer tested → 2 more tweaks (APK #3 building, bg `b5utpkr8x`, JS-only so native cached ~fast):
  - **Veg vs Non-veg card was clumsy** — dropped the `(41)/(11)` counts from chips (now clean `Veg 79%` / `Non-veg 21%`) + added a trend line "Last period was N% veg" (mirrors Home-vs-Outside). `InsightsScreen`.
  - **Streak pill floated high** on the "This month" row → row now `alignItems:flex-end` (baseline-aligns pill to title). `HomeScreen`.
- OPEN for Sameer's eye: veg bar uses standard bright green/red (recognizability) sitting under the sage Home-vs-Outside bar — offered to harmonize the green toward brand sage if the two stacked greens still clash.
- `tsc=0`, **69/69**. react-native-share works iOS+Android (same `RNShare.open` path); iOS only verifiable via EAS/TestFlight, not the connected Android phone.
- **⛔ NEXT:** install APK #3 → Sameer confirms veg card + streak alignment + share on device. Then commit+merge (branch first). Store builds via EAS, never this debug APK. iOS 1.1.0 + Android closed-track push still pending; Android production gated by Google's 12-tester/14-day unlock.

## Prior Session (2026-08-24 — first-user retention feedback: 4 UX fixes shipped in code, reminder strategy pending)
- Context: iOS live, links sent to family/friends. Real churn signal — people add meals for 2–3 days then drop off (discipline). Sameer raised 5 items.
- **✅ Item 2 (autofocus):** tapping "Add another dish" now focuses the new row. `AddMealScreen` sets `focusSideIdx`/`focusItemIdx` before appending; new row mounts with `autoFocus`.
- **✅ Item 3 (suggestions on additional dishes):** new `src/components/DishSuggestInput.tsx` (compact as-you-type dish suggestions, inline dropdown). Now used for home "more dishes" rows AND restaurant "dishes ordered" rows — parity with main dish's DishPicker.
- **✅ Item 4 (restaurant time window):** `RestaurantDetailScreen` had all-time-only stats (misleading — "2 visits/$100" could span years). Per Sameer: detail now **inherits the range selected on the Restaurants list** (This month / Last month / Last 3 months / All) via a `range` nav param + shared `RestaurantRange` type; in-screen selector lets you change it. Ranges/getRange mirror `RestaurantScreen` exactly.
- **✅ Item 5 (share links):** `src/config/links.ts` centralizes APP_STORE_URL, PLAY_STORE_URL (package-based, resolves at Android launch), + `PLAY_STORE_LIVE` flag. `FamilyScreen` invite now shares family code + direct store links (was website); Android line = "coming soon" until `PLAY_STORE_LIVE=true` (one-flag flip, no other change). ShareStatModal CTA still shows website (image card, links not tappable) — left as-is.
- **✅ Item 1 (retention loop) — Sameer chose FULL LOOP.** (a) Daily reminder now **default-ON at onboarding** (opt-out) — `useHouseholdStore` create/join calls `enableDailyReminderDefault()` → `setDaily(true)` (asks OS permission; denial leaves it off). Returning users unaffected (only create/join paths). (b) **Copy reframed from "plan tomorrow" → "log today"** (`notifications.ts` title "Log today's meals"; Settings sub; HomeScreen dynamic body). (c) **Logging streak** = new `src/utils/streak.ts` (+6 tests) with grace-day logic; Home shows a streak card (flame + "N-day streak" / "Log today's meals" nudge → taps to Add Meal); reminder body is streak-aware. NOTE on "skip nudge if logged": kept the daily reminder always-firing (a repeating local notif can't be conditionally skipped without breaking re-engagement for people who stopped opening the app) — instead the copy turns AFFIRMING when logged ("logged today — log again tomorrow"), so it never nags. Explained to Sameer.
- **✅ Switch household (Sameer's "switch profile"):** clarified = one active household at a time, switch by entering another invite code in Settings (not back-and-forth). `useHouseholdStore.switchHousehold(code,userId)` reuses `joinHousehold` (arrayUnion + moves user.householdId) + clears meal/dish caches + reloads. Settings "Switch household" row → code-entry modal. Preferences are per-USER so budget/currency/dining survive a switch.
- **✅ First-run product demo = interactive coach-marks (Sameer's pick).** New `src/stores/useTourStore.ts` (persists `tourSeen_v1`) + `src/components/TourOverlay.tsx` (dim + computed spotlight ring on bottom tabs / Home FAB + coach card; Skip/Back/Next; 7 steps: Home→log meal(+kids)→Calendar→Plan→Insights→Restaurants→Settings). Drives REAL tabs via nested `Main` nav; rendered in `MainTabs` (sibling of Tab.Navigator). Auto-starts once after onboarding (HomeScreen effect, guarded by `seen`); replay via Settings "See product tour". Hydrated in App.tsx.
- **Health:** `tsc=0`, **56/56 tests pass**. Native RN app — NOT browser-verifiable; **needs on-device test by Sameer.**
- **✅ Android RELEASE APK (local, NOT EAS):** rebuilt via short-path `C:\S` (New Arch MAX_PATH). Gotchas fixed this session: (a) `cmd /c gradlew.bat` fails — `NoDefaultCurrentDirectoryInExePath=1` on this box → must use `.\gradlew.bat`; (b) Gradle JVM SSL to Maven failed on fresh cache (`PKIX...trust anchors`) → appended `systemProp.javax.net.ssl.trustStoreType=WINDOWS-ROOT` to **C:\S's gradle.properties ONLY** (Windows-only; must NOT commit → breaks mac/EAS). Signs w/ debug keystore (5E:8F in Firebase). APK → `sofra-release.apk` at repo root.
- **✅ APK BUILT + INSTALLED on Pixel 10 Pro.** Incremental rebuild (2m26s, native cached) → `sofra-release.apk` (94 MB, debug-signed). Uninstalled old Play-signed split-APK build, installed new, launched clean (pid, no FATAL). `*.apk`/`*.aab` now gitignored.
- **✅ COMMITTED + MERGED to `main`** (`c363341`, via branch `retention-onboarding-switch`) + pushed. Also added `docs/release-playbook.md` (Apple + Android launch steps, verified vs current store rules).
- **Website Android link:** NOT updated — correct. Android app is in CLOSED TESTING, not production, so there is NO public Play link yet (`details?id=com.thaliplan.app` 404s for non-testers). Website already shows iOS live button + "Coming soon on Google Play". At Android production launch: flip `PLAY_STORE_LIVE=true` (app) + update Play button in `docs/index.html` (TODO present).
- **✅ FEEDBACK ROUND 2 (2026-08-25) — 6 refinements, all done (tsc=0, 56/56):**
  1. **Tour redesign:** dropped the bottom spotlight cutout+ring (looked bad against the card). Now a clean coach card (bottom-sheet, icon badge, dots, Skip/Back/Next) over a soft scrim; still navigates the REAL screens. `TourOverlay.tsx` rewritten.
  2. **Share = branded CARD (not plain text):** new `src/components/ShareAppCard.tsx` — captureRef PNG (Sofra brand + tagline + big family code + App Store & Google Play) shared via expo-sharing, with a message carrying BOTH tappable store links + code.
  3. **Google Play link included now:** `PLAY_STORE_LIVE=true` (in-app sharing only; website stays "coming soon"). Avoids a rebuild when Android goes public.
  4. **Switch/join family moved OUT of Settings → Family tab** (`FamilyScreen`): shows family name + invite code, "Share invite & app" (card), and "Switch or join a family" (code modal → switchHousehold). Removed the Settings Household section.
  5. **"See product tour" kept separate** in Settings under its own "Guide" section.
  6. **Fancier streak card:** new `src/components/StreakCard.tsx` — SVG gradient (primary→takeout), flame badge, big count, 7-day activity strip, "Log" CTA when not logged today. Replaces the plain inline card.
  7. **Notifications truly default-ON:** `DEFAULTS.daily=true` + new `assertDaily()` called from Home (prompts OS permission contextually, not at login). Fixes "why did it ask me to enable it" — the create/join-only enable missed reinstalls into an existing household.
- **⛔ NEXT:** (1) rebuild+install refined APK on Pixel (in progress), Sameer device-tests. (2) Commit + merge round-2. (3) Android: after 14-day closed test → Apply for production (playbook). (4) Store releases via EAS + version bump (never the debug APK). (5) EAS iOS 1.1.0 later. (6) ASO title open.

## Prior Session (2026-08-18 — iOS APPROVED + website wired to App Store ✅)
- **Apple approved Sofra — app is now LIVE on the App Store.** Public link `apps.apple.com/app/id6797710095` resolves.
- **Website updated to reflect the launch** (`docs/index.html`, `#get` section): iOS button now links to `https://apps.apple.com/app/id6797710095` (opens new tab), label **"Download on the App Store"** — was a disabled "Coming soon" placeholder. Pill → "Now on iPhone · Android soon"; lead copy updated. Google Play button intentionally left as "Coming soon" (Android not public yet). All "Get the app" nav/hero links route to `#get`, so they now reach the live button.
- **✅ COMMITTED + PUSHED** to `main` (`af56114`) → GitHub Pages redeploying sofra.savvylabs.dev.
- **ASO metadata review (advisory, no changes made yet):** reviewed a Claude-chat ASO suggestion for App Store. Verdict: Promotional Text + Description = good, paste ~as-is. Keyword field needs fixes — `whattocook` is a dead fused token (→`cook`), drop `grocery` (no such feature, 2.3.7 risk) and `recipe` (contradicts positioning); revised 96-char string given: `dinner,cook,tiffin,thali,takeout,dineout,household,weekly,kids,leftovers,mealprep,cuisine,memory`. **Biggest lever flagged: title is pure brand "Sofra" — recommend keyword-rich `Sofra: Family Meal Planner` (26 chars); description is NOT indexed on Apple.** Mechanics: only Promotional Text updates live/no-review; subtitle/keywords/title/description need a metadata-only version submission.
- **⛔ NEXT:** (1) Sameer to pick title direction (keyword-rich vs pure brand) → then hand paste-ready set and/or drive ASC via Chrome to submit. (2) When Android goes live, wire the Play button (TODO in `docs/index.html`). (3) Launch messages — Apple line now safe to send (previously 404'd).

## Prior Session (2026-08-15 — Apple 2.1 rejection RESOLVED + RESUBMITTED ✅)
- **Rejection = Guideline 2.1 Information Needed / 2.1.0 App Completeness** (NOT a bug — Apple wanted a documentation package for the new app). Reviewer HAD accessed the app. 7 asks: (1) screen recording, (2) devices/OS tested, (3) function+audience, (4) setup+demo login, (5) external services, (6) regional differences, (7) regulated-industry/3rd-party.
- **DONE via Chrome (Sameer logged in, then went to bed authorizing autonomous resubmit):** (a) Filled ASC App Review Info **Notes** (~2760 chars) covering items 2-7 + walkthrough; corrected 2 facts from app.json — app declares **camera** usage (NSCameraUsageDescription) not just photo library, and **supportsTablet=true** (iPhone+iPad). (b) Posted a point-by-point **Reply to App Review** (all 7 items; demo login testuser-apple@testuser.com / same as pw). (c) Resubmitted: version page **Update Review** (Rejected→Ready for Review) → **Resubmit to App Review** → now **🟡 Waiting for Review**. Submission ID 04026622-…8481c0.
- **⚠️ ONE GAP:** could NOT provide item 1's actual **screen recording** (must be captured on Sameer's physical iPhone). Reply says "screen recording available on request" + gives full text walkthrough. If Apple insists on the video → Sameer records ~30-60s on iPhone, ATTACH TO CHAT, Claude attaches to a reply + resubmits.
- **⛔ NEXT:** watch email + Resolution Center for reviewer response. If they re-reject asking for the video, get the recording from Sameer. If approved → auto-releases (still set to Automatically release). Do NOT cancel/resubmit blindly.

## Prior Session (2026-08-10 — VERIFIED Apple submission via Chrome; everything correct, 6 days queue; inquiry drafted but Apple form won't render)
- **FULL ASC AUDIT (Chrome, Sameer logged in) — nothing missing, submission is 100% correct:** Version badge = 🟡 **1.0.1 Waiting for Review** (correctly submitted, NOT stuck in "Prepare for Submission"). Build **6 (1.0.1)** attached. Screenshots present (6 iPhone 6.5" incl. hero first + iPad). Metadata complete (desc, keywords, support/marketing URL = sofra.savvylabs.dev, copyright). **App Review Info: Sign-in required ✓, demo acct `testuser-apple@testuser.com` (user+pass), contact info filled.** Agreements: **Free Apps Agreement = Active** (Aug 3 2026–Aug 3 2027) — correct for a free app; Paid Apps = unsigned/"New" = harmless (no IAP). No Apple messages, no Resolution Center thread.
- **The wait is the only finding:** App Review submissions log shows active submission **Date Submitted = Aug 4, 2026 at 3:58 PM** (Submission ID 04026622-…8481c0) → **~6 days in Waiting for Review** (today Aug 10). Two earlier same-line submissions show **Removed** (Aug 3 11:29 PM first attempt; Aug 4 3:30 PM pre-hero) — these cancel+resubmits each reset the queue = likely part of why it's slow. **Told Sameer: do NOT cancel/resubmit again.**
- **Sameer chose a NON-expedite status inquiry** (declined expedite). Found the correct channel: developer.apple.com/contact → **App Review → "App Review Status" → Email** (subtopic 30020, solution CONTACT.EML.GEN). NOTE: the App-Review contact form (`/contact/app-store/?topic=expedite`) ONLY submits expedite (app name+platform+Send, no message box); all other dropdown options redirect to help articles — so status inquiry must go via the Developer Support tree.
- **⚠️ BLOCKED sending it:** Apple's developer.apple.com contact SPA rendered fine initially then degraded to permanent blank/skeleton (empty a11y tree, dark screenshots) across reloads + a fresh tab + base /contact/ page — form fields never hydrate. Could not fill/submit. **Status-inquiry message is DRAFTED** (Apple ID 6797710095, v1.0.1 build 6, waiting since Aug 4, demo acct noted, Sign in with Apple mentioned) and given to Sameer to paste manually, OR retry automation later when Apple's form behaves.
- **⛔ NEXT:** (1) Sameer paste the drafted inquiry at developer.apple.com/contact → App Review → App Review Status → Email (or ask Claude to retry the automated send). (2) Otherwise just wait — 6 days still within extended-normal for a first review; watch email + Resolution Center. Honest expectation: inquiry likely returns a canned "review times vary, please wait." Do NOT cancel/resubmit.

## Prior Session (2026-08-07 — Apple review status Q + APP-FLOW DEMO VIDEO built)
- **✅ APP-FLOW DEMO VIDEO** → `branding/assets/video/final/sofra_demo_western.mp4` (32.4s, 1080×1920, ~5.5MB, h264+silent aac). Real screens only (no hallucinated UI), branded phone frame on warm-paper gradient + Fraunces "Sofra." wordmark + Inter captions + Ken-Burns zoom + 0.6s xfades, ending HOLDING on brand end-card (App Store/Google Play pills + sofra.savvylabs.dev). Flow: home→addmeal→plan→restaurant→insights→celebration→endcard. Generator rebuilt fresh (scratchpad ephemeral): `scratchpad/vidkit/build-demo.js` (Chrome-headless composite of base64 screen PNG + embedded Fraunces600/Inter500 → ffmpeg-static zoompan `veryfast` + xfade chain w/ cumulative offsets). Frame-verified (t2/t9/t25/t31). Run `node build-demo.js indian` for the Indian-screens cut.
- **⚠️ NOT a spec-compliant Apple App Preview:** Apple caps App Previews at 30s (this=32s) + wants real on-device capture at exact device res (886×1920 / 1290×2796). This composite = great DEMO/marketing (reviewer context, website, socials, messaging) but can't go in the listing gallery as an official App Preview. True App Preview = separate effort (screen-rec the running app on device).
- **DEMO-VIDEO OPTIONS offered (awaiting Sameer):** Indian cut (`sofra_demo_indian.mp4`), 30s social trim, caption/order tweaks.
- **⚠️ Sameer: the slideshow ≠ a demo.** Wants a real tap-driven demo (finger taps a control → screen responds). Built `scratchpad/vidkit/build-interactive.js`: animated touch indicator + tap ripple drives REAL navigation (tap ＋→Add Meal pushes in; tap Dinner toggle; back chevron→slides out; tab taps cross-fade Home→Plan→Insights→Calendar→Home) → holds on brand card. ~23s, 1080×1920@30. Deterministic `SEEK(t)` timeline in HTML (base64 screens+Fraunces/Inter), captured frame-by-frame via puppeteer-core + system Chrome (~1s/frame, 690 frames ≈ 11min) → ffmpeg. **LIMITATION:** single-screen mockups can't show in-screen state changes (field fill, star light-up); honest "what happens" = navigation only. Richer per-tap results (filled dish, new meal on home) would need NEW edited screen-state HTMLs. Render was in-flight at window end → verify frames + deliver next.
- **Apple review status Q:** ASC shows **"Waiting for Review"** (in queue, reviewer not started — all 48h is queue time, zero review time). Advised: do NOT edit ASC (editing a Waiting-for-Review version = back of queue); Firebase shows **Apple-ID sign-ins appearing** → reviewer likely already testing via Sign in with Apple (auto-verified, bypasses the email-verify gate) → sit tight. Email-verify gate only blocks raw email/password accounts; fake demo domain `testuser-apple@testuser.com` can't receive the link → if reviewer needs creds they'll ask in Resolution Center (reply keeps queue place). Fix-when-asked = real inbox (Gmail +addressing) or "use Sign in with Apple" note. If still Waiting at ~72h → Contact Us → App Review.
- Apple public link `apps.apple.com/app/id6797710095` **404s** → app NOT "Ready for Sale" yet. Don't send the Apple line in launch messages until ASC shows Ready for Sale.
- **⛔ NEXT:** (1) let review ride (Waiting → In Review on its own); watch email + Resolution Center + Firebase sign-ins; at ~72h Waiting → Contact Us → App Review. (2) Demo video: decide Indian cut / 30s social trim / caption tweaks.

## Prior Session (2026-08-06 — LAUNCH-VIDEO STRATEGY REALIGN, collab/no-build)
- **Sameer's call (agreed):** the 3 "family-at-table" brand commercials don't communicate what Sofra *is* or which problem it solves → wrong tool for launch. Launch video must be **demo-led** (problem → app in action → outcome), not a TV-style brand film. Rationale: at launch nobody knows the product; also few people open the texted website link, so the video must carry the whole message *self-contained + sound-off*.
- **Agreed structure:** ~30–45s master = (1) universal pain (the 6pm "what's for dinner" scramble), (2) real screen-recording of Sofra's ONE hero moment, (3) calm outcome + clear "Download Sofra" CTA. The existing family-dinner footage is *repositioned* as the closing payoff, not scrapped.
- **Channels (Sameer):** all, but priority = friends/family first, then Instagram. Both want the SAME master, cut two ways: friends/family = personal opener + big captions, sound-off; Instagram = scroll-stopping first 2s, 9:16, baked captions.
- **⛔ BLOCKER before scripting:** need Sameer's ONE-sentence "hero moment" — the single thing Sofra does that makes someone say "I need that" (likely the weekly auto-planner / instant "what's for dinner" answer, possibly cultural/thali angle). Not scripting off a guess.
- **✅ INDIAN-DATA SCREEN SET BUILT** (for the Indian-family video cut — real UI, only data swapped, zero hallucination). New in `branding/assets/screens/`: `home_in` (Sharma Family, ₹3,200, Aloo Gobi/Dal Tadka/Aloo Paratha), `plan_in` (Rajma Chawal, Paneer Butter Masala, Poha, Chole — **no beef**), `restaurant_in` (**Sagar Ratna** — Masala Dosa 3× ★★★★★, Idli Sambar, ₹640/visit = the memory-proof screen), `insights_in` (North/South Indian/Chinese/Continental cuisine mix, Aloo Paratha tiffin, Home-vs-Outside). Each = editable `.html` + rendered `.png` (Chrome headless @1280×2706, `--virtual-time-budget=6000`). Visually verified: ₹ renders, Fraunces+Inter load, layout intact. Website gallery + Western files UNTOUCHED (video-only assets).
- **Video plan agreed:** demo-led master (pain → one-tap plan → "because it remembers" via restaurant+insights → family payoff + CTA), cut 2 ways (Western `*.png` / Indian `*_in.png`), 2 family versions (Western English / Indian Hindi native-VO). Hard rule for any AI video tool: generate ONLY human/kitchen footage, NEVER draw app UI — composite the real PNGs in post.
- **✅ INDIAN SCREENS RELOCATED** for clean differentiation → `branding/assets/screens/indian/{home,plan,restaurant,insights}.{html,png}` (dropped `_in` suffix; folder differentiates). Western set stays in parent, untouched.
- **✅ LAUNCH-VIDEO PROMPT DOC WRITTEN** → `branding/prompts/launch-video-demo-led.md`: full demo-led master (4 beats), ready-to-paste AI-footage prompts for Beat 1 (pain) + Beat 4 (payoff) × Western & Indian families (+ negatives = no phone/UI/text), the HARD no-UI-hallucination rule, Beats 2&3 = real screen composite (screen-rec or animated PNGs), sound-off captions, EN + native-Hindi VO scripts (Devanagari + transliteration; no AI Hindi TTS), assembly/edit plan, end-card path (`branding/assets/end-cards/sofra_endcard_9x16.png`), deliverables checklist. App is FREE both stores; website sofra.savvylabs.dev.
- **✅ BEATS 2 & 3 FULLY RENDERED AS FINISHED VIDEO (Claude did it — Sameer builds nothing).** Sameer wants a hand-off (MD + images) → AI tool outputs final; the app-demo half is now pre-made so no tool is needed for it. Installed `ffmpeg-static` in scratchpad (no system install), built phone-framed 1080×1920 composites of the REAL screens (plan/restaurant/insights, per family) with baked captions + Sofra wordmark on warm-paper gradient, then zoompan slow-zoom + 0.4s xfade → **`branding/assets/video/clips/{western,indian}/app_demo.mp4`** (17.2s, h264 yuv420p, ~3 MB each). Stills also saved at `branding/assets/video/stills/<family>/*.png`. Generator: `scratchpad/vidkit/build-app-demo.js` (base64-embeds screen PNG into composite HTML → Chrome headless render → ffmpeg). **GOTCHA fixed:** `medium` preset made zoompan crawl (single-thread) — use `veryfast`; also Western `restaurant.png` was missing from `branding/assets/screens/` (only in docs/) → re-rendered from its HTML. Frame-verified: layout, ₹, stars, caption all correct.
- **✅ HAND-OFF DOC** → `branding/prompts/HANDOFF-launch-video.md`: single file to give the AI tool — says Beats 2&3 are DONE clips (drop in), tool only generates Beat 1 (pain) + Beat 4 (payoff) human footage (Western+Indian prompts + negatives) and stitches Beat1 → app_demo.mp4 → Beat4 → end card. Full detail in `branding/prompts/launch-video-demo-led.md`.
- **✅✅ FULL LAUNCH VIDEO COMPLETE — 100% self-made, NO external AI tool used.** Sameer chose the animated-fallback route. Built typographic/line-art bookends (Beat 1 pain + Beat 4 payoff) as branded posters → Chrome render → ffmpeg zoompan + cream fade-in, then assembled **Beat1 → app_demo.mp4 → Beat4 → BRAND END CARD** with 0.5s xfades. Generator: `scratchpad/vidkit/build-full.js`. Beat1 motifs: Western=fridge, Indian=stacked-tiffin line-art + Devanagari "आज खाने में क्या बनाऊँ?" (Mukta font) under English "What's for dinner?". Beat4=bowl/thali + "Dinner, sorted." Output: **`branding/assets/video/final/sofra_launch_{western,indian}.mp4`** (30.9s, 1080×1920, ~4.5 MB). Per Sameer's request the video ENDS HOLDING on the real brand card (`sofra_endcard_9x16.png`) — removed the end fade-out, hold 3.2s; verified true last frame = full-opacity brand card. Bookend stills at `branding/assets/video/stills/<fam>/b1.png,b4.png`.
- **✅ WESTERN CUT WITH REAL FOOTAGE MERGED.** Sameer generated real Beat1/Beat4 clips (Veo-style) → `~/Downloads/Woman_staring_inside_open_refrig…mp4` (pain) + `Family_sharing_meal_at_table…mp4` (payoff), both 720×1280/24fps/6s w/ audio. Script `scratchpad/vidkit/merge-western-real.js`: upscaled footage to 1080×1920@30fps (lanczos), added silent stereo to app_demo + endcard, assembled Beat1→app_demo→Beat4→BRAND CARD with 0.5s video xfade + audio acrossfade (offsets synced). Output **`branding/assets/video/final/sofra_launch_western_real.mp4`** (30.97s, 1080×1920, 30fps, stereo aac). Frame-verified: real opener, app section, real payoff, ends holding on `sofra_endcard_9x16.png`. NOTE: middle (app+endcard) is silent — beat ambient fades out/in around it; a music bed would unify. Indian cut still uses typographic bookends (`sofra_launch_indian.mp4`) until Sameer generates Indian footage.
- **✅ MUSIC BED ADDED (Western real cut).** Script `scratchpad/vidkit/add-music.js` synthesizes a royalty-free warm Cmaj9 ambient pad (6 sines low octave → amix → lowpass 2k + slow tremolo + aecho + fades + alimiter) and mixes it UNDER the footage ambient (music vol 1.25, amix normalize=0, alimiter 0.95). Output **`branding/assets/video/final/sofra_launch_western_real_music.mp4`** (30.97s, video stream copied, no re-encode). Level-verified: whole mean −28 dB / peak −3.9 dB (no clip); middle now max −20.8 dB (was −32, silent-feeling) → only ~6 dB gentle dip vs beats = unified. Non-music version kept as `sofra_launch_western_real.mp4`. Pad is synthesized (not a licensed track) — swap for a real track later if desired.
- **✅✅ INDIAN REAL-FOOTAGE CUT DONE (with music).** Sameer's Indian clips: `~/Downloads/Woman_deciding_what_to_cook_202608061255 (1).mp4` (Beat1, NATIVE 1080×1920 — authentic Indian kitchen: kurta, bindi, steel dabbas of dal, pressure cooker on gas) + `Family_sharing_dinner_at_table_202608061255.mp4` (Beat4, 720×1280 — roti/dal/sabzi in steel serveware). Script `scratchpad/vidkit/merge-indian-real.js` (used the 1080p "(1)" file, not the 720p dup) → `sofra_launch_indian_real.mp4` → `add-music.js` → **`branding/assets/video/final/sofra_launch_indian_real_music.mp4`** (30.97s, 1080×1920, peak −7 dB no clip, ends on brand card). BOTH launch cuts now final with real footage + bed: `sofra_launch_western_real_music.mp4` + `sofra_launch_indian_real_music.mp4`.
- **✅ LAUNCH MESSAGES DRAFTED (English + Marathi).** Per Sameer's 6-part strategy (excitement/first public launch → intro → why-Sofra 5 bullets → portal → Apple → beta link). Both delivered in chat as copy-paste blocks; Marathi in natural Devanagari. Links used: portal `https://sofra.savvylabs.dev`; internal-tester/beta `https://play.google.com/apps/internaltest/4701720489862037731`; Apple `https://apps.apple.com/app/id6797710095`. **⚠️ 2 CAVEATS FLAGGED:** (1) **Apple link 404s** (bare + /us/ both) via WebFetch → iOS app NOT public yet (in review / not released) — don't send Apple line until ASC shows "Ready for Sale". (2) Play **internal-test link only works for pre-added tester emails** (currently 2) — most recipients can't join → suggested "DM me for early Android access" or move to open testing. English pairs w/ Western commercial, Marathi w/ Indian.
- **⛔ NEXT (optional):** confirm Apple app live status; decide beta-access wording/open-testing; 1:1 feed + 15s IG-hook crops; optional Hindi message; swap synth pad for licensed track; native VO. `ffmpeg-static` lives in scratchpad (ephemeral) — re-`npm i ffmpeg-static` in `scratchpad/vidkit` to rebuild.

## Prior Session (2026-08-05 — GOOGLE SIGN-IN FIXED (wrong SHA-1) ✅ + closed test SUBMITTED for review ✅)
- **✅✅ GOOGLE SIGN-IN ROOT CAUSE = WRONG Play-signing SHA-1 in Firebase.** Pulled `adb logcat` from Sameer's device during a live failed sign-in: `GoogleSignatureVerifier: package info is not set correctly` + `Auth [GetTokenResponseHandler] Server returned error: This android application is not registered to use OAuth2.0 … SHA-1 mismatch`. Pulled the installed `base.apk` (`installer=com.android.vending` = genuine Play build) and ran `apksigner verify --print-certs`: **real Play App Signing cert = SHA-1 `41:C8:B0:C8:40:C6:71:9F:5D:C3:14:06:4D:A2:1E:76:B7:2F:C1:F9`, SHA-256 `3A:82…1A:AB`.** Firebase had the SHA-256 RIGHT (`3A:82…`) but the SHA-1 WRONG (`17:F7…` belongs to a different key — mis-copied from Play's new "quantum-ready" App-signing page which lists multiple keys). Google Sign-in legacy API matches on SHA-1 → rejected every install. **FIX: added `41:C8…C1:F9` to Firebase → Android app.** Firebase auto-created the matching OAuth client (`349329204088-mvhn…`, Android) within ~1 min. **Sameer confirmed sign-in WORKS on device.** Global fix (project-level, all Play-signed installs — internal/closed/open/prod all re-signed with `41:C8`).
- **✅ `google-services.json` REFRESHED in repo** (now all 5 Android OAuth clients incl. `41:C8`) — **committed `59acdbb` + MERGED to `main` (fast-forward) + PUSHED to origin.** `android-play-launch` now fully contained in `main`; PR #8 auto-closes.
- **✅ VIDEO PROMPTS delivered → `branding/prompts/video-prompts-studio.md`**: (A) app-walkthrough storyboard (VO script + scene map using generic `docs/assets/screens/*.png`); (B) 3 brand-commercial prompts, one arc localized 3 ways — American/English, N-Indian desi/Hindi, Maharashtrian/Marathi (in-language VO lines + tagline supers + negatives). KEY RULE captured: never let AI draw the app UI/logo/text — composite real screens in post; re-record Hindi/Marathi VO with native speakers.
- **✅ 2 MISSING walkthrough screens RENDERED** (Rivera-Family generic, 1280×2706, matching existing mockups): `restaurant.html`/`profile.html` in `branding/assets/screens/` → PNGs `docs/assets/screens/restaurant.png` (restaurant detail: dishes + star ratings) + `profile.png` (profile/settings: name/photo edit, household+invite code, appearance/notifications, sign-out/delete). Chrome-headless render pipeline: `chrome --headless=new --window-size=1280,2706 --virtual-time-budget=4000 --screenshot=<abs .png> file:///<abs .html>`.
- **✅ NEW `branding/` DIRECTORY** — consolidated all brand/marketing/commercial content (Sameer's request): `git mv` docs/video-prompts*.md + launch-teaser.md → `branding/prompts/`; `assets/brand/marketing/` → `branding/assets/`; logo SVGs → `branding/logo/`. Added `branding/README.md` index; sed-updated all doc path refs. SAFE: app icons live in `./assets/` (not assets/brand), website uses `docs/assets/` copies — nothing in build/site broke.
- **✅ VIDEO END-CARDS built** → `branding/assets/end-cards/sofra_endcard_9x16.png` + `_16x9.png` (Chrome-headless from HTML): centered Sofra tile+wordmark, tagline, TERRACOTTA "Free on the App Store / Google Play" pills, website. App is FREE on both stores. NOTE: pills are Sofra-styled placeholders — swap official Apple/Google badges for public store use.
- **✅ COMMERCIAL PRODUCTION KIT** (Sameer wants to batch-generate real-human 3-language ads, not prompt one-by-one): (1) `branding/prompts/commercials-scene-script.md` = LTX-Studio/Flow-ready scene script (3 commercials × 3 shots + static end-card, per-language VO/supers); (2) `branding/production/shots.json` = machine manifest; (3) `branding/production/generate_commercials.py` = Veo batch generator via `google-genai` (dry-run validated 9 shots; idempotent; `--only`/`--include-vo`; renders/ gitignored). Recommended tools = LTX Studio or Google Flow (Veo 3.1) for visuals; ElevenLabs/native artist for Hindi+Marathi VO (AI Marathi TTS unreliable); composite real app screen + end-card in post.
- **✅ CLOSED TESTING (Alpha) SUBMITTED FOR REVIEW.** The Alpha track had a draft release w/ NO bundle. Via Chrome: added AAB v3 (versionCode 3, 1.0.1) from library → release notes present → assigned tester list "sofra Test" (2 users) → track now ACTIVE → Publishing overview → **Submitted 14 changes for review** (closed-testing rollout + store listing + all app-content declarations + category). Now "Changes in review" (quick-checks ~6 min → Google review ≤7 days). This triggers the identity review that FIXES the temp name "com.thaliplan.app (unreviewed)" → "Sofra".
- **⛔ NEXT FOR SAMEER (production gate):** closed test now has only **2 testers**; Google requires **≥12 opted-in testers for 14 continuous days** before PRODUCTION. Add ≥10 more emails to the "sofra Test" list (Play Console → Closed testing → Testers) and have them opt in via the closed-test link. Review of the current submission proceeds regardless.

## Prior Session (2026-08-05 — Android AAB LIVE on internal testing ✅ + SHA-1 in Firebase ✅)
- **✅✅ AAB UPLOADED to Play internal testing** via `eas submit` (build `b221a8bb`, v1.0.1, versionCode 3). **Root cause of the 4 earlier failures = the service account had account-level "Release to testing tracks" but NOT app-level "Manage store presence"; EAS's submit touches the store-presence edit, so it 403'd.** FIX: Users&perms → SA → App permissions → Add app → Sofra → +Manage store presence → Save. Next submit succeeded instantly ("Submitted your app to Google Play Store! All done!"). NOT propagation after all.
- **✅ SHA-1 + SHA-256 (Play App Signing key) ADDED to Firebase** (thaliplan → Android com.thaliplan.app). New: SHA-1 `17:F7:87:3F:A2:40:47:E6:EF:49:FD:DF:E6:E5:23:E9:50:D5:F0:DB`, SHA-256 `3A:82:2E:74:70:5E:9C:9C:5B:82:1D:AA:87:2B:1B:AC:2E:84:37:05:40:B4:51:56:16:5E:86:D1:65:EC:1A:AB`. Now 4 SHA-1s + 1 SHA-256 total (debug 5E:8F, EAS upload EE:40, 2D:0B, Play-signing 17:F7). Google Sign-in will work on the Play build. SHA-256 future-proofs App Check/Play Integrity.
- **⛔ REMAINING FOR SAMEER:** (1) **Upload 8 store screenshots + icon + feature** (drag from `branding/assets/android/`, Chrome file_upload can't). (2) **Roll out the DRAFT internal release** (eas.json `releaseStatus:draft` → Play Console → Internal testing → review & roll out) + **add internal testers** + share opt-in link. (3) Then smoke-test the store build (Google Sign-in especially).
- **NOTE:** personal Play acct ⇒ 12-tester/14-day CLOSED testing required before PRODUCTION; internal testing is instant (≤100 testers) — start there.
- **RELEASE NOTES drafted for Sameer (paste into Internal testing → Release notes `<en-US>`, 500-char cap):** debut v1.0.1 copy — full store version (welcome + 6 feature bullets), concise version, tester-checklist version. Given in chat; not stored in a doc.
- **✅ STORE ASSETS ALL UPLOADED (by Sameer):** verified in store listing — app icon SET, feature graphic SET, phone screenshots "8 of 8" assigned (`sofra_shot_01..08`, all in asset library). Store listing COMPLETE. So the "Chrome can't upload" blocker is moot — Sameer did it.
- **⛔ APP NAME still shows "com.thaliplan.app (unreviewed)" b/c app = DRAFT, not yet REVIEWED.** Name+icon ARE "Sofra" in console. Clears only after Google review. **"Send app for review" is DISABLED** (Publishing overview): "complete required steps in dashboard." Pending/blocking items: **Content Rating = "Submit new questionnaire"** (needs submitting — a declaration), **Target audience = "18 and older"** (defensible: avoids Families policy; confirm w/ Sameer), Closed-testing Alpha country settings. Asked Sameer: shall Claude fill content-rating (standard meal-app answers: no violence/sexual/gambling) + send for review, or does he answer it. Internal testing WORKS now regardless — temp name is cosmetic.
- **GOOGLE SIGN-IN fix = wait ~1h (propagation), key is CORRECT** — see below.
- **✅ ADVERTISING ID declaration DONE (Claude, "No")** — was the actual last "needs attention" App-content item (no ad/analytics SDKs; consistent w/ no-ads data safety). App content now "caught up with everything."
- **⛔ NAME "com.thaliplan.app"→"Sofra" REQUIRES app REVIEW, which for a PERSONAL acct requires COMPLETING A CLOSED TEST — internal testing never triggers the identity review.** Blocker = "Closed testing - Alpha" track is a DRAFT (177 countries set, but NO app bundle attached, not rolled out) → "Send app for review" stays disabled. PATH: attach AAB v3 to Alpha → add ≥12 testers → roll out (triggers review → name fixes ~1-2 days) → run 14 days → apply for production. The 12-testers/14-days is a hard Google gate. **ASKED Sameer: shall Claude roll out the closed Alpha test now (closed to tester list, 177 countries) to trigger review + start the clock?** Awaiting his yes. Internal testing works today regardless; temp name is cosmetic.
- **✅ INTERNAL RELEASE IS LIVE + tester bug fixed:** release 1.0.1 = "Available to internal testers" (rolled out 5 Aug 18:23). Sameer's "no email / app not available" = TWO things: (1) internal testing NEVER emails — opt-in link is the only way in; (2) his email was mistyped in the tester list as `sameerrajwde@gmail.com` (missing the 'a') so his real account wasn't authorized. FIXED via Chrome: list "sofra Test" now = `sameerrajwade@gmail.com` + `asmitakhude@gmail.com`. **Opt-in link: https://play.google.com/apps/internaltest/4701720489862037731** — open on phone signed into the EXACT listed account → Become a tester → install (first release can take up to ~2h to propagate). App shows temp name "com.thaliplan.app (unreviewed)" until Google review — normal.

## Prior Session (2026-08-05 earlier — Android Play Console launch, autonomous)
- **GOAL:** publish Sofra to Play Store internal testing autonomously (Sameer away). Driven via Chrome automation on the Play Console + Google Cloud Console.
- **✅ DONE:** Play Console app "Sofra" (com.thaliplan.app) created; **all 9 compliance forms complete** (privacy policy, sign-in details, ads, content rating, target audience, data safety, government apps, financial features, health); category=Food & Drink + contact email/website set; **store listing TEXT saved as draft** (title "Sofra", short desc, full desc 1340 chars).
- **✅ SERVICE ACCOUNT for `eas submit` created** (the API path, since personal Play accounts lack the old API-access UI): enabled **Google Play Android Developer API** on GCP project `thaliplan`; created SA **`play-console-publish@thaliplan.iam.gserviceaccount.com`**; downloaded JSON key → **`play-service-account.json` at repo root (gitignored ✅, never committed)**. `eas.json` already points at this path.
- **✅ SERVICE ACCOUNT PERMISSION GRANTED (Sameer, in Play Console):** `play-console-publish@…` is now an Active user with **Release to testing tracks + Release to production** (verified saved). `eas submit` pipeline now authenticates + sees `com.thaliplan.app` end-to-end.
- **⏳ AAB UPLOAD STILL FAILING — permission not yet API-visible:** 4 `eas submit` runs failed with errors that *flip-flop* between "caller does not have permission" ↔ "missing necessary permissions" (incl. a retry 20min after grant, bash `b6j16t8h9`). Flip-flop = Play API eventual-consistency propagation across Google replicas (Google: up to 24h). Current permission is **ACCOUNT-level** only. **NEXT FIX (higher probability, asked Sameer): add APP-level grant** — Users&perms → the SA → App permissions tab → Add app → Sofra → check "Release to testing tracks" → Save (this is what Expo's pre-check wants; page left open in Chrome). **Fallback auto-retry running: bash `br6q0g7ml` sleeps 30min then re-submits.** Build ready: `b221a8bb`, v1.0.1, versionCode 3. Internal track still shows No releases/bundles.
- **✅ ANDROID STORE SCREENSHOTS — full 8-set generated (commit `9bb9507`):** iOS shots (1290×2796 = 2.17:1) are REJECTED by Play (max 2:1) — that was Sameer's "sizing" error. Built dedicated Android set at **1080×1920 (9:16), 24-bit, no-alpha, ~370KB** via 5 new branded `shot_*.html` (addmeal/calendar/celebration/share/household) + existing home/plan/insights → Chrome headless @2× → sharp downscale/flatten. Numbered `sofra_shot_01..08_*.png` for upload order; captions matched to actual screens (household src=signup, celebration src=plan+confetti). Old un-numbered PNGs removed. Icon 512² + feature 1024×500 already valid.
- **⛔ STILL NEEDS SAMEER (independent of the above):**
  1. **Upload store-listing VISUAL ASSETS** (Chrome `file_upload` only accepts chat-attached files): drag from `branding/assets/android/` → `sofra_icon_512.png` (App icon), `sofra_feature.png` (Feature graphic), `sofra_shot_01..08_*.png` (8 phone screenshots).
  2. **SHA-1 → Firebase** (after AAB uploads) — Claude will pull the Play App Signing SHA-1 (Play Console → App integrity) and hand it ready-to-paste; adding a fingerprint to Firebase is the same account-config change the safety gate blocks Claude from finalizing.
- **THEN:** verify Google Sign-in on the Play build → send Sameer the phone notification.
- **PR:** https://github.com/sameerrajwade/Sofra/pull/8 (branch `android-play-launch`) — PROGRESS + marketing assets. `play-service-account.json` gitignored, never committed.
- Play Console IDs: developer acct `8107930340888208101`, app `4972790754924510458`. AAB URL: https://expo.dev/artifacts/eas/wqkWv--_wyKbiDhAYmWmrpSTW9PtpHUxpgFZZKqDXEs.aab

## Prior Session (2026-08-04 — iOS 1.0.1 SUBMITTED FOR REVIEW ✅ + off sdk57 branch)
- **BRANCH:** left `sdk57-upgrade`; now on **`main`** (fast-forwarded to merge `3a20e1a` = all SDK57/iOS work). PROGRESS.md edit uncommitted (as before). Backup branches intact.
- **iOS GO-PUBLIC DONE via ASC (Chrome):** app was "1.0 Waiting for Review" w/ Build 5 (=1.0.0, has the New-Arch photo bug) — never released, so still the DEBUT. Removed 1.0 from review → edited **Version field 1.0→1.0.1** → removed Build 5, **selected Build 6 (1.0.1)** → Saved → **Add for Review → Submit for Review → "1 Item Submitted"**. Now **"1.0.1 Waiting for Review"**, review ≤48h, email on completion. No export-compliance prompt (ITSAppUsesNonExemptEncryption=false already set).
- **Release = "Automatically release this version"** (already selected) → goes LIVE to everyone on Apple approval, no manual step. Territories = all except EU (non-trader).
- **Decisions (Sameer, confirmed):** demo login `testuser-apple@testuser.com` is real + email-verified (works for reviewers); KEPT existing 5 branded iPhone 6.5" screenshots (Apple-accepted, generic data) — did NOT swap to newer 3-shot brand set. What's New NOT needed (debut release, app never public).
- **NEXT (Apple side): just wait for review.** If APPROVED → auto-releases publicly (done). If REJECTED → read reason in ASC App Review, fix, resubmit. Watch email.
- **HERO SCREENSHOT (this window):** built a clean Sofra title card (icon tile + Fraunces wordmark + sage rule + "Plan meals, remember it all" tagline, warm-paper bg). Generator = `scratchpad/gen-hero.js` (Chrome headless @DSF, fonts base64-embedded; explicit px width fixes a viewport off-center bug). Output in `branding/assets/ios/`: `sofra_ios67_brand_hero.png` (1290×2796), `sofra_ios65_brand_hero.png` (1242×2688), `sofra_ios_ipad_brand_hero.png` (2048×2732). Sameer approved design + wants it as FIRST screenshot + iPad too.
- **LAUNCH TEASER KIT (this window) → `branding/prompts/launch-teaser.md`:** for a surprise WhatsApp announce. Built 4K 9:16 brand cards (`branding/assets/ios/sofra_video_endcard_9x16_4k.png` w/ website pill + `sofra_video_titlecard_9x16_4k.png` clean); generator `scratchpad/gen-endcard.js`. Doc has: ready-to-paste **Veo 3** prompts (2× 8s text-to-video human/kitchen clips + optional image-to-video brand reveal using the title card) + negative prompt; edit plan (stitch 2 clips + overlay end-card last ~2.5s + app-screen insert; upscale to 4K); WhatsApp message copy (short + warm); TestFlight beta-onboarding note (external/public-link, needs beta review). **Claude can't render video — Sameer runs Veo 3 (Flow/Gemini), outputs ≤1080p → upscale.** Key rule: never let Veo draw the real UI/logo/text (use screen-rec + the PNG cards). Website = https://sofra.savvylabs.dev.
- **✅ HERO ADDED + RESUBMITTED — "1 Item Submitted" again.** Chrome `file_upload` only accepts chat-attached files (repo AND scratchpad paths both rejected), so **Sameer dragged the heroes into ASC himself + reordered**: iPhone 6.5" now 6 shots (Sofra hero #1), iPad 4 shots (hero #1). Verified Build 6 / 1.0.1 / demo login / "Automatically release" all intact → Add for Review → **Submit for Review DONE**. Back in queue, ≤48h, auto-release to everyone on approval. **iOS launch = fully staged; just awaiting Apple.** (Next update version → wire the hero into that release's screenshots too.)

## Prior Session (2026-08-04 — iOS live + Android APK install + photo-upload fix)
- **iOS SHIPPED:** app is LIVE on the App Store; internal testers added ✅ — iOS launch path done.
- **#BUG photo/avatar upload broke on BOTH platforms after SDK57 (New Arch)** — root cause: `fetch(uri).blob()` in `storage.ts` is broken under RN New Architecture (SDK52→57), so `uploadProfilePicture` threw → "photo couldn't upload" alert. FIX (no new deps): AvatarPicker now uses image-picker `includeBase64:true` → passes `{uri,base64,type}` (new exported `PickedImage`); `storage.ts` `uploadProfilePicture(userId, base64, contentType)` → `uploadString(ref, base64, 'base64', {contentType})` (also satisfies Storage rule's `contentType.matches('image/.*')`); ProfileScreen consumes new shape. **tsc=0, 50/50 tests.** NOT committed yet. Files: `src/components/AvatarPicker.tsx`, `src/services/storage.ts`, `src/screens/ProfileScreen.tsx`.
- **LOCAL Android build UNBLOCKED via short path:** copied repo (robocopy, minus .git/build caches) to **`C:\S`** → building `assembleRelease` there dodges New-Arch MAX_PATH (Ninja mirrors source path inside build dir, so short root shrinks it twice: ~298→~252 <260). Release signs w/ debug keystore (SHA-1 5E:8F… already in Firebase → Google Sign-in works, no EAS SHA-1 needed). **BUILD SUCCESSFUL in 34m11s (exit 0) — MAX_PATH SOLVED by short path.** APK: `C:\S\android\app\build\outputs\apk\release\app-release.apk` (94 MB), signed w/ debug keystore (5E:8F… already in Firebase → Google Sign-in works). Install needs uninstall of current EAS-signed build first (diff key EE:40 vs 5E:8F; data safe in Firebase). adb device still connected `192.168.86.121:35499`.
- **COMMITTED `4662106` (photo fix + share link + card CTA + name edit + version→1.0.1). EAS iOS PRODUCTION build 6 RUNNING** = build `b8883869` (auto-inc 5→6, creds valid, Team 3HQ9T7PKN4). Latest local APK copied into DiningPlanner `android/app/build/outputs/apk/release/` (gitignored). NEXT after build: `eas submit -p ios` → then ASC (Chrome) attach build 6 + What's New + submit for review.
- **✅ VERSION = 1.0.1 (Sameer confirmed).** iOS build 6 (`b8883869`, 1.0.1) built + **`eas submit -p ios` DONE** (submission `7fa31931`, ASC App ID 6797710095, non-interactive via stored API key) → **STUCK IN EAS FREE-TIER QUEUE (30+ min, status=Queued, "waiting for submission process to start")** — NOT a failure, just free-tier congestion (yesterday's submit was fast bc queue was empty). Build 6 has NOT reached Apple yet (ASC TestFlight still shows only 1.0.0 build 5 Complete from Aug 3; builds 4 & 2 Failed). CLI process alive+polling. A DUPLICATE submit (c845ee17, 11:28) got started (Sameer ran cmd manually / thought Ctrl+C cancelled it — it did NOT; Ctrl+C only stops local CLI, EAS submission keeps running server-side). Claude CANCELLED the duplicate in the EAS dashboard (now =Canceled). **Clean state: ONE original submission `7fa31931` (build 6, 1.0.1) still QUEUED free-tier ~45min, not started.** Options: wait (free queue clears eventually, non-urgent — app not public yet) OR pay EAS priority. Awaiting Sameer's call.
- **WEBSITE + ASSET REORG + MERGE-TO-MAIN (this session):** (a) Split marketing assets by platform: NEW `branding/assets/android/` (Play icon/feature/branded shots + HTML srcs) mirroring `ios/`; shared social/brand assets stay at root; `screens/` stays shared. **GOTCHA: `.gitignore` had bare `android/` → ignored the new folder; anchored to `/android/` (root RN build only) so marketing android/ is tracked.** (b) Website: DELETED `docs/architecture.html` (exposed Firestore collection/doc schema) + stripped its nav/footer links across all pages; `#get` now shows App Store + Google Play (coming-soon, no dead links, branded SVGs); FAQ expanded (platforms=iOS+Android, Apple Sign-In, photo+name edit); security.html emoji→branded line-SVG icons. privacy.html Firebase mention KEPT (legit sub-processor disclosure). No new app screens → gallery unchanged. **✅ MERGED to main: PR #7 (merge commit `3a20e1a`, 10 commits — whole SDK57/iOS line). GitHub Pages (main/docs) rebuilding → sofra.savvylabs.dev updates in ~1-2 min.** Branch sdk57-upgrade kept.
- **✅✅ RESOLVED — Sameer upgraded to EAS Starter ($19/mo, high-priority queue, cancellable). The QUEUED submission `7fa31931` got retroactively bumped + COMPLETED in seconds → Status=Success, binary UPLOADED to App Store Connect 12:50 PM. Build 6 (1.0.1) now PROCESSING at Apple (~5-10 min → appears in TestFlight).** No re-run needed. EAS pricing ref: Free=low-priority queue (caused the 2h wait); Starter $19 = high priority + $45 build credit (also helps upcoming Android prod build); Production $199 overkill. Cancel anytime.
- **2-HOUR QUEUE DIAGNOSED (not a bug):** original `7fa31931` STILL Queued after 2h. Verified status.expo.dev = ALL OPERATIONAL, no incident. Root cause = EAS free-tier queue has NO SLA + deprioritized behind paid; some days minutes, some days hours. Submission is valid, just not picked up. **Re-running does NOT help (same queue) — DON'T.** Paths: (1) EAS PRIORITY submit (paid) = only reliable queue-skip — recommend if shipping today; will hit same wall on Android prod build; (2) leave overnight = clears on its own, no urgency (app still in Apple review, not public); (3) Apple Transporter (free) = bypasses EAS but needs a Mac. Awaiting Sameer's pick.
- **✅ BRANDED iOS SCREENSHOTS generated (6.7" 1290×2796 + 6.5" 1242×2688) for home/plan/insights** → `branding/assets/ios/sofra_ios{67,65}_brand_{home,plan,insights}.png`. Include Sofra lockup + Fraunces headline + sage rule + subtitle + phone w/ GENERIC data (Rivera Family). Generator = `scratchpad/gen-ios-shots.js` (system Chrome headless @2x + sharp downscale; embeds Fraunces/Inter TTFs + lockup; run with `NODE_PATH=<proj>/node_modules`). Sameer approved design.
- **⚠️ OPEN — iPad branded shots (2048×2732):** wider canvas needs different layout (not centered phone); plain iPad shots exist from before. Awaiting: brand iPad too, or use plain?
- **⚠️ OPEN — GO PUBLIC:** to release 1.0.1 to everyone still need (Chrome/ASC): pull in-review 1.0.0 record→set 1.0.1, upload new screenshots, add What's New (photo fix/name edit/share links), attach build 6, submit for review.
- **~~OPEN DECISION 1 — launch version #~~ RESOLVED (1.0.1):** app is NOT public yet (1.0.0 still UNDER REVIEW, never released). Build 6 is tagged 1.0.1. Options: ship debut as **1.0.1** (no rebuild, valid — recommended) OR revert app.json→1.0.0 + rebuild (~20m) for a "1.0.0=launch" story. Awaiting Sameer. What's New draft: photo-upload fix / editable name / share links.
- **⚠️ OPEN DECISION 2 — iOS branded screenshots:** Android has RICH branded store shots (`sofra_shot_{home,plan,insights}.png`, HTML src `shot_*.html`, Sofra frame+copy+link); iOS `sofra_ios_67_*` are plainer device frames. Plan = re-render `shot_*.html` at iOS canvas (NOT resize — aspect 9:16→9:19.5 differs) → export 6.7" 1290×2796 + 6.5" 1242×2688 + iPad 2048×2732. Awaiting Sameer: which screens (3 vs all 5) + full copy/link overlays y/n.
- **✅ ON-DEVICE VERIFIED (Sameer, Pixel 10 Pro): photo upload WORKS + name edit WORKS.** Photo fix = XHR→blob→uploadBytes (below). FONT: Sameer+wife prefer CURRENT (Fraunces/A) — **font change DEFERRED to later MVP**, no code change; mockup archived at artifact 36dc36a7. NEXT = commit `sdk57-upgrade` + EAS iOS build (ship photo/share/name fixes to App Store) + EAS Android production AAB.
- **PHOTO FIX v1 (base64 uploadString) FAILED on-device** — Firebase's uploadString('base64') also reconstructs a Blob from ArrayBuffer, which RN doesn't support → still threw. **FIX v2 (canonical RN method): XHR `responseType:'blob'` → `uploadBytes(ref, blob, {contentType})`** (`storage.ts` `uriToBlob`); AvatarPicker passes `{uri,type}` (dropped base64); ProfileScreen now SURFACES the real error (code+message in alert + console.error) so a 3rd failure is diagnosable via `adb logcat`. **NOT yet on-device verified.**
- **4 UX ITEMS DONE in code (batched w/ photo fix):** (2) FamilyScreen invite share appends `https://sofra.savvylabs.dev`; (3) ShareStatModal card adds "Get Sofra free · sofra.savvylabs.dev" CTA under tagline; (4) ProfileScreen name now tappable (pencil) → Paper Dialog edits name → `updateUserProfile(id,{name})`+auth store (fixes Apple email-prefix name; Portal host = PaperProvider App.tsx:92). tsc=0, 50/50. (1) FONT = still TODO (mock variants). Synced src→C:\S, incremental rebuild = **3m26s (JS-only, native cached) → APK rebuilt + installed in-place (`adb install -r`, same debug key = no uninstall, data kept).** ON-DEVICE TEST of photo fix + 4 items PENDING.
- **FONT compare (item 1) DELIVERED as Artifact:** https://claude.ai/code/artifact/36dc36a7-0113-460c-a5a6-9427610f02a1 — real Home screen in 3 type treatments (A current all-Fraunces / B toned-down: serif only on brand+stat number / C formal: all-Inter, no serif). Built with actual Fraunces+Inter TTFs base64-embedded (from `node_modules/@expo-google-fonts`, only 2 families installed). Recommendation = **B**. Awaiting Sameer+wife's pick → then wire font choice into `src/config/theme.ts` `Fonts`. (Browser-pane verify blocked — new-origin approval needed while user away; validated HTML structurally instead.)
- **(original 4 UX items context):** (1) FONT: wife (PO) says Fraunces looks fancy/informal — plan mock 2-3 variants as screenshots (current / lighter Fraunces / neutral serif), decide from visuals not abstract. (2) FAMILY-CODE SHARE has NO link (`FamilyScreen.tsx:40` msg = code only) → append smart link `sofra.savvylabs.dev` (one link, both stores). (3) SHARE CARD has brand+tagline but NO download CTA → add footer `sofra.savvylabs.dev`/"Get Sofra free". (4) APPLE name shows email-prefix "sameerrajwade" not "Sameer Rajwade" — ROOT CAUSE: Apple returns name ONLY on 1st-ever auth; Sameer re-authed → null → `useAuthStore.ts:75/158` falls back to `email.split('@')[0]`. FIX = editable name on Profile (`updateUserProfile(id,{name})` exists) — NOT overkill, it's the only reliable recovery. #2/#3/#4 are JS-only → fast incremental rebuild (~2-3m, no C++). Then commit → EAS iOS/Android.
- **Android EAS APK (`4da33c3f`, ~94 MB) INSTALLED on Pixel 10 Pro via wireless adb.** adb at `C:\Users\samee\AppData\Local\Android\Sdk\platform-tools\adb.exe` (NOT on PATH). Wireless flow: `adb pair 192.168.86.121:40225 <code>` (needed kill-server first — protocol-fault on 1st try) → `adb connect 192.168.86.121:35499`. Install: signature mismatch vs old debug-signed app → **uninstalled `com.thaliplan.app` then installed fresh** (data safe in Firebase, user re-signs in).
- **EAS SHA-1 ADDED TO FIREBASE ✅** so Google Sign-in works. Extracted from APK via `apksigner verify --print-certs` (keytool -jarfile failed = APK v2/v3 sig only). EAS SHA-1 = `EE:40:8F:10:C5:D2:FE:26:69:E1:98:21:8B:7C:21:DF:62:81:FF:A3` (SHA-256 `0B:37:...:8C:85`). Added via Chrome to Firebase console → thaliplan → Android app fingerprints (now 3 SHA-1s). **AWAITING Sameer's device test of Google Sign-in.**

## Prior Session (2026-08-03 latest — Expo SDK 52→57 upgrade, branch `sdk57-upgrade`)
- **WHY:** Apple rejected the iOS build — must use iOS 26 SDK / Xcode 26. Expo SDK 52 (RN 0.76) can't compile on Xcode 26 (`fmt` consteval error). Sameer chose to UPGRADE (not patch). Branch `sdk57-upgrade` off `ios-setup`; clean checkpoint commit `68c817b`; Android backup intact at `C:\Users\samee\DiningPlanner-Android`.
- **DONE:** `npm i expo@^57` + `expo install --fix` → expo 57.0.9, react 19.2.3, react-native 0.86.2, typescript 6, @types/react 19, all expo modules 57.x. Fixes: app.json splash→`expo-splash-screen` plugin (+font/sharing/status-bar plugins auto-added); tsconfig `types:[jest,node]` (TS6); code: RN0.86 `ColorSchemeName 'unspecified'` handling in useThemeStore + App.tsx Appearance listener; Celebration `absoluteFillObject`→explicit edges. **tsc=0 (was 167), 50/50 tests, expo-doctor 20/20.** Committed `7eaec79`.
- **iOS COMPILE FIX CONFIRMED:** prod build `ff68c56f` (SDK57, build 4) FINISHED on Xcode 26 → fmt wall cleared. Submitted (2-min upload; ASC API key stored so non-interactive submit works). **Apple PASSED the SDK check but rejected build 4 with ITMS-90683: missing Info.plist purpose strings** (AvatarPicker uses launchImageLibrary+launchCamera). FIX: app.json ios.infoPlist += `NSPhotoLibraryUsageDescription` + `NSCameraUsageDescription` (commit `3a67fdc`, iOS-only). Rebuilding as build 5 = `a519e2a1` (in progress). **⚠️ Still NOT runtime-tested — New Arch; watch chart-kit, google-signin@13, vector-icons.**
- **ANDROID LOCAL BUILD (SDK57) UNVIABLE on Windows — ROOT CAUSE = upgrade's New Architecture:** New-Arch C++ codegen creates ~298-char paths (e.g. `...react_codegen_rngesturehandler_codegen.dir\C_\Users\samee\DiningPlanner\node_modules\...RNGestureHandlerDetectorShadowNode.cpp.o`) > Windows MAX_PATH 260. SDK52 (old arch) didn't → why local worked before. Tried 3 ways: (1) from C: → `ninja: longer than 260`; (2) enabled `LongPathsEnabled=1` (Sameer, admin) → NDK ninja still ignores it, same error; (3) `subst X:` → fixed 260 BUT broke Expo autolinking (`cmd` + Node can't find package.json on a virtual drive). subst removed. Only reliable LOCAL fix = physically move repo to short real path (e.g. C:\S). Cloud/EAS unaffected (Linux). Keystore prep done + reverted: prebuild kept debug.keystore 5E:8F…F6:25; `android/local.properties`→SDK path; JDK17 OK.
- **DECISION (Sameer): build Android test APK on EAS** → `4da33c3f` (preview/apk, New-Arch, SDK57) building. **SHA-1 clarification for Sameer:** adding EAS keystore SHA-1 to Firebase is ADDITIVE (old debug SHA-1 5E:8F… stays) → existing debug-signed testers KEEP working, NO reinstall from the SHA-1. BUT the EAS APK is a DIFFERENT signing key → can't install over the old debug-signed app (signatures mismatch) → a tester switching to it must uninstall+reinstall (data safe in Firebase). **OPEN Q to Sameer:** proceed with EAS keystore + add its SHA-1, OR configure EAS to sign with the existing debug keystore (seamless update for current testers, no new SHA-1). Awaiting his choice; APK build polling.
- **⚠️ ANDROID LOCK ENDED:** shared codebase moved to SDK 57 → Android must be re-tested + rebuilt (new Play AAB) before its launch. The already-built Android AAB `b81c9f41` is on OLD SDK 52.
- **iOS build 4 rejected ITMS-90683** (missing Info.plist purpose strings — AvatarPicker uses image-picker); FIXED app.json ios.infoPlist += NSPhotoLibraryUsageDescription + NSCameraUsageDescription (commit `3a67fdc`). Build 5 `a519e2a1` (SDK57) UPLOADED + PASSED → TestFlight "Ready to Submit" ✅. Submit works non-interactive now (ASC API key stored from Sameer's first submit).
- **iOS LISTING PROGRESS (Claude via Chrome):** ✅ app record + name "Sofra: Family Meal Planner" + subtitle "Plan meals, remember it all" + categories F&D/Lifestyle + desc/keywords/promo/URLs/copyright + Privacy Policy URL. ✅ **App Privacy label PUBLISHED** (5 data types: Name/Email/Photos/OtherUserContent/UserID — all App Functionality, Linked, NO tracking). **STILL TODO: screenshots** (Claude CAN'T upload — Chrome file_upload only takes user-attached files; resized to 1242×2688 + iPad 2048×2732 and SENT to Sameer to drag in), **Age Rating** (→4+ all None), **Content Rights** (no 3rd-party), **Pricing** (Free), **attach build 5 + export compliance**, then Add for Review. Demo account = Sameer.
- **ANDROID EAS APK BUILT** (local Windows blocked by MAX_PATH): build `4da33c3f` (preview/apk, SDK57) FINISHED → install URL `https://expo.dev/artifacts/eas/hhe00vI1i3uMa77xgwNDlDX0QVU20NyjnuuYvbDH-Lo.apk`. Signed w/ EAS keystore (diff SHA-1 from debug) → **Google Sign-in needs EAS SHA-1 added to Firebase (TODO, Claude via Chrome)**; email/pw + Apple work now. adb currently shows NO device (wireless dropped) → Sameer installs via phone browser download. App name decision: keep "Family Meal Planner" (SEO) over "Family Meal Memory".

## Prior Session (2026-08-03 — Sign in with Apple + iOS publish plan, branch `ios-setup`)
- **REALITY CHECK for Sameer:** the only iOS builds on EAS are SIMULATOR builds (`preview` profile, `ios.simulator:true`) → `.tar.gz`, Appetize-only, NOT App-Store-uploadable. Android production `.aab` (`b81c9f41`) IS store-ready. To ship iOS we must make a NEW signed device `.ipa` via `eas build -p ios --profile production` — EAS builds+signs in the cloud, NO Mac needed. Sameer confirmed Apple Developer acct is ACTIVE.
- **Sign in with Apple IMPLEMENTED** (App Store guideline 4.8 requires it when Google login is offered — top rejection cause). iOS-only, Android-inert. Added deps `expo-apple-authentication@~7.1.3` + `expo-crypto@~14.0.2` (flagged: new deps, but iOS-native/no Android behavior change; button is `Platform.OS==='ios'` + `isAvailableAsync` gated). `app.json`: `ios.usesAppleSignIn:true` + plugin (android block untouched). `auth.ts` `signInWithApple()` uses SHA-256 nonce (Crypto) → Firebase `OAuthProvider('apple.com')`; captures Apple's one-time name → displayName. Store action + official `AppleAuthenticationButton` in AuthScreen (dark-aware). tsc=0, 50/50 tests. NOT built yet (needs fresh EAS build for the entitlement).
- **DONE this session via Chrome automation:** (1) ✅ Firebase Apple provider ENABLED (Auth→Sign-in method; Services ID left blank — native iOS needs only the toggle). (2) ✅ Apple **Team ID = `3HQ9T7PKN4`** captured; membership ACTIVE (Individual, renews 2027-08-04, auto-renew on). (3) ✅ `eas.json` → added `submit.production.ios.appleTeamId`. (4) ✅ Apple assets generated by subagent in `branding/assets/ios/` — `sofra_icon_1024.png` (1024², no-alpha, App-Store-safe), 5× iPhone 6.7" `sofra_ios_67_*.png` (1290×2796), 3× iPad 12.9" `sofra_ios_ipad_*.png` (2048×2732). Visually verified — branded frames, generic Rivera-Family data.
- **DSA trader status RESOLVED ✅ = NON-TRADER** (Sameer is on H1B — declaring "trader" = asserting a business/professional capacity, which we deliberately avoided; also a free app w/ no revenue is genuinely non-commercial = non-trader is accurate). Done via Chrome; ASC Compliance now shows DSA Active. TRADEOFF (accepted): app NOT distributed in EU. **H1B WATCH-ITEM: do NOT enable the deferred paywall / any monetization (IAP, ads, paid) without an immigration attorney — free app w/ no income is low-risk; any self-employment income is an H1B unauthorized-work question.** Free Apps Agreement Active; Paid Apps Agreement unsigned (not needed).
- **DECISIONS (2026-08-03):** keep iPad support (supportsTablet stays true, iPad screenshots kept); territories = all EXCEPT EU (per non-trader). App name = "Sofra: Family Meal Planner" (fallback if "Sofra" taken). Listing pack written → `docs/ios-appstore-listing.md` (paste-ready copy + App Privacy label + age-rating + reviewer notes). Committed `6c2fd6d` (Apple sign-in) + `4e30328` (listing pack).
- **SUBMISSION IN PROGRESS (2026-08-03 later):** Prod build `20b48e5f` (Xcode16/iOS18.2 SDK) built + UPLOADED by Sameer via `eas submit` (interactive; created+stored an ASC API key on EAS → future submits can be non-interactive). **Apple REJECTED it: must build with iOS 26 SDK / Xcode 26.** FIX (Android-safe, iOS-only): `eas.json` build.production.ios.image=`macos-tahoe-26.5-xcode-26.6`; triggered rebuild `4e6a7226` non-interactively (reused stored dist cert/profile, no Apple login). **RISK: Expo SDK 52 compiling on Xcode 26 may fail → would force an Expo SDK upgrade (big, Android-lock implications).** Polling the rebuild.
- **ASC app record CREATED:** "Sofra: Family Meal Planner", Apple ID (ascAppId) `6797710095`, bundle com.thaliplan.app, SKU sofra-ios-001, English US. `eas.json` submit.production.ios has appleTeamId+ascAppId. **Metadata DONE via Chrome** (from `docs/ios-appstore-listing.md`): promo text, description, keywords, support/marketing URL (sofra.savvylabs.dev), copyright, subtitle "Plan meals, remember it all", category Food & Drink/Lifestyle. **Metadata STILL TODO:** screenshots (must resize 1290×2796→**1242×2688** for the 6.5" slot; iPad 2048×2732 ok), App Privacy nutrition label, Age Rating questionnaire (all None→4+), Content Rights (no 3rd-party), Pricing (Free), then select build + export-compliance + Add for Review.
- **Appetize test build READY** (fresh, incl. Sign in with Apple): sim build `62dc2105` → `https://expo.dev/artifacts/eas/irjkXHk1owOVtNvrcrpfeJgLbnHaeaPn9fNBJihsMXE.tar.gz` (SDK rule doesn't affect simulator/Appetize).
- **REMAINING (Sameer):** create a demo test account in-app → for Apple reviewers (listing-pack §6); test the Appetize build. Everything else Claude drives.

## Prior Session (2026-08-02 — pre-launch auth fixes, branch `ios-setup`)
- **DECISION: block launch to fix auth first** (Path A = Firebase link-based, no backend/no billing). Code-only + Firebase-console; ZERO Android-config diff (no app.json/eas.json/package.json touched — lock holds). tsc=0, **50/50 tests pass**. ALL changes are shared RN/Firebase-JS code → apply to BOTH iOS + Android (no platform branch).
- **#SECURITY Firebase errors no longer leaked:** new `src/utils/authErrors.ts` maps codes → generic copy; wrong-password / unknown-account / invalid-credential ALL say "Incorrect email or password." (no DB/SDK leak, no account-enumeration). Wired into every `useAuthStore` catch. Locked by `authErrors.test.ts` (4 tests).
- **Sender name:** Sameer owns savvylabs.dev NOT sofra.com; only wants inbox to read "Sofra" (not app.thaliplan). Path A sender-name="Sofra" console field fully covers it — no domain/backend. From-address stays firebase domain (he doesn't care).
- **#4 reset email OPEN:** Sameer confirms NOTHING arrives (inbox+spam). Most likely = tested a Google-only account (no password → Firebase silently sends nothing). NEXT: check Auth→Users Providers col for that email; if only-Google, expected. Else create fresh email/pw account + retest; if still nothing → project email-config dig.
- **BUILD STRATEGY (Sameer's call): Android = LOCAL, iOS = EAS.** Committed `e0bd110` on `ios-setup`. Cancelled an unneeded EAS Android build; free tier = 1 concurrent. Android release APK built LOCALLY via `npx expo run:android --variant release` (device connected over wireless adb; release signs w/ debug keystore SHA1 5E:8F:16:...:F6:25 = the stable one already in Firebase → Google Sign-in works). Build = 3m11s (NORMAL; the "slow"/hang was expo run:android idling on Metro:8081 after BUILD SUCCESSFUL — next time use `cd android && ./gradlew assembleRelease` to build+exit). APK `android/app/build/outputs/apk/release/app-release.apk` (67MB); INSTALLED on Pixel (com.thaliplan.app v1.0.0, 14:48). AWAITING Sameer device test of the 4 fixes. iOS sim build on EAS (`--profile preview`) for Appetize DONE = build `620270a7`, artifact https://expo.dev/artifacts/eas/Mhwupxa54G8EnSKzpGJuecYiyDruK3QJyg72ruuG4aw.tar.gz — REPLACES stale `e7a0f565` (predates these fixes). Local toolchain confirmed: SDK, adb, gradle, JDK17 all present.
- **Google Sign-in caveat on local/debug-signed APK:** debug-keystore SHA-1 must be in Firebase or Google sign-in silently fails; email/password + new features test fine regardless.
- **VERIFICATION EMAIL CONFIRMED WORKING** (Sameer received it). **Level A branding DONE by Claude via browser automation in Sameer's Chrome** (Firebase console has no CLI/API for these): Public-facing name → "Sofra" (Project Settings); Email-verification + Password-reset templates Sender name → "Sofra" (both saved OK). %APP_NAME% now renders "Sofra" in all subjects/bodies. NO rebuild (server-side, both platforms). Project is on BLAZE (Level B feasible w/o plan change). Level B (custom domain noreply@savvylabs.dev + full HTML via Cloud Fn+Resend) DEFERRED. NOT done: visible do-not-reply footer in body (reply-to already =noreply; offered, awaiting Sameer OK to edit body HTML).
- **Forgot-pw copy fixed (code):** removed the "just use Continue with Google" nudge — a user may register abc@gmail.com w/ email+password on purpose; don't steer to Google. Now neutral "if an account exists, a link is on its way." tsc=0, 50/50.
- **#1 Email verification (NEW):** `signUpWithEmail` now sends a verification link; auth store tracks `emailVerified`; new gate screen `VerifyEmailScreen` sits between auth and app ("I've verified"/"Resend"). Google accounts arrive verified → skip gate. Wired in `App.tsx` + `AppNavigator`.
- **#2 Forgot password:** code path was already correct (`sendPasswordResetEmail`) — real cause of "no email" = tested a Google-only account (no password) or spam. Hardened the success copy to say so.
- **#3 Branding:** Path A can only set sender NAME=Sofra + subject/body + no-reply footer (From-address stays Firebase domain; custom sender needs Path B). Exact paste-in copy → `docs/firebase-email-templates.md` (SAMEER console task).
- **#4 Meal-type bug FIXED:** tapping Dinner on Home now pre-selects Dinner in AddMeal (passed `mealType` nav param) instead of defaulting to Lunch and falsely reporting a conflict.
- **NEEDS DEVICE TEST before Android build:** verify-email gate + resend + reset copy + meal-type slot. Not yet run on device (RN — not browser-previewable).

## Prior Session (2026-08-02 — Appetize walkthrough)
- Reviewed `ios-appetize-runbook.md`; iOS sim build #2 (`e7a0f565`) is the artifact to test. FLAGGED: EAS artifact URLs expire ~30d — if the .tar.gz 404s, `npx eas-cli build:view e7a0f565` or fresh `eas build -p ios --profile preview`.

## Prior Session (2026-08-02 — iOS wiring on branch `ios-setup`)
- **Android LOCK established:** backup `C:\Users\samee\DiningPlanner-Android` (repo minus node_modules); work on branch `ios-setup`; hard constraint added (see Constraints). Verified Android config byte-for-byte untouched in every edit.
- **Registered iOS app in Firebase** (`thaliplan` project, bundle `com.thaliplan.app`, nickname "ThaliPlan iOS") → additive, Android app untouched. Got iOS OAuth client.
- **Wired iOS Google Sign-in (JS-SDK-correct, no plist in repo):** `app.json` plugins → google-signin `iosUrlScheme` (proven iOS-Info.plist-only, no Android branch); `auth.ts` → added `iosClientId` (Android ignores); `eas.json` preview → `ios.simulator:true` (Appetize path, no Apple acct needed). iOS OAuth values: client `349329204088-nmihufdrn14vsqikc5tpotqf37otvaui`.
- **EAS linked:** project `sofrasavvylabsdev` (owner `savvylabs`, id f4ddb333-3bef-4b83-a470-a98b046a86a8); app.json slug → `sofrasavvylabsdev` (approved; Expo-internal only, Android package id unchanged). Added `ios.infoPlist.ITSAppUsesNonExemptEncryption:false`.
- **DISCOVERED + FIXED pre-existing EAS-build blocker (affects Android too):** `@expo/vector-icons@15.1.1` peer-needs `expo-font>=14` but SDK52 pins `expo-font@13.0.4` → `npm install` ERESOLVE fails on EAS (and locally). Fix = `.npmrc` `legacy-peer-deps=true` (ZERO version change; dry-run tree identical). **Android production EAS build will need this same .npmrc.**
- **Health:** tsc=0, 46/46 tests pass. Committed on `ios-setup` as `f514818`.
- **iOS builds:** #1 `0c7036a9` ERRORED (npm conflict, pre-fix). #2 `e7a0f565` **FINISHED** ✅ — iOS SIMULATOR build (isForIosSimulator=true, SDK52, v1.0.0 build1). Artifact (.tar.gz): https://expo.dev/artifacts/eas/ug9sdxLZ8LsngkQ4dbO9nf0hyXXPfx2Tisu9COOnlks.tar.gz — ready for Appetize.io.
- **Prior session (2026-08-01):**
- **Website icon refresh:** feature-card emoji (🍲✨📊…) → 7 branded SVG icons. Live on `sofra.savvylabs.dev`.
- **iOS deployment research & guides:** Created 4 comprehensive guides (deploy checklist, Firebase setup, App Store submission, quick-ref). Key findings: iOS differs from Android (URL schemes instead of SHA-1, stricter privacy label, silent failures documented).
- **iOS testing strategy:** Appetize.io cloud simulator (free tier: 30 min/month, 3 min/session) for pre-testing before giving builds to friends. No Mac/iPhone/Apple account needed for initial testing.
- **Model decision:** Opus 4.8 Medium effort chosen for iOS work (better reasoning on multi-step config, catches silent failures, worth cost vs. wasted Appetize sessions).
- **Launch readiness:** Android path ready. iOS path fully documented + testing strategy confirmed.

## Next Up (Sameer's launch path)
**Android PRODUCTION AAB — BUILT ✅** (commit `280446f`; build `b81c9f41-f1db-465f-82ec-58f5b536f330`; versionCode auto→2; EAS-managed upload key). AAB: https://expo.dev/artifacts/eas/sdbfOC8hQUrQTv-HJfDE9FZDVrLVhggoTchkDbhJM7k.aab
1. Upload AAB → Play Console (personal acct ⇒ 12-tester/14-day closed test before prod; internal testing instant).
2. **CRITICAL SHA-1 (Google Sign-in breaks without it):** Play build is signed w/ EAS upload key + Play App Signing key — BOTH SHA-1s differ from the debug keystore (5E:8F...) that works locally. After 1st upload: Play Console→App integrity (App-signing + Upload SHA-1) + `npx eas-cli credentials` (EAS upload SHA-1) → add ALL to Firebase→Android app fingerprints. Claude can do this via Chrome automation like the email templates.
3. Data Safety form, content rating, target audience, privacy URL, store listing (assets in `branding/assets/`).

**iOS — NOW LIVE PATH (Apple Developer acct ACTIVE as of 2026-08-03):** Sign in with Apple done in code. Remaining to ship: enable Apple provider in Firebase → create app in App Store Connect → add `submit.production.ios` to eas.json (needs Apple Team ID + ASC App ID) → `eas build -p ios --profile production` (cloud signing, no Mac) → `eas submit -p ios` → 6.7" screenshots + App Privacy label + metadata → submit for review. NOTE: existing `620270a7` is a SIMULATOR build only — cannot go to the App Store.

**Still open:** device-test the 4 fixes on the installed APK; Providers-col check for reset email (#4); do-not-reply footer decision.

**iOS (simulator build READY — Appetize next):**
1. ✅ Build #2 FINISHED — artifact URL above. NEXT: create Appetize.io account, upload the .tar.gz (or paste the artifact URL). Guard the free tier (30min/mo, 3min/session) — run the checklist deliberately.
2. Run the 10-point smoke test (watch Google Sign-in especially — simulator OAuth is the main risk).
3. If a build fails: `eas build:view <id> --json` → logFiles[0] → `curl -sk --compressed <url>` (it's NDJSON; grep `npm error`/phase).
4. If all pass → enroll Apple Developer ($99/yr) → signed build → TestFlight → App Store (`ios-deploy-checklist.md`).
- NOTE: `@react-native-google-signin` needs a dev/custom build (not Expo Go). EAS login is done (sameerrajwade); eas-cli NOT on Sameer's real-terminal PATH (use `npx eas-cli` or `npm i -g eas-cli`).
- Guides ready: `ios-deploy-checklist.md`, `ios-firebase-setup.md`, `ios-appstore-submission.md`, `ios-quick-ref.md`.

**Post-launch:** Update `#get` button href (line 120) to live Play URL.

## Constraints
- **ANDROID LOCK (iOS work in progress):** Android is FROZEN. iOS changes are additive to iOS-only sections ONLY. HANDS OFF: `app.json` root keys + `"android"` block (L23-29); `eas.json` existing `android`/`submit` keys; `package.json` dependency versions (no bumps for iOS). No new/upgraded deps to support iOS without flagging Android impact first. Every iOS commit must show ZERO diff to Android config. iOS work lives on branch `ios-setup`. Backup restore point: `C:\Users\samee\DiningPlanner-Android` (full repo minus node_modules).
- **Firebase = JS SDK (not native RN Firebase):** `src/config/firebase.ts` uses a platform-agnostic `firebaseConfig` JS object → iOS reuses the SAME config for Firestore/Auth/Storage. NO `GoogleService-Info.plist` needed for those. The iOS guides' plist/native steps mostly DON'T apply. Only iOS-specific secret = Google Sign-in iOS OAuth client (iosClientId + reversed-client-id URL scheme) in `app.json`.
- Repo: github.com/sameerrajwade/Sofra. Package id `com.thaliplan.app` must NOT change (Firebase-linked).
- **Never handle signing/keystore passwords — Sameer signs.** Signing path = **EAS managed** (cloud build, EAS-stored upload key, Play App Signing).
- Motion = built-in RN Animated only (no Reanimated). Paywall/monetization DEFERRED to post-launch.
- Develop → test → show results → only then build/deliver. Verify on device via `adb exec-out screencap -p`.
- Firebase project = `thaliplan`. Prod rule deploys: `firebase deploy --only firestore:rules` / `--only storage`.

## Key file & asset locations
- **Video prompts:** `branding/prompts/video-prompts.md` — 10× 20-second brand-commercial briefs (real-people shoots): cast, setting, timecoded beats, which app screen to feature, on-screen line, + production notes. End-card = logo lockup + Google Play badge.
- **Branding / marketing assets:** `branding/assets/` (has its own `README.md`). Master logo source: `branding/logo/icon_full.svg` (terracotta tile) + `branding/logo/mark_cream.svg` (bowl only). Palette: terracotta `#C0532E`, sage `#5E8B6A`, warm paper `#FBF7F2`. Render pipeline = Chrome headless @2× → sharp downscale; icon via sharp-from-SVG. Editable `.html` source sits next to every `.png`. Exports:
  - `sofra_icon_512.png` 512×512 — **Play Store app icon** (full-bleed)
  - `sofra_feature.png` 1024×500 — **Play feature graphic** (subtle URL)
  - `sofra_shot_home/plan/insights.png` 1080×1920 — **Play phone screenshots** (branded frames; embed the GENERIC mockups — store-ready, no test data)
  - `sofra_reel.png` 1080×1920 — YouTube Shorts / Instagram Reels (+ Play pill)
  - `sofra_square.png` 1080×1080 — Instagram feed
  - `sofra_og.png` 1200×630 — website Open Graph / link-preview image
  - `sofra_lockup.png` 900×280 — transparent horizontal logo lockup (site header/press)
- **Generic app-screen mockups:** `branding/assets/screens/*.html` → rendered PNGs. All 9 screens (login, household, home, addmeal, plan, celebration, insights, share, calendar) rebuilt with GENERIC global data — "The Rivera Family"; dishes Veggie Stir-Fry / Grilled Salmon / Spaghetti Bolognese / Beef Tacos / Roast Chicken; restaurants Nonna's Kitchen / Green Bowl; cuisines Italian/American/Mexican/Asian; USD. **Copied into `docs/assets/screens/*.png` — the live site gallery (index.html) is now fully generic.**
- **Store listing copy:** `docs/store-listing.md` (title, short/full description, category, URLs).
- **Android deploy checklist:** `docs/android-deploy-checklist.md` (signing/build, Firebase, Play compliance, listing, rollout, launch sequence).
- **MVP2 backlog:** `docs/MVP2.md`.
- **NotebookLM ad brief PDF:** `C:\Users\samee\Downloads\Sofra_Ad_Brief_for_NotebookLM.pdf` (12pp). NOTE: embeds PRE-rename screenshots ("Dine Outs") — regenerate if visuals must match current UI.
- **Website:** GitHub Pages from `docs/`, custom domain `sofra.savvylabs.dev` (via `docs/CNAME`). Pages: index.html, guide.html, features.html, security.html, architecture.html, privacy.html, terms.html.

## LAUNCH STATUS

### DONE (code / infra / assets — all verified)
- **Launch-blocker bug fixed:** Dine Out/Takeout with an unrated dish wrote `items:[{name, rating:undefined}]` → Firestore "Unsupported field value: undefined". Fixed at source + deep recursive `stripUndefined` (`src/utils/sanitize.ts`) now wraps EVERY Firestore write. Device-verified: dine-out saves cleanly.
- **5-agent audit → 20+ fixes** (meal / planner / insights / restaurants / auth). Incl. **P0 account-deletion data-loss** (now deletes Auth user FIRST), planner kids-tiffin leak + data-loss, insights false "0% home" / range headline / empty-window / TZ bucketing, restaurant future-visit counts + TZ dates, avatar 1MB cap (512px), atomic arrayUnion/arrayRemove membership, signup household-name sync.
- **Unique Dishes = home-only** (was double-counting outside meals); Dish Library + Insights consistent. **Relative dates** Today/Yesterday/N-days-ago (`src/utils/relativeDate.ts`).
- **Plan↔Calendar synced:** planner suggests HOME dishes only; saved outside meals show restaurant name + dish; day header font + TODAY highlight box match Calendar.
- **Firestore + Storage rules deployed to prod** (leave-household branch added; both match repo).
- **eas.json** wired: `submit.production.android` (internal track, draft), production `autoIncrement` + `appVersionSource: remote`. Hermes on. AAB config in place.
- **Brand kit + store copy + privacy/terms** ready. In-app account deletion live.

### REMAINING FOR LAUNCH — Sameer console tasks (NOT code)
1. `npm i -g eas-cli` → `eas login` → `eas build -p android --profile production` → accept "generate keystore" → download the **.aab**.
2. Play Console: create app "Sofra" → **Internal testing** track → upload the .aab (or `eas submit -p android` after placing `play-service-account.json` at repo root — it's gitignored).
3. **CRITICAL:** after first upload, copy the **Play App Signing key SHA-1** (Play Console → App integrity) AND the upload-key SHA-1 → add BOTH to Firebase console (project settings → Android app). Then **verify Google Sign-in on the internal-testing build** — it silently fails if SHA-1 is missing.
4. Play compliance: **Data Safety form** (collects: email, name, user content/meals, avatar photo; encrypted in transit; user-deletable), **content rating** (IARC → Food & Drink, likely Everyone), **target audience** (not child-directed), **privacy URL** https://sofra.savvylabs.dev/privacy.html, category Food & Drink.
5. Store listing: paste copy from `docs/store-listing.md`; upload `sofra_icon_512.png` (512×512), `sofra_feature.png` (1024×500), and ≥2 of `sofra_shot_*.png` (1080×1920).
6. Rollout gate: **personal Play account** ⇒ 12-tester/14-day closed testing before production; **internal testing is instant** (up to 100 testers) — start there.
7. Pre-submit smoke test on the STORE artifact: sign-in → household → log meal (incl. dine-out) → generate+accept plan → insights → avatar upload → delete account.

## WEBSITE — REVAMPED (done this session)
`sofra.savvylabs.dev` (GitHub Pages from `docs/`). Consumer launch pass DONE:
- **Screens fully generic + live** — all 9 `docs/assets/screens/*.png` (Rivera Family, global dishes) power BOTH the index gallery AND `guide.html`. The old numbered `docs/screens/*.png` are ORPHANED (referenced by nothing) — safe to delete later.
- **Nav de-dev'd** on index + guide: `Features · Guide · FAQ · Get the app` (Architecture/Security demoted to footer).
- **Open Graph / Twitter meta** added to index + guide (image = `docs/assets/og.png` = the 1200×630 brand OG). `docs/assets/lockup.png` also copied in for future header use.
- **FAQ section** added (recipe app? shared account? cost? privacy? platforms? planning?).
- **"Get Sofra"** section → launch/**"Coming soon on Google Play"** state (removed the dead `CHANGE-ME` GitHub link). ⚠️ ONE TODO left in `index.html` (HTML comment at #get): after Play launch, set the button href to the live listing URL + relabel "Get it on Google Play".
- theme-color + canonical added. Static, GitHub Pages, dark-aware.

## Verified on device (Pixel 10 Pro, this session)
Dine-out save works (no crash) · Home "Unique Dishes" = 1 (home-only) · Dish Library "Palak Paratha · Today · 1 dish" · Plan shows Saturday TODAY box + "Honest"→"Dosa" dine-out + home-only generated dishes + Calendar-matching fonts.

## Backlog / non-blocking / later
- **MVP2** (`docs/MVP2.md`): relative-date larger units (weeks/months), optional combined home+outside dish view, notification deep-linking.
- **Non-blocking watch-items:** orphaned Storage avatar bytes on account delete (rule-safe); spending-trend chart with exactly 2 equal data points (low-confidence react-native-chart-kit).
- **Post-launch:** crash reporting (Sentry vs Crashlytics — needs DSN), Firebase App Check (Play Integrity), reads Tier 3 (denormalized stats only before large histories), icon/splash PNG regen if desired, i18n.
- Regenerate the NotebookLM PDF screenshots if it'll be shared (currently pre-rename).

## History (condensed — earlier sessions)
- **PRs #2–#5 all MERGED** to `main`: #2 (14 UX/device fixes), #3 (GitHub Pages site), #4 (Firestore reads cache-first single-source — ~2-3k reads/day → ~1 load/launch), #5 (Insights/Dish window fixes, "Outside Meals" card, tappable Most Cooked, windowed dish counts, date-rollover recompute, pure dishStats/insightsRange utils).
- Cache-first store: all meals/dishes loaded once per session, screens filter in memory, writes update memory locally, re-read only on pull-to-refresh/household change (no onSnapshot by design). `useMealStore.test.ts` locks it.
- Prior verified-correct behaviors (not bugs): dish counts include thali sides (`items`); windowed vs all-time counts; kids excluded from family metrics.
